{
  "className":"swingtree.Border_Delegation_Spec",
  "title":"Delegating to the Border a Component already had",
  "narrative":"A Swing component usually arrives with a border its look and feel installed, and\n    SwingTree replaces that border with one of its own, so that it can paint styles,\n    animations and borders of your own. It does not throw the original away though:\n    as long as your style does not define a border itself, the original is still\n    painted, so a component you did not style in that respect keeps looking exactly\n    the way the look and feel intended.\n\n    \"Exactly\" is the word this specification is about. `getBorder()` really does hand\n    back a different object afterwards, so \"it still looks the same\" is a claim about\n    SwingTree rather than something Swing guarantees for us, and claims like that are\n    worth holding to pixels.\n\n    Every test below therefore renders a component SwingTree built next to a plain Swing\n    component wearing the very same border, and insists the two images are identical -\n    at a modest size and at a maximized one, on an ordinary screen and on a high\n    resolution one, for every border we could get our hands on.",
  "subjects":["swingtree.UI"],
  "statistics":{
    "runs":"26",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"A component keeps the border it came with, pixel for pixel. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [8]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [9]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [10]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [11]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [12]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with, pixel for pixel. [13]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The same border is worn by two panels: one built through SwingTree, and one a plain\n            `JPanel` which Swing paints all by itself. Rendering both has to produce the very\n            same image, whatever the border is and whatever size the panel was stretched to.\n\n            Note that the border sits on the panel before SwingTree ever sees it, which is the\n            situation a real application is in: the look and feel installed the border long\n            before any styling happened.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel handed to SwingTree with that border already on it, styled only with a padding.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)"]},

        {"kind":"and","text":"And a plain Swing panel wearing the very same border.","code":["var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both at the same size,","code":["var throughSwingTree = render(swingTreePanel, width, height)","var plainSwing       = render(plainPanel, width, height)"]},

        {"kind":"then","text":"SwingTree really did take the border over, so there was something to compare,","code":["swingTreePanel.getBorder() !== inheritedBorder","plainPanel.getBorder() === inheritedBorder"]},

        {"kind":"and","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"not a single pixel differs.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try every kind of border we can think of, on a modest panel and on a maximized one.","code":{"inheritedBorder":["new LineBorder(Color.RED, 1, false)","new LineBorder(Color.RED, 1, false)","new LineBorder(Color.BLUE, 5, false)","new LineBorder(new Color(0, 128, 0, 128), 3, true)","new MatteBorder(4, 8, 12, 16, Color.MAGENTA)","BorderFactory.createEtchedBorder()","BorderFactory.createRaisedSoftBevelBorder()","BorderFactory.createLoweredBevelBorder()","BorderFactory.createTitledBorder(\"A long enough title\")","BorderFactory.createDashedBorder(Color.BLUE)","BorderFactory.createCompoundBorder(new LineBorder(Color.RED, 2, true), new EmptyBorder(4, 4, 4, 4))","new RoundedOutlineBorder(1, 8)","new RoundedOutlineBorder(6, 24)","new RoundedOutlineBorder(3, 64)"],"width":["300","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400","1400"],"height":["200","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000","1000"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with on a high resolution screen too. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            On a high resolution display the graphics a component paints into carries a scale\n            factor, and at 125% or 150% - the two most common desktop scalings - a whole number\n            of component pixels is not a whole number of screen pixels. That is where a border\n            is most easily rendered a hair differently than Swing would render it, so it is\n            worth checking rather than assuming.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A SwingTree panel and a plain Swing panel, both wearing a thin antialiased outline.","code":["var inheritedBorder = new RoundedOutlineBorder(1, 12)","var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both onto a screen scaled by the given factor,","code":["var throughSwingTree = render(swingTreePanel, 1400, 1000, scale)","var plainSwing       = render(plainPanel, 1400, 1000, scale)"]},

        {"kind":"then","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"the two are still identical.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try the scalings a real desktop actually uses.","code":{"scale":["1.0d","1.25d","1.5d","2.0d"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with on a high resolution screen too. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            On a high resolution display the graphics a component paints into carries a scale\n            factor, and at 125% or 150% - the two most common desktop scalings - a whole number\n            of component pixels is not a whole number of screen pixels. That is where a border\n            is most easily rendered a hair differently than Swing would render it, so it is\n            worth checking rather than assuming.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A SwingTree panel and a plain Swing panel, both wearing a thin antialiased outline.","code":["var inheritedBorder = new RoundedOutlineBorder(1, 12)","var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both onto a screen scaled by the given factor,","code":["var throughSwingTree = render(swingTreePanel, 1400, 1000, scale)","var plainSwing       = render(plainPanel, 1400, 1000, scale)"]},

        {"kind":"then","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"the two are still identical.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try the scalings a real desktop actually uses.","code":{"scale":["1.0d","1.25d","1.5d","2.0d"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with on a high resolution screen too. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            On a high resolution display the graphics a component paints into carries a scale\n            factor, and at 125% or 150% - the two most common desktop scalings - a whole number\n            of component pixels is not a whole number of screen pixels. That is where a border\n            is most easily rendered a hair differently than Swing would render it, so it is\n            worth checking rather than assuming.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A SwingTree panel and a plain Swing panel, both wearing a thin antialiased outline.","code":["var inheritedBorder = new RoundedOutlineBorder(1, 12)","var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both onto a screen scaled by the given factor,","code":["var throughSwingTree = render(swingTreePanel, 1400, 1000, scale)","var plainSwing       = render(plainPanel, 1400, 1000, scale)"]},

        {"kind":"then","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"the two are still identical.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try the scalings a real desktop actually uses.","code":{"scale":["1.0d","1.25d","1.5d","2.0d"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component keeps the border it came with on a high resolution screen too. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            On a high resolution display the graphics a component paints into carries a scale\n            factor, and at 125% or 150% - the two most common desktop scalings - a whole number\n            of component pixels is not a whole number of screen pixels. That is where a border\n            is most easily rendered a hair differently than Swing would render it, so it is\n            worth checking rather than assuming.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A SwingTree panel and a plain Swing panel, both wearing a thin antialiased outline.","code":["var inheritedBorder = new RoundedOutlineBorder(1, 12)","var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both onto a screen scaled by the given factor,","code":["var throughSwingTree = render(swingTreePanel, 1400, 1000, scale)","var plainSwing       = render(plainPanel, 1400, 1000, scale)"]},

        {"kind":"then","text":"the border really did leave paint behind, so the comparison is not vacuous,","code":["paintedPixels(plainSwing) > 0"]},

        {"kind":"and","text":"the two are still identical.","code":["differingPixels(throughSwingTree, plainSwing) == 0"]},

        {"kind":"where","text":"We try the scalings a real desktop actually uses.","code":{"scale":["1.0d","1.25d","1.5d","2.0d"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The borders a real look and feel installs survive the delegation. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Hand written borders can only stand in for the real thing, so here we take the actual\n            borders out of the look and feels a SwingTree application is likely to run under and\n            hold them to the same standard.\n\n            Note that a border is asked to paint onto a plain `JPanel` here rather than onto the\n            component it was made for, and not every look and feel border draws anything in that\n            situation - Metal's scroll pane border, for one, draws nothing at all. That is what\n            the check on painted pixels below is for: a border that paints nothing would compare\n            two blank images and pass without testing anything.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We switch to the given look and feel and take the border it gives a component.","code":["Utility.setLaF(lookAndFeel)","var inheritedBorder = borderSource.call()"]},

        {"kind":"and","text":"A SwingTree panel and a plain Swing panel, both wearing it.","code":["var raw = new JPanel()","raw.setBorder(inheritedBorder)","var swingTreePanel = UI.of(raw).withStyle(it -> it.padding(1) ).get(JPanel)","var plainPanel = new JPanel()","plainPanel.setBorder(inheritedBorder)"]},

        {"kind":"when","text":"We render both, on an ordinary screen and on a fractionally scaled one,","code":["var plainOnAnOrdinaryScreen = render(plainPanel, 1400, 1000)","var plainOnAHiDpiScreen     = render(plainPanel, 1400, 1000, 1.25d)","var onAnOrdinaryScreen = differingPixels(render(swingTreePanel, 1400, 1000), plainOnAnOrdinaryScreen)","var onAHiDpiScreen     = differingPixels(render(swingTreePanel, 1400, 1000, 1.25d), plainOnAHiDpiScreen)"]},

        {"kind":"then","text":"this look and feel border really does paint onto a panel, so the comparison means something,","code":["paintedPixels(plainOnAnOrdinaryScreen) > 0","paintedPixels(plainOnAHiDpiScreen) > 0"]},

        {"kind":"and","text":"both renderings are identical.","code":["onAnOrdinaryScreen == 0","onAHiDpiScreen == 0"]},

        {"kind":"where","text":"We try the look and feels a SwingTree application is likely to run under.","code":{"lookAndFeel":["Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.NIMBUS","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.FLAT_BRIGHT","Utility.LaF.DEFAULT"],"borderSource":["{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JTextArea().getBorder() }","{ new JScrollPane().getBorder() }","{ new JTextField().getBorder() }","{ new JButton(\"x\").getBorder() }","{ new JTextField().getBorder() }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A component styled with a border of its own does not paint the one it came with.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The inherited border is a fallback, not an addition: as soon as your style defines a\n            border, that is the border, and the one the look and feel installed is not painted at\n            all. This is the condition under which the delegation above happens in the first\n            place, so it belongs next to it.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel with a look and feel border, styled with a SwingTree border on top.","code":["var counting = new CountingBorder(new LineBorder(Color.RED, 1, false))","var raw = new JPanel()","raw.setBorder(counting)","var panel = UI.of(raw).withStyle(it -> it.border(3, Color.BLUE) ).get(JPanel)"]},

        {"kind":"when","text":"We render the component,","code":["render(panel, 1400, 1000)"]},

        {"kind":"then","text":"the border it came with was never asked to paint anything.","code":["counting.timesPainted == 0"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
