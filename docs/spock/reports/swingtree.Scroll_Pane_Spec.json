{
  "className":"swingtree.Scroll_Pane_Spec",
  "title":"The Scroll Pane",
  "narrative":"Just like for any other main component in Swing,\n    Swing-Tree also supports a nice API for\n    building UIs with scroll panes.\n\n    A scroll pane is a component that allows\n    the user to scroll through a larger view\n    of a component. It is a container that\n    contains a single component, called the\n    viewport. The viewport is the area that\n    is actually visible to the user.\n\n    The scroll pane also contains a set of\n    scrollbars that allow the user to scroll\n    the viewport.\n\n    In this specification, we will see how\n    to build a scroll pane with Swing-Tree.",
  "subjects":["swingtree.UIForScrollPane","javax.swing.JScrollPane"],
  "statistics":{
    "runs":"25",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"Use the `UI.ScrollBarPolicy` enum to configure the scroll pane scroll bars.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Note that this is based on the rather non-descriptive `with` method.\n            We are using it because the type and name of the enum instance\n            already describe the scroll bar policy.\n            You will find this pattern in other places in Swing-Tree,\n            where the `with` method is used to configure a component\n            using an enum instance. \n        "]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom scroll bar policy.","code":["var ui =","        UI.scrollPane()","        .withScrollBarPolicy(UI.Active.NEVER)"]},

        {"kind":"and","text":"Then we build the scroll pane component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected scroll bar policies.","code":["scrollPane.getHorizontalScrollBarPolicy() == JScrollPane.HORIZONTAL_SCROLLBAR_NEVER","scrollPane.getVerticalScrollBarPolicy() == JScrollPane.VERTICAL_SCROLLBAR_NEVER"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Configure both the horizontal and vertical scroll policy individually.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Note that this is based on the rather non-descriptive `withHorizontal`\n            and `withVertical` methods.\n            We are using them because the type and name of the enum instance\n            already describe the scroll bar policy.\n            You will find this pattern in other places in Swing-Tree,\n            where the `with` method, or variations of it, are used to configure a component\n            in a fluent way.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom scroll bar policy.","code":["var ui =","        UI.scrollPane()","        .withHorizontalScrollBarPolicy(UI.Active.NEVER)","        .withVerticalScrollBarPolicy(UI.Active.ALWAYS)"]},

        {"kind":"and","text":"We actually build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected scroll bar policies.","code":["scrollPane.getHorizontalScrollBarPolicy() == JScrollPane.HORIZONTAL_SCROLLBAR_NEVER","scrollPane.getVerticalScrollBarPolicy() == JScrollPane.VERTICAL_SCROLLBAR_ALWAYS"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"We can configure the vertical and horizontal scroll bar scroll increment of a scroll pane.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":[]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom scroll increment.","code":["var ui =","        UI.scrollPane()","        .withHorizontalScrollIncrement(42)","        .withVerticalScrollIncrement(24)"]},

        {"kind":"and","text":"We actually build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected scroll increments.","code":["scrollPane.getHorizontalScrollBar().getUnitIncrement() == 42","scrollPane.getVerticalScrollBar().getUnitIncrement() == 24"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"We can configure the general scroll increment of the scroll pane scroll bars.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Note that this sets the scroll increment unit for both\n            the vertical and horizontal scroll bars.\n            So any previously set scroll increment unit for the\n            vertical or horizontal scroll bar will be overwritten.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom scroll increment.","code":["var ui =","        UI.scrollPane()","        .withScrollIncrement(42)"]},

        {"kind":"and","text":"We actually build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected scroll increments, both vertical and horizontally.","code":["scrollPane.getHorizontalScrollBar().getUnitIncrement() == 42","scrollPane.getVerticalScrollBar().getUnitIncrement() == 42"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The horizontal as well as vertical block scroll increment can be configured easily.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":[]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom block scroll increment.","code":["var ui =","        UI.scrollPane()","        .withHorizontalBlockScrollIncrement(42)","        .withVerticalBlockScrollIncrement(24)"]},

        {"kind":"and","text":"We actually build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected block scroll increments.","code":["scrollPane.getHorizontalScrollBar().getBlockIncrement() == 42","scrollPane.getVerticalScrollBar().getBlockIncrement() == 24"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Configure the block scroll increment for both scroll bars in one line.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Note that this sets the block scroll increment unit for both\n            the vertical and horizontal scroll bars.\n            So any previously set block scroll increment unit for the\n            vertical or horizontal scroll bar will be overwritten.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom block scroll increment.","code":["var ui =","        UI.scrollPane()","        .withBlockScrollIncrement(42)"]},

        {"kind":"and","text":"We actually build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected block scroll increments, both vertical and horizontally.","code":["scrollPane.getHorizontalScrollBar().getBlockIncrement() == 42","scrollPane.getVerticalScrollBar().getBlockIncrement() == 42"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Use a declarative configurator lambda instead implementing the `Scrollable` interface manually.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Classical Swing has the `Scrollable` interface, which is an optional\n            interface the scroll pane content component may implement in order\n            to configure how the component should be scrolled in the\n            viewport of the scroll pane.\n\n            This is a bit cumbersome to implement, and it prevents you from keeping your\n            UI declarative, as you have to use inheritance instead of composition.\n\n            Swing-Tree offers a solution to this through a declarative configurator lambda\n            passed to the ´UI.scrollPane(Configurator)´ factory method.\n            In this lambda, you can configure the scroll pane content component\n            behavior in the viewport as you would with the `Scrollable` interface.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We create a scroll pane with a custom scrollable configurator.","code":["var ui =","        UI.scrollPane( it -> it","            .prefSize(160, 130)","            .blockIncrement(7)","            .unitIncrement(5)","            .fitHeight(false)","            .fitWidth(true)","        )","        .add(","            UI.panel().withSize(140, 100)","            .add(","                UI.html(\"<p> This is a long text that should be scrollable. </p>\")","            )","        )"]},

        {"kind":"and","text":"We then build the component:","code":["var scrollPane = ui.get(JScrollPane)"]},

        {"kind":"expect","text":"The scroll pane has the expected scrollable behavior.","code":["scrollPane.getViewport().getView().getPreferredScrollableViewportSize() == new java.awt.Dimension(160, 130)","scrollPane.getViewport().getView().getScrollableBlockIncrement(null, 0,0) == 7","scrollPane.getViewport().getView().getScrollableUnitIncrement(null, 0,0) == 5","scrollPane.getViewport().getView().getScrollableTracksViewportHeight() == false","scrollPane.getViewport().getView().getScrollableTracksViewportWidth() == true"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The scroll configuration API produces a scroll pane whose content layout is calculated correctly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            In this little test we check if the layout of the content\n            of a scroll pane is calculated correctly, for both the\n            case where we use the scroll conf API to fit the viewport\n            and for the case where we do not.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A bit of content for the scroll pane content layout test.","code":["var TEXT =  \"This is a little story about a long sentence which is unfortunately too long to fit horizontally \" +","            \"placed on a single line of text in a panel inside a scroll pane. This is why it is a good idea \" +","            \"to place me in a scroll pane.\";"]},

        {"kind":"and","text":"We create a UI with a scroll pane layout.","code":["var ui =","    UI.frame(\"Scroll Pane Layout Test\")","    .peek(it->it.setPreferredSize(new Dimension(350, 550)))","    .add(","        UI.panel(\"wrap, fill\").withPrefSize(350, 550)","        .add(\"shrink\",UI.label(\"Not implementing Scrollable:\"))","        .add(\"grow, push\",","            UI.scrollPane().id(\"scroll-1\")","            .add(","                UI.panel(\"wrap\", \"\", \"[]push[]\").id(\"content-1\")","                .withBackground(UI.Color.LIGHT_GRAY)","                .add(UI.html(TEXT).withFont(new Font(\"Ubuntu\", Font.BOLD, 12)))","                .add(UI.html(\"END\").withFont(new Font(\"Ubuntu\", Font.BOLD, 12)))","            )","        )","        .add(\"shrink\",UI.label(\"Using Scroll Conf:\"))","        .add(\"grow, push\",","            UI.scrollPane(it -> it.fitWidth(true))","            .id(\"scroll-2\")","            .add(","                UI.panel(\"wrap\", \"\", \"[]push[]\").id(\"content-2\")","                .withBackground(UI.Color.LIGHT_GRAY)","                .add(UI.html(TEXT).withFont(new Font(\"Ubuntu\", Font.BOLD, 12)))","                .add(UI.html(\"END\").withFont(new Font(\"Ubuntu\", Font.BOLD, 12)))","            )","        )","    );"]},

        {"kind":"and","text":"We build the UI:","code":["var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We do the layout of the component...","code":["UI.runNow(()->frame.pack())"]},

        {"kind":"then","text":"The layout is calculated correctly.","code":["frame.getWidth() == 350","frame.getHeight() == 550"]},

        {"kind":"when","text":"We filter out the content panels...","code":["var scroll1 = new Utility.Query(frame).find(JScrollPane, \"scroll-1\").orElseThrow(NoSuchElementException::new)","var scroll2 = new Utility.Query(frame).find(JScrollPane, \"scroll-2\").orElseThrow(NoSuchElementException::new)","var content1 = new Utility.Query(frame).find(JPanel, \"content-1\").orElseThrow(NoSuchElementException::new)","var content2 = new Utility.Query(frame).find(JPanel, \"content-2\").orElseThrow(NoSuchElementException::new)"]},

        {"kind":"then","text":"The content panels have the covariance relative to their viewport.","code":["content1.getSize() != scroll1.getViewport().getSize()","content2.getSize() == scroll2.getViewport().getSize()"]},

        {"kind":"and","text":"","code":["content1.getWidth() > scroll1.getViewport().getWidth()","content1.getHeight() == scroll1.getViewport().getHeight()"]},

        {"kind":"and","text":"The content panels have the expected size.","code":["content1.getWidth() > 910","200 <= content1.getHeight() && content1.getHeight() <= 230","315 <= content2.getWidth()  && content2.getWidth()  <= 335","215 <= content2.getHeight() && content2.getHeight() <= 245"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The view of a configured scroll pane reports a preferred size which tracks its content.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When you configure a scroll pane through the `UI.scrollPane(Configurator)` factory\n            method, then SwingTree places a thin delegation box between the viewport and your\n            content component. The box exists so that the `Scrollable` behaviour can be supplied\n            declaratively instead of through inheritance, and it is meant to be completely\n            transparent with respect to sizing: whatever the content component says it needs,\n            the box says too.\n\n            This little test pins that transparency down for the case that tends to break it,\n            namely a content component which changes size after the box has already been\n            measured once. Note that we deliberately measure the box while it is in the\n            invalidated state, because that is the state it spends every layout pass in, and\n            because an invalid component does not receive `invalidate()` from its children a\n            second time. So a box which remembered its last measurement would answer with a\n            stale size here, and the scroll pane would then lay out and scroll a view that\n            does not exist anymore.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A label whose text we are going to grow later on, placed inside a configured scroll pane.","code":["var label = new JLabel(\"short\")","var ui =","    UI.frame(\"Scroll Pane Content Growth Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 200)))","    .add(","        UI.scrollPane( conf -> conf.fitWidth(true) ).id(\"scroll\")","        .add(","            UI.panel(\"fill, ins 0\").id(\"content\")","            .add(\"grow\", UI.of(label))","        )","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )"]},

        {"kind":"and","text":"We look up the scroll pane as well as the delegation box acting as its view.","code":["var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var content    = new Utility.Query(frame).find(JPanel, \"content\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"expect","text":"Right after the first layout the box already agrees with its content.","code":["UI.runAndGet( () -> view.getPreferredSize() ) == UI.runAndGet( () -> content.getPreferredSize() )"]},

        {"kind":"when","text":"The box enters the invalidated state it is in throughout every layout pass,","code":["UI.runNow( () -> view.invalidate() )"]},

        {"kind":"and","text":"and it is measured while being in that state, just like a layout pass would do,","code":["var widthBefore = UI.runAndGet( () -> view.getPreferredSize() ).width"]},

        {"kind":"and","text":"and the content then becomes substantially wider.","code":["UI.runNow( () -> label.setText(\"a piece of text which is a great deal longer than the previous one, by a wide margin indeed\") )"]},

        {"kind":"then","text":"The box grew together with its content instead of reporting the size it had before.","code":["UI.runAndGet( () -> view.getPreferredSize() ).width > widthBefore"]},

        {"kind":"and","text":"It still reports exactly what the content component itself reports.","code":["UI.runAndGet( () -> view.getPreferredSize() ) == UI.runAndGet( () -> content.getPreferredSize() )"]},

        {"kind":"when","text":"We lay the whole frame out again and shrink the content back down,","code":["UI.runNow( () -> frame.validate() )","var widthOfWideContent = UI.runAndGet( () -> view.getPreferredSize() ).width","UI.runNow( () -> label.setText(\"short\") )"]},

        {"kind":"then","text":"the box shrinks along with it.","code":["UI.runAndGet( () -> view.getPreferredSize() ).width < widthOfWideContent","UI.runAndGet( () -> view.getPreferredSize() ) == UI.runAndGet( () -> content.getPreferredSize() )"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Asking a configured scroll pane for a scroll increment does not measure its content.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A scroll pane configured through `UI.scrollPane(Configurator)` builds a fresh\n            configuration object for every single question the scroll pane asks it, and Swing\n            asks a handful of those per layout pass. Most of them are cheap questions whose\n            answer does not depend on how big the content is, like \"how far is one scroll\n            unit?\", and the same goes for every value the configurator supplies itself.\n\n            None of those should measure the content component, because measuring a densely\n            populated view is by far the most expensive thing a scroll pane ever does.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A content component whose layout manager counts how often it is measured.","code":["var layout  = new CountingLayout()","var ui =","    UI.frame(\"Scroll Increment Measurement Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 200)))","    .add(","        UI.scrollPane( conf -> conf","            .unitIncrement(7)","            .blockIncrement(13)","            .fitWidth(true)","            .fitHeight(false)","        )","        .id(\"scroll\")","        .add(UI.of(new JPanel(layout)).id(\"content\"))","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )"]},

        {"kind":"and","text":"We look up the content as well as the delegation box acting as the view.","code":["var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var content    = new Utility.Query(frame).find(JPanel, \"content\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"when","text":"We invalidate the content so that no cached measurement can answer on its behalf,","code":["UI.runNow( () -> content.invalidate() )"]},

        {"kind":"and","text":"we start counting from here,","code":["var measurementsBefore = layout.measurements"]},

        {"kind":"and","text":"and then ask the view every question whose answer the configurator already supplied.","code":["var unitIncrement  = UI.runAndGet( () -> view.getScrollableUnitIncrement(null, SwingConstants.VERTICAL, 1) )","var blockIncrement = UI.runAndGet( () -> view.getScrollableBlockIncrement(null, SwingConstants.VERTICAL, 1) )","var tracksWidth    = UI.runAndGet( () -> view.getScrollableTracksViewportWidth() )","var tracksHeight   = UI.runAndGet( () -> view.getScrollableTracksViewportHeight() )"]},

        {"kind":"then","text":"We receive exactly what was configured,","code":["unitIncrement  == 7","blockIncrement == 13","tracksWidth    == true","tracksHeight   == false"]},

        {"kind":"and","text":"and the content was not measured a single time for any of it.","code":["layout.measurements == measurementsBefore"]},

        {"kind":"when","text":"We ask for the preferred viewport size, which the configurator did not supply,","code":["var preferredViewportSize = UI.runAndGet( () -> view.getPreferredScrollableViewportSize() )"]},

        {"kind":"then","text":"the content is measured after all, because now the answer does depend on it.","code":["preferredViewportSize == new Dimension(300, 200)","layout.measurements > measurementsBefore"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A scroll pane still derives the viewport fitting defaults from the content it was given. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `fitWidth` and `fitHeight` flags of the scroll configuration API have defaults\n            which are derived from the content: a content component which is already smaller\n            than the viewport has nothing to scroll and may just as well be stretched to fill\n            it, whereas one which is larger must keep its own size so that it can be scrolled.\n\n            These defaults are computed only when they are actually read, so this test makes\n            sure that deferring them did not quietly change what they are.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A scroll pane with an identity configurator, so that all defaults survive.","code":["var content = new JPanel(new CountingLayout(width: contentWidth, height: contentHeight))","var ui =","    UI.frame(\"Scroll Fitting Defaults Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 300)))","    .add(","        UI.scrollPane( conf -> conf ).id(\"scroll\")","        .withScrollBarPolicy(UI.Active.NEVER)","        .add(UI.of(content))","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"expect","text":"The viewport only takes over a dimension the content does not need for itself.","code":["UI.runAndGet( () -> view.getScrollableTracksViewportWidth()  ) == expectedToFitWidth","UI.runAndGet( () -> view.getScrollableTracksViewportHeight() ) == expectedToFitHeight"]},

        {"kind":"where","text":"","code":{"contentWidth":["50","5000","50","5000"],"contentHeight":["50","50","5000","5000"],"expectedToFitWidth":["true","false","true","false"],"expectedToFitHeight":["true","true","false","false"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A scroll pane still derives the viewport fitting defaults from the content it was given. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `fitWidth` and `fitHeight` flags of the scroll configuration API have defaults\n            which are derived from the content: a content component which is already smaller\n            than the viewport has nothing to scroll and may just as well be stretched to fill\n            it, whereas one which is larger must keep its own size so that it can be scrolled.\n\n            These defaults are computed only when they are actually read, so this test makes\n            sure that deferring them did not quietly change what they are.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A scroll pane with an identity configurator, so that all defaults survive.","code":["var content = new JPanel(new CountingLayout(width: contentWidth, height: contentHeight))","var ui =","    UI.frame(\"Scroll Fitting Defaults Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 300)))","    .add(","        UI.scrollPane( conf -> conf ).id(\"scroll\")","        .withScrollBarPolicy(UI.Active.NEVER)","        .add(UI.of(content))","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"expect","text":"The viewport only takes over a dimension the content does not need for itself.","code":["UI.runAndGet( () -> view.getScrollableTracksViewportWidth()  ) == expectedToFitWidth","UI.runAndGet( () -> view.getScrollableTracksViewportHeight() ) == expectedToFitHeight"]},

        {"kind":"where","text":"","code":{"contentWidth":["50","5000","50","5000"],"contentHeight":["50","50","5000","5000"],"expectedToFitWidth":["true","false","true","false"],"expectedToFitHeight":["true","true","false","false"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A scroll pane still derives the viewport fitting defaults from the content it was given. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `fitWidth` and `fitHeight` flags of the scroll configuration API have defaults\n            which are derived from the content: a content component which is already smaller\n            than the viewport has nothing to scroll and may just as well be stretched to fill\n            it, whereas one which is larger must keep its own size so that it can be scrolled.\n\n            These defaults are computed only when they are actually read, so this test makes\n            sure that deferring them did not quietly change what they are.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A scroll pane with an identity configurator, so that all defaults survive.","code":["var content = new JPanel(new CountingLayout(width: contentWidth, height: contentHeight))","var ui =","    UI.frame(\"Scroll Fitting Defaults Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 300)))","    .add(","        UI.scrollPane( conf -> conf ).id(\"scroll\")","        .withScrollBarPolicy(UI.Active.NEVER)","        .add(UI.of(content))","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"expect","text":"The viewport only takes over a dimension the content does not need for itself.","code":["UI.runAndGet( () -> view.getScrollableTracksViewportWidth()  ) == expectedToFitWidth","UI.runAndGet( () -> view.getScrollableTracksViewportHeight() ) == expectedToFitHeight"]},

        {"kind":"where","text":"","code":{"contentWidth":["50","5000","50","5000"],"contentHeight":["50","50","5000","5000"],"expectedToFitWidth":["true","false","true","false"],"expectedToFitHeight":["true","true","false","false"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A scroll pane still derives the viewport fitting defaults from the content it was given. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `fitWidth` and `fitHeight` flags of the scroll configuration API have defaults\n            which are derived from the content: a content component which is already smaller\n            than the viewport has nothing to scroll and may just as well be stretched to fill\n            it, whereas one which is larger must keep its own size so that it can be scrolled.\n\n            These defaults are computed only when they are actually read, so this test makes\n            sure that deferring them did not quietly change what they are.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A scroll pane with an identity configurator, so that all defaults survive.","code":["var content = new JPanel(new CountingLayout(width: contentWidth, height: contentHeight))","var ui =","    UI.frame(\"Scroll Fitting Defaults Test\")","    .peek(it -> it.setPreferredSize(new Dimension(400, 300)))","    .add(","        UI.scrollPane( conf -> conf ).id(\"scroll\")","        .withScrollBarPolicy(UI.Active.NEVER)","        .add(UI.of(content))","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var view       = UI.runAndGet( () -> scrollPane.getViewport().getView() )"]},

        {"kind":"expect","text":"The viewport only takes over a dimension the content does not need for itself.","code":["UI.runAndGet( () -> view.getScrollableTracksViewportWidth()  ) == expectedToFitWidth","UI.runAndGet( () -> view.getScrollableTracksViewportHeight() ) == expectedToFitHeight"]},

        {"kind":"where","text":"","code":{"contentWidth":["50","5000","50","5000"],"contentHeight":["50","50","5000","5000"],"expectedToFitWidth":["true","false","true","false"],"expectedToFitHeight":["true","true","false","false"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can add a custom ´Scrollable´ component to a scroll pane with layout constraints that work.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            In this little test we check if the usage of layout constraints\n            when adding a custom Scrollable component to a scroll pane works.\n            Note that this is not something supported in regular Swing, \n            SwingTree however, will wrap and delegate your custom Scrollable\n            component in a JPanel, with a ´MigLayout´ instance that\n            respects the layout constraints you set.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            We create a UI with 2 different scroll panes each containing a custom\n            ´Scrollable´ component. The first one however receives no layout constraints,\n            while the second one does.\n        ","code":["var ui =","    UI.frame(\"Scrollable Pane Layout Test\")","    .peek(it->it.setPreferredSize(new Dimension(500, 300)))","    .add(","        UI.panel(\"fill\", \"[grow][grow]\").withPrefSize(500, 300)","        .add(\"shrink\",UI.label(\"Without Constraints:\"))","        .add(\"grow, push, wrap\",","            UI.scrollPane().id(\"scroll-1\")","            .add(","                UI.of(new CustomScrollablePanel()).withLayout(\"wrap 2\").id(\"content-1\")","                .add(UI.label(\"Forename: \"))","                .add(UI.textField(\"Joey\"))","                .add(UI.label(\"Surname: \"))","                .add(UI.textField(\"Carbstrong\"))","            )","        )","        .add(\"shrink\",UI.label(\"With Constraints:\"))","        .add(\"grow, push, wrap\",","            UI.scrollPane().id(\"scroll-2\")","            .id(\"scroll-2\")","            .add(\"grow, push\",","                UI.of(new CustomScrollablePanel()).id(\"content-2\")","                .add(UI.label(\"Forename: \"))","                .add(UI.textField(\"Joey\"))","                .add(UI.label(\"Surname: \"))","                .add(UI.textField(\"Carbstrong\"))","            )","        )","    );"]},

        {"kind":"and","text":"Then we just build the UI:","code":["var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We do the layout of the component...","code":["UI.runNow( () -> frame.pack() )"]},

        {"kind":"and","text":"We fetch the content panels so we can check their layout.","code":["var scroll1 = new Utility.Query(frame).find(JScrollPane, \"scroll-1\" ).orElseThrow(NoSuchElementException::new)","var scroll2 = new Utility.Query(frame).find(JScrollPane, \"scroll-2\" ).orElseThrow(NoSuchElementException::new)","var content1    = new Utility.Query(frame).find(JPanel,      \"content-1\").orElseThrow(NoSuchElementException::new)","var content2    = new Utility.Query(frame).find(JPanel,      \"content-2\").orElseThrow(NoSuchElementException::new)"]},

        {"kind":"then","text":"The content panels have the covariance relative to their viewport.","code":["content1.getSize().getWidth()  == scroll1.getViewport().getSize().getWidth()","content1.getSize().getHeight() <  scroll1.getViewport().getSize().getHeight()","content2.getSize().getWidth()  == scroll2.getViewport().getSize().getWidth()","content2.getSize().getHeight() <  scroll2.getViewport().getSize().getHeight()"]},

        {"kind":"and","text":"The content components are in fact of the custom scrollable type we use for testing.","code":["content1 instanceof CustomScrollablePanel","content2 instanceof CustomScrollablePanel"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"When you add a `Scrollable` component to a scroll pane with a layout constraint, SwingTree ignores the constraint and logs a warning.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When you add a component to a scroll pane together with a layout constraint, like\n            `add(\"width 250px\", UI.panel())`, SwingTree places a small panel with a `MigLayout`\n            between the viewport and your component, and that panel applies the constraint.\n            Plain Swing has no such feature, because a viewport holds exactly one component and\n            has no layout manager which could read a constraint.\n\n            For a component which implements `Scrollable`, SwingTree does not do this. It adds\n            the component directly to the viewport, does not apply the constraint, and logs a\n            warning which tells you so. In this scenario we add the same kind of custom\n            `Scrollable` component to two scroll panes, one of them with the constraint\n            `width 250px`, and we check that both components end up exactly as wide as their\n            viewport. So this scenario asserts on purpose that the constraint has no effect.\n\n            You may wonder why SwingTree does not simply apply the constraint. Swing's own\n            `Scrollable` components, like `JTextPane`, `JList`, `JTree` and `JTable`, only\n            behave correctly when their parent is the viewport. Before a `JTextPane` answers\n            `getScrollableTracksViewportWidth()` with `true`, it checks that\n            `SwingUtilities.getUnwrappedParent(this)` is a `JViewport`, and a `JTable` makes the\n            same check before it installs its column header into the scroll pane. With a panel\n            between the viewport and such a component, the text of a text pane would stop\n            wrapping, a list would no longer fill the viewport, and a table would have no header.\n\n            A `JLayer` between the viewport and the component would keep the viewport visible to\n            these checks, because `SwingUtilities.getUnwrappedParent` looks through a `JLayer`.\n            But a `JLayer` passes every `Scrollable` question on to the component unchanged. The\n            `visibleRect` given to `getScrollableUnitIncrement(..)` is measured from the top left\n            corner of the `JLayer`, not from the top left corner of the component. As soon as a\n            constraint moves the component inside the `JLayer`, for example with a gap above it,\n            the component would compute its scroll steps from the wrong position, and a `JList`\n            would scroll by the wrong amount and no longer stop at the start of a row.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A log spy, which records every message logged from here on.","code":["var logSpy = LogSpy.attach()"]},

        {"kind":"and","text":"Two scroll panes with the same kind of custom `Scrollable` component, one of them added with the constraint `width 250px`.","code":["var ui =","    UI.frame(\"Scrollable Constraints Test\")","    .peek(it -> it.setPreferredSize(new Dimension(600, 400)))","    .add(","        UI.panel(\"fill, wrap 1\", \"[grow]\", \"[grow][grow]\").withPrefSize(600, 400)","        .add(\"grow, push\",","            UI.scrollPane().id(\"scroll-unconstrained\")","            .add(","                UI.of(new CustomScrollablePanel()).id(\"content-unconstrained\")","                .add(UI.label(\"Hello\"))","            )","        )","        .add(\"grow, push\",","            UI.scrollPane().id(\"scroll-constrained\")","            .add(\"width 250px\",","                UI.of(new CustomScrollablePanel()).id(\"content-constrained\")","                .add(UI.label(\"Hello\"))","            )","        )","    )"]},

        {"kind":"and","text":"We build the UI and let it lay itself out:","code":["var frame = ui.get(JFrame)","UI.runNow( () -> frame.pack() )"]},

        {"kind":"and","text":"We look up the two scroll panes and their content components.","code":["var unconstrainedScroll  = new Utility.Query(frame).find(JScrollPane, \"scroll-unconstrained\").orElseThrow(NoSuchElementException::new)","var constrainedScroll    = new Utility.Query(frame).find(JScrollPane, \"scroll-constrained\").orElseThrow(NoSuchElementException::new)","var unconstrainedContent = new Utility.Query(frame).find(JPanel, \"content-unconstrained\").orElseThrow(NoSuchElementException::new)","var constrainedContent   = new Utility.Query(frame).find(JPanel, \"content-constrained\").orElseThrow(NoSuchElementException::new)"]},

        {"kind":"expect","text":"The component added without a constraint is as wide as its viewport, because its own `Scrollable` implementation asks for that.","code":["unconstrainedContent.getWidth() == unconstrainedScroll.getViewport().getWidth()"]},

        {"kind":"and","text":"The component added with `width 250px` is not 250 pixels wide, but exactly as wide as its viewport as well.","code":["constrainedContent.getWidth() == constrainedScroll.getViewport().getWidth()"]},

        {"kind":"and","text":"That viewport is wider than 250 pixels, so an applied constraint would have made a visible difference.","code":["constrainedScroll.getViewport().getWidth() > 250"]},

        {"kind":"and","text":"The component is the view of the viewport, so the viewport asks the component itself how to scroll it.","code":["UI.runAndGet( () -> constrainedScroll.getViewport().getView() ) == constrainedContent","UI.runAndGet( () -> constrainedScroll.getViewport().getView().getScrollableTracksViewportWidth()  ) == true","UI.runAndGet( () -> constrainedScroll.getViewport().getView().getScrollableTracksViewportHeight() ) == false","UI.runAndGet( () -> constrainedScroll.getViewport().getView().getScrollableUnitIncrement(null, SwingConstants.VERTICAL, 1) ) == 10","UI.runAndGet( () -> constrainedScroll.getViewport().getView().getScrollableBlockIncrement(null, SwingConstants.VERTICAL, 1) ) == 10"]},

        {"kind":"and","text":"SwingTree logged exactly one warning about it, which names the ignored constraint and the type of the component.","code":["var warningsAboutTheConstraint = logSpy.warnings().findAll { it.contains(CustomScrollablePanel.name) }","warningsAboutTheConstraint.size() == 1","warningsAboutTheConstraint[0].contains(\"width 250px\")"]},

        {"kind":"cleanup","text":"","code":["logSpy?.detach()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A text pane added to a scroll pane together with a layout constraint wraps its text at the width of the viewport. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When you put a `JTextPane` showing a long paragraph into a scroll pane, you expect\n            the viewport to decide how wide the text may be. The text pane is exactly as wide\n            as the viewport, every line breaks where the viewport ends, and you never see a\n            horizontal scroll bar. This scenario makes sure that all of this keeps working when\n            you add the text pane through `add(GROW_Y, UI.of(textPane))` instead of\n            `add(UI.of(textPane))`.\n\n            To see how a layout constraint could break it, let's look at how Swing decides the\n            width of the text pane. On every layout pass the `ViewportLayout` of the viewport asks its\n            view `getScrollableTracksViewportWidth()`. If the answer is `true`, the view gets\n            the width of the viewport. If it is `false`, the view gets its own preferred width,\n            which for a text pane is the width of the whole paragraph written on a single line.\n            A `JTextPane` answers `true` only after it has checked that its parent, as returned\n            by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`.\n\n            So if SwingTree put a panel between the viewport and the text pane, to have a layout\n            manager for the constraint, the text pane would find that panel instead of the\n            viewport and answer `false`. You would then see a paragraph that runs past the right\n            edge of the viewport, with no line breaks, and a horizontal scroll bar under it.\n            This is why SwingTree hands a component implementing `Scrollable` directly to the\n            viewport and does not apply a layout constraint to it.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A text pane showing a paragraph that is much wider than 250 pixels on a single line.","code":["var textPane = new JTextPane()","textPane.setContentType(\"text/html\")","textPane.setText(","    \"<html><p>Here you can edit the simulator setup in detail. Please be careful, \" +","    \"because wrong or inconsistent settings may break your configuration file!</p></html>\"",")"]},

        {"kind":"and","text":"A frame 250 pixels wide, with a scroll pane to which we add the text pane with a constraint.","code":["var ui =","    UI.frame(\"Text Pane Wrapping Test\")","    .peek(it -> it.setPreferredSize(new Dimension(250, 300)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(250, 300)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(constraint, UI.of(textPane))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"and","text":"Before any layout pass the text pane has no size yet, so it measures the paragraph on a single line.","code":["var widthOfTheParagraphOnASingleLine = UI.runAndGet( () -> textPane.getPreferredSize().width )"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportWidth = UI.runAndGet( () -> scrollPane.getViewport().getWidth() )"]},

        {"kind":"then","text":"The viewport really is too narrow for the paragraph on a single line, so the text has to wrap.","code":["viewportWidth < widthOfTheParagraphOnASingleLine"]},

        {"kind":"and","text":"The text pane is exactly as wide as the viewport.","code":["UI.runAndGet( () -> textPane.getWidth() ) == viewportWidth"]},

        {"kind":"and","text":"There is no horizontal scroll bar.","code":["UI.runAndGet( () -> scrollPane.getHorizontalScrollBar().isVisible() ) == false"]},

        {"kind":"and","text":"The text pane is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == textPane"]},

        {"kind":"where","text":"We use constraints which only ask the text pane to grow, so none of them has a width to impose.","code":{"constraint":["UILayoutConstants.GROW_Y","UILayoutConstants.GROW","\"grow, push\""]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A text pane added to a scroll pane together with a layout constraint wraps its text at the width of the viewport. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When you put a `JTextPane` showing a long paragraph into a scroll pane, you expect\n            the viewport to decide how wide the text may be. The text pane is exactly as wide\n            as the viewport, every line breaks where the viewport ends, and you never see a\n            horizontal scroll bar. This scenario makes sure that all of this keeps working when\n            you add the text pane through `add(GROW_Y, UI.of(textPane))` instead of\n            `add(UI.of(textPane))`.\n\n            To see how a layout constraint could break it, let's look at how Swing decides the\n            width of the text pane. On every layout pass the `ViewportLayout` of the viewport asks its\n            view `getScrollableTracksViewportWidth()`. If the answer is `true`, the view gets\n            the width of the viewport. If it is `false`, the view gets its own preferred width,\n            which for a text pane is the width of the whole paragraph written on a single line.\n            A `JTextPane` answers `true` only after it has checked that its parent, as returned\n            by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`.\n\n            So if SwingTree put a panel between the viewport and the text pane, to have a layout\n            manager for the constraint, the text pane would find that panel instead of the\n            viewport and answer `false`. You would then see a paragraph that runs past the right\n            edge of the viewport, with no line breaks, and a horizontal scroll bar under it.\n            This is why SwingTree hands a component implementing `Scrollable` directly to the\n            viewport and does not apply a layout constraint to it.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A text pane showing a paragraph that is much wider than 250 pixels on a single line.","code":["var textPane = new JTextPane()","textPane.setContentType(\"text/html\")","textPane.setText(","    \"<html><p>Here you can edit the simulator setup in detail. Please be careful, \" +","    \"because wrong or inconsistent settings may break your configuration file!</p></html>\"",")"]},

        {"kind":"and","text":"A frame 250 pixels wide, with a scroll pane to which we add the text pane with a constraint.","code":["var ui =","    UI.frame(\"Text Pane Wrapping Test\")","    .peek(it -> it.setPreferredSize(new Dimension(250, 300)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(250, 300)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(constraint, UI.of(textPane))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"and","text":"Before any layout pass the text pane has no size yet, so it measures the paragraph on a single line.","code":["var widthOfTheParagraphOnASingleLine = UI.runAndGet( () -> textPane.getPreferredSize().width )"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportWidth = UI.runAndGet( () -> scrollPane.getViewport().getWidth() )"]},

        {"kind":"then","text":"The viewport really is too narrow for the paragraph on a single line, so the text has to wrap.","code":["viewportWidth < widthOfTheParagraphOnASingleLine"]},

        {"kind":"and","text":"The text pane is exactly as wide as the viewport.","code":["UI.runAndGet( () -> textPane.getWidth() ) == viewportWidth"]},

        {"kind":"and","text":"There is no horizontal scroll bar.","code":["UI.runAndGet( () -> scrollPane.getHorizontalScrollBar().isVisible() ) == false"]},

        {"kind":"and","text":"The text pane is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == textPane"]},

        {"kind":"where","text":"We use constraints which only ask the text pane to grow, so none of them has a width to impose.","code":{"constraint":["UILayoutConstants.GROW_Y","UILayoutConstants.GROW","\"grow, push\""]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A text pane added to a scroll pane together with a layout constraint wraps its text at the width of the viewport. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When you put a `JTextPane` showing a long paragraph into a scroll pane, you expect\n            the viewport to decide how wide the text may be. The text pane is exactly as wide\n            as the viewport, every line breaks where the viewport ends, and you never see a\n            horizontal scroll bar. This scenario makes sure that all of this keeps working when\n            you add the text pane through `add(GROW_Y, UI.of(textPane))` instead of\n            `add(UI.of(textPane))`.\n\n            To see how a layout constraint could break it, let's look at how Swing decides the\n            width of the text pane. On every layout pass the `ViewportLayout` of the viewport asks its\n            view `getScrollableTracksViewportWidth()`. If the answer is `true`, the view gets\n            the width of the viewport. If it is `false`, the view gets its own preferred width,\n            which for a text pane is the width of the whole paragraph written on a single line.\n            A `JTextPane` answers `true` only after it has checked that its parent, as returned\n            by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`.\n\n            So if SwingTree put a panel between the viewport and the text pane, to have a layout\n            manager for the constraint, the text pane would find that panel instead of the\n            viewport and answer `false`. You would then see a paragraph that runs past the right\n            edge of the viewport, with no line breaks, and a horizontal scroll bar under it.\n            This is why SwingTree hands a component implementing `Scrollable` directly to the\n            viewport and does not apply a layout constraint to it.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A text pane showing a paragraph that is much wider than 250 pixels on a single line.","code":["var textPane = new JTextPane()","textPane.setContentType(\"text/html\")","textPane.setText(","    \"<html><p>Here you can edit the simulator setup in detail. Please be careful, \" +","    \"because wrong or inconsistent settings may break your configuration file!</p></html>\"",")"]},

        {"kind":"and","text":"A frame 250 pixels wide, with a scroll pane to which we add the text pane with a constraint.","code":["var ui =","    UI.frame(\"Text Pane Wrapping Test\")","    .peek(it -> it.setPreferredSize(new Dimension(250, 300)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(250, 300)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(constraint, UI.of(textPane))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"and","text":"Before any layout pass the text pane has no size yet, so it measures the paragraph on a single line.","code":["var widthOfTheParagraphOnASingleLine = UI.runAndGet( () -> textPane.getPreferredSize().width )"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportWidth = UI.runAndGet( () -> scrollPane.getViewport().getWidth() )"]},

        {"kind":"then","text":"The viewport really is too narrow for the paragraph on a single line, so the text has to wrap.","code":["viewportWidth < widthOfTheParagraphOnASingleLine"]},

        {"kind":"and","text":"The text pane is exactly as wide as the viewport.","code":["UI.runAndGet( () -> textPane.getWidth() ) == viewportWidth"]},

        {"kind":"and","text":"There is no horizontal scroll bar.","code":["UI.runAndGet( () -> scrollPane.getHorizontalScrollBar().isVisible() ) == false"]},

        {"kind":"and","text":"The text pane is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == textPane"]},

        {"kind":"where","text":"We use constraints which only ask the text pane to grow, so none of them has a width to impose.","code":{"constraint":["UILayoutConstants.GROW_Y","UILayoutConstants.GROW","\"grow, push\""]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Swing components implementing `Scrollable` fill the viewport of a scroll pane even when they are added with a layout constraint. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A `JList` with only two entries, placed into a much larger scroll pane, covers the\n            whole viewport. Its background fills the viewport, and when your users click into\n            the empty area below the last entry, the click still lands on the list. A `JTree`\n            and a `JTextArea` behave in the same way, and so does a `JTable` for which you\n            called `setFillsViewportHeight(true)`. This scenario makes sure that this keeps\n            working when you add the component with a layout constraint, like\n            `add(\"grow\", UI.of(list))`.\n\n            Each of these components implements the `Scrollable` methods\n            `getScrollableTracksViewportWidth()` and `getScrollableTracksViewportHeight()`,\n            and answers `true` when the viewport is larger than the preferred size of the\n            component. The `ViewportLayout` of the viewport reads those answers, and stretches\n            the component to the size of the viewport for every `true` it receives.\n\n            But before any of these components compares sizes, it checks that its parent,\n            as returned by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`. If it\n            is not, the answer is `false`, whatever the sizes are. So if SwingTree placed a\n            panel between the viewport and the component, to have a layout manager for the\n            constraint, the component would stay at its preferred size. You would see a small\n            list in the top left corner of an otherwise empty viewport, and a click next to it\n            would reach nothing.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A frame with a scroll pane of about 300 by 200 pixels, to which we add a small component with a constraint.","code":["var ui =","    UI.frame(\"Scrollable Filling Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(\"grow\", UI.of(component))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportSize = UI.runAndGet( () -> scrollPane.getViewport().getSize() )","var preferredSize = UI.runAndGet( () -> component.getPreferredSize() )"]},

        {"kind":"then","text":"The component wants less space than the viewport has, in both directions.","code":["preferredSize.width  < viewportSize.width","preferredSize.height < viewportSize.height"]},

        {"kind":"and","text":"It is nevertheless exactly as large as the viewport.","code":["UI.runAndGet( () -> component.getSize() ) == viewportSize"]},

        {"kind":"and","text":"The component is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == component"]},

        {"kind":"where","text":"We use a component of each kind of Swing `Scrollable` which checks its parent.","code":{"component":["new JTextArea(\"Hello\")","new JList<String>(\"Hello\", \"World\"] as String])","new JTree()","new JTable(\"Hello\", \"World\"]] as Object]], \"A\", \"B\"] as Object]).tap { it.setFillsViewportHeight(true) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Swing components implementing `Scrollable` fill the viewport of a scroll pane even when they are added with a layout constraint. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A `JList` with only two entries, placed into a much larger scroll pane, covers the\n            whole viewport. Its background fills the viewport, and when your users click into\n            the empty area below the last entry, the click still lands on the list. A `JTree`\n            and a `JTextArea` behave in the same way, and so does a `JTable` for which you\n            called `setFillsViewportHeight(true)`. This scenario makes sure that this keeps\n            working when you add the component with a layout constraint, like\n            `add(\"grow\", UI.of(list))`.\n\n            Each of these components implements the `Scrollable` methods\n            `getScrollableTracksViewportWidth()` and `getScrollableTracksViewportHeight()`,\n            and answers `true` when the viewport is larger than the preferred size of the\n            component. The `ViewportLayout` of the viewport reads those answers, and stretches\n            the component to the size of the viewport for every `true` it receives.\n\n            But before any of these components compares sizes, it checks that its parent,\n            as returned by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`. If it\n            is not, the answer is `false`, whatever the sizes are. So if SwingTree placed a\n            panel between the viewport and the component, to have a layout manager for the\n            constraint, the component would stay at its preferred size. You would see a small\n            list in the top left corner of an otherwise empty viewport, and a click next to it\n            would reach nothing.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A frame with a scroll pane of about 300 by 200 pixels, to which we add a small component with a constraint.","code":["var ui =","    UI.frame(\"Scrollable Filling Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(\"grow\", UI.of(component))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportSize = UI.runAndGet( () -> scrollPane.getViewport().getSize() )","var preferredSize = UI.runAndGet( () -> component.getPreferredSize() )"]},

        {"kind":"then","text":"The component wants less space than the viewport has, in both directions.","code":["preferredSize.width  < viewportSize.width","preferredSize.height < viewportSize.height"]},

        {"kind":"and","text":"It is nevertheless exactly as large as the viewport.","code":["UI.runAndGet( () -> component.getSize() ) == viewportSize"]},

        {"kind":"and","text":"The component is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == component"]},

        {"kind":"where","text":"We use a component of each kind of Swing `Scrollable` which checks its parent.","code":{"component":["new JTextArea(\"Hello\")","new JList<String>(\"Hello\", \"World\"] as String])","new JTree()","new JTable(\"Hello\", \"World\"]] as Object]], \"A\", \"B\"] as Object]).tap { it.setFillsViewportHeight(true) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Swing components implementing `Scrollable` fill the viewport of a scroll pane even when they are added with a layout constraint. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A `JList` with only two entries, placed into a much larger scroll pane, covers the\n            whole viewport. Its background fills the viewport, and when your users click into\n            the empty area below the last entry, the click still lands on the list. A `JTree`\n            and a `JTextArea` behave in the same way, and so does a `JTable` for which you\n            called `setFillsViewportHeight(true)`. This scenario makes sure that this keeps\n            working when you add the component with a layout constraint, like\n            `add(\"grow\", UI.of(list))`.\n\n            Each of these components implements the `Scrollable` methods\n            `getScrollableTracksViewportWidth()` and `getScrollableTracksViewportHeight()`,\n            and answers `true` when the viewport is larger than the preferred size of the\n            component. The `ViewportLayout` of the viewport reads those answers, and stretches\n            the component to the size of the viewport for every `true` it receives.\n\n            But before any of these components compares sizes, it checks that its parent,\n            as returned by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`. If it\n            is not, the answer is `false`, whatever the sizes are. So if SwingTree placed a\n            panel between the viewport and the component, to have a layout manager for the\n            constraint, the component would stay at its preferred size. You would see a small\n            list in the top left corner of an otherwise empty viewport, and a click next to it\n            would reach nothing.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A frame with a scroll pane of about 300 by 200 pixels, to which we add a small component with a constraint.","code":["var ui =","    UI.frame(\"Scrollable Filling Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(\"grow\", UI.of(component))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportSize = UI.runAndGet( () -> scrollPane.getViewport().getSize() )","var preferredSize = UI.runAndGet( () -> component.getPreferredSize() )"]},

        {"kind":"then","text":"The component wants less space than the viewport has, in both directions.","code":["preferredSize.width  < viewportSize.width","preferredSize.height < viewportSize.height"]},

        {"kind":"and","text":"It is nevertheless exactly as large as the viewport.","code":["UI.runAndGet( () -> component.getSize() ) == viewportSize"]},

        {"kind":"and","text":"The component is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == component"]},

        {"kind":"where","text":"We use a component of each kind of Swing `Scrollable` which checks its parent.","code":{"component":["new JTextArea(\"Hello\")","new JList<String>(\"Hello\", \"World\"] as String])","new JTree()","new JTable(\"Hello\", \"World\"]] as Object]], \"A\", \"B\"] as Object]).tap { it.setFillsViewportHeight(true) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Swing components implementing `Scrollable` fill the viewport of a scroll pane even when they are added with a layout constraint. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A `JList` with only two entries, placed into a much larger scroll pane, covers the\n            whole viewport. Its background fills the viewport, and when your users click into\n            the empty area below the last entry, the click still lands on the list. A `JTree`\n            and a `JTextArea` behave in the same way, and so does a `JTable` for which you\n            called `setFillsViewportHeight(true)`. This scenario makes sure that this keeps\n            working when you add the component with a layout constraint, like\n            `add(\"grow\", UI.of(list))`.\n\n            Each of these components implements the `Scrollable` methods\n            `getScrollableTracksViewportWidth()` and `getScrollableTracksViewportHeight()`,\n            and answers `true` when the viewport is larger than the preferred size of the\n            component. The `ViewportLayout` of the viewport reads those answers, and stretches\n            the component to the size of the viewport for every `true` it receives.\n\n            But before any of these components compares sizes, it checks that its parent,\n            as returned by `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`. If it\n            is not, the answer is `false`, whatever the sizes are. So if SwingTree placed a\n            panel between the viewport and the component, to have a layout manager for the\n            constraint, the component would stay at its preferred size. You would see a small\n            list in the top left corner of an otherwise empty viewport, and a click next to it\n            would reach nothing.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A frame with a scroll pane of about 300 by 200 pixels, to which we add a small component with a constraint.","code":["var ui =","    UI.frame(\"Scrollable Filling Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            UI.scrollPane().id(\"scroll\")","            .add(\"grow\", UI.of(component))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scrollPane = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)","var viewportSize = UI.runAndGet( () -> scrollPane.getViewport().getSize() )","var preferredSize = UI.runAndGet( () -> component.getPreferredSize() )"]},

        {"kind":"then","text":"The component wants less space than the viewport has, in both directions.","code":["preferredSize.width  < viewportSize.width","preferredSize.height < viewportSize.height"]},

        {"kind":"and","text":"It is nevertheless exactly as large as the viewport.","code":["UI.runAndGet( () -> component.getSize() ) == viewportSize"]},

        {"kind":"and","text":"The component is the view of the viewport, with no panel in between.","code":["UI.runAndGet( () -> scrollPane.getViewport().getView() ) == component"]},

        {"kind":"where","text":"We use a component of each kind of Swing `Scrollable` which checks its parent.","code":{"component":["new JTextArea(\"Hello\")","new JList<String>(\"Hello\", \"World\"] as String])","new JTree()","new JTable(\"Hello\", \"World\"]] as Object]], \"A\", \"B\"] as Object]).tap { it.setFillsViewportHeight(true) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A table added to a scroll pane together with a layout constraint still shows its column header. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The column names of a `JTable` are not painted by the table itself. They are painted\n            by a separate `JTableHeader` component, which lives in the column header area of the\n            `JScrollPane`, above the viewport, so that the names stay visible while your users\n            scroll through the rows. This scenario makes sure that you get this header whether\n            you add the table through `add(UI.of(table))` or through `add(\"grow\", UI.of(table))`,\n            and whether you configured the scroll pane through `UI.scrollPane()` or\n            `UI.scrollPane(Configurator)`.\n\n            Swing puts the header there without you asking. When the table becomes part of a\n            displayable window, `JTable.addNotify()` calls `configureEnclosingScrollPane()`, and\n            that method calls `scrollPane.setColumnHeaderView(getTableHeader())`.\n\n            It does so only if the parent of the table, as returned by\n            `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`, and the parent of that\n            viewport is a `JScrollPane`. So if SwingTree placed a panel between the viewport and\n            the table, to have a layout manager for the constraint, the table would quietly skip\n            installing its header. You would see the rows of the table without any column names\n            above them, and nothing would be logged.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A table with two named columns.","code":["var table = new JTable([[\"Hello\", \"World\"]] as Object[][], [\"A\", \"B\"] as Object[])"]},

        {"kind":"and","text":"A frame with a scroll pane to which we add the table with a constraint.","code":["var ui =","    UI.frame(\"Table Header Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            scrollPane.id(\"scroll\")","            .add(\"grow\", UI.of(table))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scroll = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)"]},

        {"kind":"then","text":"The header of the table sits in the column header area of the scroll pane.","code":["UI.runAndGet( () -> scroll.getColumnHeader()?.getView() ) == table.getTableHeader()"]},

        {"kind":"where","text":"We use a plain scroll pane as well as one configured through a configurator lambda.","code":{"scrollPane":["UI.scrollPane()","UI.scrollPane( conf -> conf.fitWidth(true) )"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A table added to a scroll pane together with a layout constraint still shows its column header. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The column names of a `JTable` are not painted by the table itself. They are painted\n            by a separate `JTableHeader` component, which lives in the column header area of the\n            `JScrollPane`, above the viewport, so that the names stay visible while your users\n            scroll through the rows. This scenario makes sure that you get this header whether\n            you add the table through `add(UI.of(table))` or through `add(\"grow\", UI.of(table))`,\n            and whether you configured the scroll pane through `UI.scrollPane()` or\n            `UI.scrollPane(Configurator)`.\n\n            Swing puts the header there without you asking. When the table becomes part of a\n            displayable window, `JTable.addNotify()` calls `configureEnclosingScrollPane()`, and\n            that method calls `scrollPane.setColumnHeaderView(getTableHeader())`.\n\n            It does so only if the parent of the table, as returned by\n            `SwingUtilities.getUnwrappedParent(this)`, is a `JViewport`, and the parent of that\n            viewport is a `JScrollPane`. So if SwingTree placed a panel between the viewport and\n            the table, to have a layout manager for the constraint, the table would quietly skip\n            installing its header. You would see the rows of the table without any column names\n            above them, and nothing would be logged.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A table with two named columns.","code":["var table = new JTable([[\"Hello\", \"World\"]] as Object[][], [\"A\", \"B\"] as Object[])"]},

        {"kind":"and","text":"A frame with a scroll pane to which we add the table with a constraint.","code":["var ui =","    UI.frame(\"Table Header Test\")","    .peek(it -> it.setPreferredSize(new Dimension(300, 200)))","    .add(","        UI.panel(\"fill, ins 0\").withPrefSize(300, 200)","        .add(UILayoutConstants.GROW,","            scrollPane.id(\"scroll\")","            .add(\"grow\", UI.of(table))","        )","    )","var frame = ui.get(JFrame)"]},

        {"kind":"when","text":"We let the frame lay itself out.","code":["UI.runNow( () -> frame.pack() )","var scroll = new Utility.Query(frame).find(JScrollPane, \"scroll\").orElseThrow(NoSuchElementException::new)"]},

        {"kind":"then","text":"The header of the table sits in the column header area of the scroll pane.","code":["UI.runAndGet( () -> scroll.getColumnHeader()?.getView() ) == table.getTableHeader()"]},

        {"kind":"where","text":"We use a plain scroll pane as well as one configured through a configurator lambda.","code":{"scrollPane":["UI.scrollPane()","UI.scrollPane( conf -> conf.fitWidth(true) )"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
