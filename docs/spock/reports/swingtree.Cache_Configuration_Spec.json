{
  "className":"swingtree.Cache_Configuration_Spec",
  "title":"Configuring the Memory/CPU Trade-off",
  "narrative":"Every one of SwingTree's internal rendering caches – style layer images,\n    noise tiles, shadow gradients and text layouts – draws its\n    memory budget from a single, unified source: the `CacheMode` configured on\n    the `SwingTree` library context, combined with the amount of system RAM.\n\n    A higher mode keeps more rendered results in memory so subsequent repaints\n    can blit them instead of re-rendering (faster, but more memory); the\n    `DISABLED` mode turns caching off entirely. The mode can be selected at\n    startup or changed at runtime, in which case the caches drop their now-excess\n    memory immediately and repopulate under the new budget.\n\n    These scenarios pin the contract down through the *public* API\n    (`SwingTree.get().getCacheMode()` / `setCacheMode(..)` and the\n    `ComponentExtension` cache observers) so they survive internal refactors; the\n    one non-public touch-point is the `CacheBudget.UNITS_OVERRIDE` test hook, used\n    purely to make the otherwise RAM-dependent budget deterministic regardless of\n    the runner's physical memory.",
  "subjects":["swingtree.SwingTree","swingtree.SwingTreeInitConfig","swingtree.style.ComponentExtension"],
  "statistics":{
    "runs":"5",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"The default cache mode is BALANCED, and it can be changed at runtime.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":[]
      },
      "blocks":[
        {"kind":"expect","text":"A freshly configured library context reports the balanced default.","code":["SwingTree.get().getCacheMode() == CacheMode.BALANCED"]},

        {"kind":"when","text":"We pick a different mode at runtime...","code":["SwingTree.get().setCacheMode(CacheMode.AGGRESSIVE)"]},

        {"kind":"then","text":"...it is reflected immediately.","code":["SwingTree.get().getCacheMode() == CacheMode.AGGRESSIVE"]},

        {"kind":"when","text":"And we turn caching off entirely.","code":["SwingTree.get().setCacheMode(CacheMode.DISABLED)"]},

        {"kind":"then","text":"That too is reflected.","code":["SwingTree.get().getCacheMode() == CacheMode.DISABLED"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The DISABLED mode turns style-layer caching off too.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":[]
      },
      "blocks":[
        {"kind":"given","text":"Caching is off and a heavily styled button.","code":["SwingTree.get().setCacheMode(CacheMode.DISABLED)","var button =","    UI.button(\"Hello!\")","      .withStyle( it -> it","            .size(120, 60)","            .borderRadius(20)","            .backgroundColor(Color.BLUE)","            .foundationColor(Color.WHITE)","      )","      .get(JButton)","var ext = ComponentExtension.from(button)"]},

        {"kind":"when","text":"We render it twice.","code":["2.times { Utility.renderSingleComponent(button) }"]},

        {"kind":"then","text":"Its background layer is never promoted to a cached image.","code":["!ext.cachedRendering(UI.Layer.BACKGROUND).isPresent()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Raising the budget at runtime takes effect without re-initializing the library.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Cache size limits are read *live* on every paint, not snapshotted once at\n            class-load time. Observed through the style-layer cache: with a zero budget\n            nothing is admitted, and raising the budget at runtime makes subsequent\n            paints start caching — no re-initialization required.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A zero budget (a maximally constrained machine) and a styled label painted repeatedly.","code":["CacheBudget.UNITS_OVERRIDE = 0","ComponentExtension.updateAllCachesFromLibraryConfig() // clears every global rendering cache","var label = UI.label(\"Platform 9\")","              .withStyle({ it.backgroundColor(java.awt.Color.BLUE).borderRadius(8) })","              .get(javax.swing.JLabel)","5.times { paint(label) }"]},

        {"kind":"expect","text":"Nothing was admitted to the style-layer cache while the budget was zero.","code":["ComponentExtension.globalRenderCacheEntryCounts().toMap()[\"style layers\"] == 0"]},

        {"kind":"when","text":"We raise the budget at runtime and paint a few more times.","code":["CacheBudget.UNITS_OVERRIDE = 10","5.times { paint(label) }"]},

        {"kind":"then","text":"Caching kicks in — the live limit reacted to the runtime change.","code":["ComponentExtension.globalRenderCacheEntryCounts().toMap()[\"style layers\"] > 0"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Stretch tiling can be turned off at runtime, restoring exact-size cache keys.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Size independent (stretch tiled) render caching is what makes\n            resizing styled components cheap: eligible styles share one cached\n            rendering across all sizes. Because it reconstructs pixels rather\n            than re-rendering them, it also ships with a safety hatch:\n            `SwingTree.get().setCacheTilingEnabled(false)` (or the system\n            property `swingtree.cacheMode.tiling=false`) restores the classic\n            exact-size cache keying, where every resize re-renders. This\n            scenario pins the whole round trip: on -> off -> on again.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A deterministic budget and a styled button (flat colors: stretch tileable).","code":["CacheBudget.UNITS_OVERRIDE = 10","ComponentExtension.updateAllCachesFromLibraryConfig()","var button =","    UI.button(\"Switch\")","      .withStyle( it -> it","            .borderRadius(16)","            .backgroundColor(new Color(60, 130, 90))","            .foundationColor(new Color(240, 240, 235))","      )","      .get(JButton)","button.setSize(140, 60)","var ext = ComponentExtension.from(button)"]},

        {"kind":"expect","text":"Stretch tiling is enabled by default.","code":["SwingTree.get().isCacheTilingEnabled()"]},

        {"kind":"when","text":"We warm the cache and then resize, with tiling enabled.","code":["2.times { Utility.renderSingleComponent(button) }","int missesWhileTiled = ext.cacheMissCount(UI.Layer.BACKGROUND)","button.setSize(280, 90)","Utility.renderSingleComponent(button)"]},

        {"kind":"then","text":"The resize did not require any fresh rendering.","code":["ext.cacheMissCount(UI.Layer.BACKGROUND) == missesWhileTiled"]},

        {"kind":"when","text":"We flip the safety hatch and resize twice more.","code":["SwingTree.get().setCacheTilingEnabled(false)","button.setSize(310, 100)","Utility.renderSingleComponent(button)","int missesAfterFirstUntiledPaint = ext.cacheMissCount(UI.Layer.BACKGROUND)","button.setSize(340, 110)","Utility.renderSingleComponent(button)"]},

        {"kind":"then","text":"The flag is off, and now every new size requires a fresh rendering - the classic behavior.","code":["!SwingTree.get().isCacheTilingEnabled()","ext.cacheMissCount(UI.Layer.BACKGROUND) > missesAfterFirstUntiledPaint"]},

        {"kind":"when","text":"We re-enable tiling, let one paint repopulate the canonical rendering, and resize again.","code":["SwingTree.get().setCacheTilingEnabled(true)","button.setSize(360, 120)","Utility.renderSingleComponent(button)","int missesAfterRepopulation = ext.cacheMissCount(UI.Layer.BACKGROUND)","button.setSize(400, 130)","Utility.renderSingleComponent(button)"]},

        {"kind":"then","text":"Resizing is miss-resistant again.","code":["SwingTree.get().isCacheTilingEnabled()","ext.cacheMissCount(UI.Layer.BACKGROUND) == missesAfterRepopulation"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The live cache monitoring snapshot covers every global rendering cache.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `ComponentExtension.globalRenderCacheEntryCounts()` is the observation point\n            for cache monitoring — the dev tool displays it live, and tests pin the\n            budget contract through it. Its keys are a stable, display-friendly\n            inventory of SwingTree's global rendering caches.\n        "]
      },
      "blocks":[
        {"kind":"expect","text":"One entry per global rendering cache, in stable order, never negative.","code":["ComponentExtension.globalRenderCacheEntryCounts().keySet().toList() == [\"style layers\", \"text layouts\", \"noise paints\", \"shadow gradients\"]","ComponentExtension.globalRenderCacheEntryCounts().values().every { it >= 0 }"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
