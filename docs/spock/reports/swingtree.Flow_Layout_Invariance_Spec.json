{
  "className":"swingtree.Flow_Layout_Invariance_Spec",
  "title":"Flow Layout Invariance",
  "narrative":"The `ResponsiveGridFlowLayout` is modelled after the plain old `FlowLayout`\n    of the JDK, and it extends it with responsive cell spans. A hard requirement\n    follows from that: **a responsive grid flow layout which was not given any\n    responsive behaviour must behave exactly like a `FlowLayout`**. Anybody\n    swapping the one for the other should not be able to tell the difference,\n    which is what makes `withFlowLayout()` a safe replacement for\n    `withLayout(new FlowLayout(..))`.\n\n    This specification pins that equivalence down across alignments, gap sizes,\n    container widths, component orientations, container insets, hidden children\n    and baseline alignment. Every feature builds the same UI twice, once with\n    each layout manager, and then compares the results.\n\n    There is exactly one deliberate exception, and the last two features below\n    describe it: `preferredLayoutSize`. A `FlowLayout` always reports the size of\n    a single row, even when its children demonstrably wrap into several. The\n    responsive grid flow layout reports the height its rows really occupy,\n    because a wrapping layout that misreports its height cannot be nested and\n    cannot be scrolled correctly.",
  "subjects":["swingtree.layout.ResponsiveGridFlowLayout"],
  "statistics":{
    "runs":"84",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [8]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [9]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [10]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [11]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [12]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [13]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [14]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [15]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [16]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [17]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [18]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [19]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A responsive flow layout places its components exactly where a `FlowLayout` places them. [20]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The core of the equivalence: for the very same children and the very\n            same container width, every single component has to end up at exactly\n            the same position and size under both layout managers.\n\n            The widths in this table are chosen to span the interesting range. The\n            five boxes need 180 pixels to fit into a single row when the gaps are\n            5, so the narrow widths in this table genuinely force the layout to\n            wrap into several rows.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with identical children, one per layout manager.","code":["var ours = _panelOf(","                new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are given the same size and told to lay out their children,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"every child ends up in exactly the same place.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING"],"horizontalGap":["0","0","0","5","5","5","7","0","5","5","7","0","5","5","7","0","5","5","0","5","5"],"verticalGap":["0","0","0","5","5","5","3","0","5","5","3","0","5","5","3","0","5","5","0","5","5"],"width":["0","60","300","30","100","300","175","60","100","300","175","60","100","300","175","60","100","300","60","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [8]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [9]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [10]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A right-to-left container is laid out exactly like a right-to-left `FlowLayout` container. [11]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers mirror their rows when the container has a\n            right-to-left component orientation, and the `LEADING` and `TRAILING`\n            alignments swap sides with it. This has to agree as well, otherwise\n            the two would drift apart for anybody building a UI for a\n            right-to-left locale.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels with the same component orientation, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), leftToRight, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), leftToRight, false","            )"]},

        {"kind":"expect","text":"The two panels really do carry the orientation we asked for.","code":["ours.getComponentOrientation().isLeftToRight() == leftToRight","awts.getComponentOrientation().isLeftToRight() == leftToRight"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the mirrored positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.TRAILING","UI.HorizontalAlignment.LEADING","UI.HorizontalAlignment.TRAILING"],"leftToRight":["false","false","false","false","false","false","false","false","false","false","true","true"],"width":["100","300","100","300","100","300","100","300","100","300","100","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The insets of the container are honoured exactly like a `FlowLayout` honours them. [8]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A border on the container shrinks the space available for the rows and\n            shifts them right and down. Both layout managers read those insets\n            from the container itself, so the outcome has to be identical.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels carrying the same asymmetric border, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), FIVE_BOXES(), true, true)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                FIVE_BOXES(), true, true","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the inset rows agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["60","175","300","60","175","300","60","175","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Children of differing heights are aligned in their row exactly like a `FlowLayout` aligns them. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A row is as tall as its tallest child, and the shorter children are\n            centered within it. Feeding the layout children of four different\n            heights makes any disagreement about the row height, or about the\n            vertical centering inside it, immediately visible.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels holding four equally wide but differently tall children.","code":["var boxes = [[40, 10], [40, 30], [40, 20], [40, 50]]","var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, 5, 5), boxes, true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5),","                boxes, true, false","            )"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the row heights and the vertical placement inside them agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["60","110","300","60","110","300","110"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Baseline aligned rows come out exactly like the baseline aligned rows of a `FlowLayout`. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Both layout managers can align the children of a row on their text\n            baseline instead of centering them. A baseline aligned row is as tall\n            as the deepest ascent plus the deepest descent found in it, and those\n            two may well come from two different children — which makes such a row\n            taller than any single child in it.\n\n            The two labels below are picked to make exactly that happen: a large\n            one whose baseline sits low in its bounds, and a small one padded from\n            below so that its baseline sits high in its bounds.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels with deliberately mismatched baselines.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 300)","var awtBounds = _laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"the baseline shifted positions agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"width":["30","100","300","100","300","100","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred size of a baseline aligned row is the one a `FlowLayout` reports. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Aligning on the baseline does not only move the children around, it\n            also makes the row taller than its tallest child whenever the deepest\n            ascent and the deepest descent come from two different children. Both\n            layout managers have to fold that into the height they report.\n\n            The widths in this table all leave the two labels in a single row, so\n            there is no wrapping involved and the two layout managers have to\n            agree exactly — this is the guard which keeps the responsive layout\n            from quietly reporting the height of its tallest child instead.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels whose baselines sit at very different depths.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at a width which keeps the two labels in one row,","code":["_laidOutBoundsOf(ours, width, 300)","_laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"they agree on the height that aligning the baselines demands.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"and","text":"\n            And that height really is more than the tallest of the two labels\n            needs on its own, which is what makes this a meaningful check.\n        ","code":["var tallest = Math.max((int) ours.getComponent(0).getPreferredSize().height,","                       (int) ours.getComponent(1).getPreferredSize().height)","ours.getLayout().preferredLayoutSize(ours).height > tallest + 10"]},

        {"kind":"where","text":"A width of 0 is the state before the first layout pass.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["0","300","0","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred size of a baseline aligned row is the one a `FlowLayout` reports. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Aligning on the baseline does not only move the children around, it\n            also makes the row taller than its tallest child whenever the deepest\n            ascent and the deepest descent come from two different children. Both\n            layout managers have to fold that into the height they report.\n\n            The widths in this table all leave the two labels in a single row, so\n            there is no wrapping involved and the two layout managers have to\n            agree exactly — this is the guard which keeps the responsive layout\n            from quietly reporting the height of its tallest child instead.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels whose baselines sit at very different depths.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at a width which keeps the two labels in one row,","code":["_laidOutBoundsOf(ours, width, 300)","_laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"they agree on the height that aligning the baselines demands.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"and","text":"\n            And that height really is more than the tallest of the two labels\n            needs on its own, which is what makes this a meaningful check.\n        ","code":["var tallest = Math.max((int) ours.getComponent(0).getPreferredSize().height,","                       (int) ours.getComponent(1).getPreferredSize().height)","ours.getLayout().preferredLayoutSize(ours).height > tallest + 10"]},

        {"kind":"where","text":"A width of 0 is the state before the first layout pass.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["0","300","0","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred size of a baseline aligned row is the one a `FlowLayout` reports. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Aligning on the baseline does not only move the children around, it\n            also makes the row taller than its tallest child whenever the deepest\n            ascent and the deepest descent come from two different children. Both\n            layout managers have to fold that into the height they report.\n\n            The widths in this table all leave the two labels in a single row, so\n            there is no wrapping involved and the two layout managers have to\n            agree exactly — this is the guard which keeps the responsive layout\n            from quietly reporting the height of its tallest child instead.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels whose baselines sit at very different depths.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at a width which keeps the two labels in one row,","code":["_laidOutBoundsOf(ours, width, 300)","_laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"they agree on the height that aligning the baselines demands.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"and","text":"\n            And that height really is more than the tallest of the two labels\n            needs on its own, which is what makes this a meaningful check.\n        ","code":["var tallest = Math.max((int) ours.getComponent(0).getPreferredSize().height,","                       (int) ours.getComponent(1).getPreferredSize().height)","ours.getLayout().preferredLayoutSize(ours).height > tallest + 10"]},

        {"kind":"where","text":"A width of 0 is the state before the first layout pass.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["0","300","0","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred size of a baseline aligned row is the one a `FlowLayout` reports. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Aligning on the baseline does not only move the children around, it\n            also makes the row taller than its tallest child whenever the deepest\n            ascent and the deepest descent come from two different children. Both\n            layout managers have to fold that into the height they report.\n\n            The widths in this table all leave the two labels in a single row, so\n            there is no wrapping involved and the two layout managers have to\n            agree exactly — this is the guard which keeps the responsive layout\n            from quietly reporting the height of its tallest child instead.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels whose baselines sit at very different depths.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at a width which keeps the two labels in one row,","code":["_laidOutBoundsOf(ours, width, 300)","_laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"they agree on the height that aligning the baselines demands.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"and","text":"\n            And that height really is more than the tallest of the two labels\n            needs on its own, which is what makes this a meaningful check.\n        ","code":["var tallest = Math.max((int) ours.getComponent(0).getPreferredSize().height,","                       (int) ours.getComponent(1).getPreferredSize().height)","ours.getLayout().preferredLayoutSize(ours).height > tallest + 10"]},

        {"kind":"where","text":"A width of 0 is the state before the first layout pass.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["0","300","0","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred size of a baseline aligned row is the one a `FlowLayout` reports. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Aligning on the baseline does not only move the children around, it\n            also makes the row taller than its tallest child whenever the deepest\n            ascent and the deepest descent come from two different children. Both\n            layout managers have to fold that into the height they report.\n\n            The widths in this table all leave the two labels in a single row, so\n            there is no wrapping involved and the two layout managers have to\n            agree exactly — this is the guard which keeps the responsive layout\n            from quietly reporting the height of its tallest child instead.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A layout manager of each kind, both aligning on the baseline.","code":["var ourLayout = new ResponsiveGridFlowLayout(alignment, 5, 5)","ourLayout.setAlignOnBaseline(true)","var awtLayout = new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5)","awtLayout.setAlignOnBaseline(true)"]},

        {"kind":"and","text":"Two panels holding labels whose baselines sit at very different depths.","code":["var mismatchedLabels = { ->","    var big = new JLabel(\"Big\")","    big.setFont(new Font(\"SansSerif\", Font.PLAIN, 40))","    var low = new JLabel(\"low\")","    low.setFont(new Font(\"SansSerif\", Font.PLAIN, 10))","    low.setBorder(new EmptyBorder(0, 0, 40, 0))","    return [big, low]","}","var ours = UI.panel().withLayout(ourLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)","var awts = UI.panel().withLayout(awtLayout)","                .apply({ ui -> mismatchedLabels().each { l -> ui.add(UI.of(l)) } })","                .get(JPanel)"]},

        {"kind":"when","text":"Both are laid out at a width which keeps the two labels in one row,","code":["_laidOutBoundsOf(ours, width, 300)","_laidOutBoundsOf(awts, width, 300)"]},

        {"kind":"then","text":"they agree on the height that aligning the baselines demands.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"and","text":"\n            And that height really is more than the tallest of the two labels\n            needs on its own, which is what makes this a meaningful check.\n        ","code":["var tallest = Math.max((int) ours.getComponent(0).getPreferredSize().height,","                       (int) ours.getComponent(1).getPreferredSize().height)","ours.getLayout().preferredLayoutSize(ours).height > tallest + 10"]},

        {"kind":"where","text":"A width of 0 is the state before the first layout pass.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["0","300","0","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Hidden children are skipped exactly like a `FlowLayout` skips them. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An invisible child takes part in neither the rows nor the reported\n            sizes. The hidden child in this feature is deliberately far wider and\n            taller than its visible siblings, so that a layout manager which\n            failed to skip it would produce very obviously different numbers.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel builder which places a large hidden child between two small visible ones.","code":["var withHidden = { layout ->","    return UI.panel().withLayout(layout)","            .add(UI.box().withPrefSize(30, 20))","            .add(UI.box().withPrefSize(200, 90).peek({ it.setVisible(false) }))","            .add(UI.box().withPrefSize(30, 20))","            .get(JPanel)","}","var ours = withHidden(new ResponsiveGridFlowLayout(alignment, 5, 5))","var awts = withHidden(new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5))"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the visible children agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"and so does the minimum size, which the hidden child must not inflate.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["50","300","50","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Hidden children are skipped exactly like a `FlowLayout` skips them. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An invisible child takes part in neither the rows nor the reported\n            sizes. The hidden child in this feature is deliberately far wider and\n            taller than its visible siblings, so that a layout manager which\n            failed to skip it would produce very obviously different numbers.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel builder which places a large hidden child between two small visible ones.","code":["var withHidden = { layout ->","    return UI.panel().withLayout(layout)","            .add(UI.box().withPrefSize(30, 20))","            .add(UI.box().withPrefSize(200, 90).peek({ it.setVisible(false) }))","            .add(UI.box().withPrefSize(30, 20))","            .get(JPanel)","}","var ours = withHidden(new ResponsiveGridFlowLayout(alignment, 5, 5))","var awts = withHidden(new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5))"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the visible children agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"and so does the minimum size, which the hidden child must not inflate.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["50","300","50","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Hidden children are skipped exactly like a `FlowLayout` skips them. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An invisible child takes part in neither the rows nor the reported\n            sizes. The hidden child in this feature is deliberately far wider and\n            taller than its visible siblings, so that a layout manager which\n            failed to skip it would produce very obviously different numbers.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel builder which places a large hidden child between two small visible ones.","code":["var withHidden = { layout ->","    return UI.panel().withLayout(layout)","            .add(UI.box().withPrefSize(30, 20))","            .add(UI.box().withPrefSize(200, 90).peek({ it.setVisible(false) }))","            .add(UI.box().withPrefSize(30, 20))","            .get(JPanel)","}","var ours = withHidden(new ResponsiveGridFlowLayout(alignment, 5, 5))","var awts = withHidden(new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5))"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the visible children agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"and so does the minimum size, which the hidden child must not inflate.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["50","300","50","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Hidden children are skipped exactly like a `FlowLayout` skips them. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An invisible child takes part in neither the rows nor the reported\n            sizes. The hidden child in this feature is deliberately far wider and\n            taller than its visible siblings, so that a layout manager which\n            failed to skip it would produce very obviously different numbers.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel builder which places a large hidden child between two small visible ones.","code":["var withHidden = { layout ->","    return UI.panel().withLayout(layout)","            .add(UI.box().withPrefSize(30, 20))","            .add(UI.box().withPrefSize(200, 90).peek({ it.setVisible(false) }))","            .add(UI.box().withPrefSize(30, 20))","            .get(JPanel)","}","var ours = withHidden(new ResponsiveGridFlowLayout(alignment, 5, 5))","var awts = withHidden(new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5))"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the visible children agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"and so does the minimum size, which the hidden child must not inflate.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["50","300","50","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Hidden children are skipped exactly like a `FlowLayout` skips them. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            An invisible child takes part in neither the rows nor the reported\n            sizes. The hidden child in this feature is deliberately far wider and\n            taller than its visible siblings, so that a layout manager which\n            failed to skip it would produce very obviously different numbers.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A panel builder which places a large hidden child between two small visible ones.","code":["var withHidden = { layout ->","    return UI.panel().withLayout(layout)","            .add(UI.box().withPrefSize(30, 20))","            .add(UI.box().withPrefSize(200, 90).peek({ it.setVisible(false) }))","            .add(UI.box().withPrefSize(30, 20))","            .get(JPanel)","}","var ours = withHidden(new ResponsiveGridFlowLayout(alignment, 5, 5))","var awts = withHidden(new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), 5, 5))"]},

        {"kind":"when","text":"Both are laid out at the same width,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 200)","var awtBounds = _laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"the visible children agree exactly.","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"and so does the minimum size, which the hidden child must not inflate.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"width":["50","300","50","300","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The minimum layout size is exactly the minimum layout size of a `FlowLayout`. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `minimumLayoutSize` answers with a single row of minimum sized\n            children under both layout managers, and it does so regardless of how\n            wide the container currently is. This is the sharpest part of the\n            equivalence: unlike the preferred size, there is no wrapping involved\n            and therefore no reason for the two to ever differ.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, withInsets)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, withInsets","            )"]},

        {"kind":"expect","text":"Before any layout pass the two agree.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"when","text":"We squeeze both containers into a width which forces several rows,","code":["_laidOutBoundsOf(ours, 60, 200)","_laidOutBoundsOf(awts, 60, 200)"]},

        {"kind":"then","text":"the minimum size is unchanged and still agrees.","code":["ours.getLayout().minimumLayoutSize(ours) == awts.getLayout().minimumLayoutSize(awts)"]},

        {"kind":"where","text":"","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","5","5","7","7","5"],"verticalGap":["0","5","5","3","3","5"],"withInsets":["false","false","true","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The preferred layout size is the one of a `FlowLayout` as long as the content does not wrap. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            As long as all children fit into a single row there is nothing to\n            disagree about, and the preferred size of the two layout managers is\n            identical.\n\n            The width of `0` in this table is the state a container is in before\n            it has ever been laid out. Both layout managers then assume a single\n            row, which is what lets a window pack itself around a flow layout.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(alignment, horizontalGap, verticalGap), FIVE_BOXES(), true, false)","var awts = _panelOf(","                new FlowLayout(alignment.forFlowLayout().orElse(FlowLayout.CENTER), horizontalGap, verticalGap),","                FIVE_BOXES(), true, false","            )"]},

        {"kind":"when","text":"Both are laid out at a width which does not force any wrapping,","code":["_laidOutBoundsOf(ours, width, 200)","_laidOutBoundsOf(awts, width, 200)"]},

        {"kind":"then","text":"their preferred sizes are exactly the same.","code":["ours.getLayout().preferredLayoutSize(ours) == awts.getLayout().preferredLayoutSize(awts)"]},

        {"kind":"where","text":"The five boxes need 180 pixels for one row at a gap of 5, and 170 at a gap of 0.","code":{"alignment":["UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.LEFT","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.CENTER","UI.HorizontalAlignment.RIGHT","UI.HorizontalAlignment.RIGHT"],"horizontalGap":["0","0","5","5","5","5","7","7"],"verticalGap":["0","0","5","5","5","5","3","3"],"width":["0","300","0","300","0","300","0","300"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"When the content wraps, the preferred height covers the extra rows, unlike a `FlowLayout`. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the one deliberate departure from `FlowLayout`, and it is the\n            reason the responsive grid flow layout can be nested and scrolled.\n\n            A `FlowLayout` reports the height of a single row no matter how narrow\n            its container is, so a parent which trusts that number hands out too\n            little height and the wrapped rows are clipped away. The responsive\n            grid flow layout instead reports the height its rows really occupy at\n            the width it currently has.\n\n            Note that the preferred *width* does not differ: both report the width\n            of the ideal single row. Only the height reacts to wrapping.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(UI.HorizontalAlignment.LEFT, 5, 5), FIVE_BOXES(), true, false)","var awts = _panelOf(new FlowLayout(FlowLayout.LEFT, 5, 5), FIVE_BOXES(), true, false)"]},

        {"kind":"when","text":"Both are laid out at a width which forces the children to wrap,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 400)","var awtBounds = _laidOutBoundsOf(awts, width, 400)"]},

        {"kind":"then","text":"they still place every child in exactly the same spot...","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"...and both really did wrap into the number of rows we expect.","code":["ourBounds.collect({ it.y }).toSet().size() == expectedRows"]},

        {"kind":"and","text":"Both agree on the preferred width, which is the ideal single row.","code":["ours.getLayout().preferredLayoutSize(ours).width == awts.getLayout().preferredLayoutSize(awts).width"]},

        {"kind":"and","text":"\n            But only our layout reports a height which actually covers those rows.\n            The `FlowLayout` still answers with the height of one single row.\n        ","code":["ours.getLayout().preferredLayoutSize(ours).height == _contentBottomOf(ours) + 5","awts.getLayout().preferredLayoutSize(awts).height == 20 + 5 + 5","ours.getLayout().preferredLayoutSize(ours).height > awts.getLayout().preferredLayoutSize(awts).height"]},

        {"kind":"where","text":"\n            Mind that a child is taken into the current row when `x + width` still\n            fits, which is checked *before* the horizontal gap is added. The five\n            boxes therefore still squeeze into a single row at a width of 175.\n        ","code":{"width":["30","60","80","100"],"expectedRows":["5","4","3","2"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"When the content wraps, the preferred height covers the extra rows, unlike a `FlowLayout`. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the one deliberate departure from `FlowLayout`, and it is the\n            reason the responsive grid flow layout can be nested and scrolled.\n\n            A `FlowLayout` reports the height of a single row no matter how narrow\n            its container is, so a parent which trusts that number hands out too\n            little height and the wrapped rows are clipped away. The responsive\n            grid flow layout instead reports the height its rows really occupy at\n            the width it currently has.\n\n            Note that the preferred *width* does not differ: both report the width\n            of the ideal single row. Only the height reacts to wrapping.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(UI.HorizontalAlignment.LEFT, 5, 5), FIVE_BOXES(), true, false)","var awts = _panelOf(new FlowLayout(FlowLayout.LEFT, 5, 5), FIVE_BOXES(), true, false)"]},

        {"kind":"when","text":"Both are laid out at a width which forces the children to wrap,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 400)","var awtBounds = _laidOutBoundsOf(awts, width, 400)"]},

        {"kind":"then","text":"they still place every child in exactly the same spot...","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"...and both really did wrap into the number of rows we expect.","code":["ourBounds.collect({ it.y }).toSet().size() == expectedRows"]},

        {"kind":"and","text":"Both agree on the preferred width, which is the ideal single row.","code":["ours.getLayout().preferredLayoutSize(ours).width == awts.getLayout().preferredLayoutSize(awts).width"]},

        {"kind":"and","text":"\n            But only our layout reports a height which actually covers those rows.\n            The `FlowLayout` still answers with the height of one single row.\n        ","code":["ours.getLayout().preferredLayoutSize(ours).height == _contentBottomOf(ours) + 5","awts.getLayout().preferredLayoutSize(awts).height == 20 + 5 + 5","ours.getLayout().preferredLayoutSize(ours).height > awts.getLayout().preferredLayoutSize(awts).height"]},

        {"kind":"where","text":"\n            Mind that a child is taken into the current row when `x + width` still\n            fits, which is checked *before* the horizontal gap is added. The five\n            boxes therefore still squeeze into a single row at a width of 175.\n        ","code":{"width":["30","60","80","100"],"expectedRows":["5","4","3","2"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"When the content wraps, the preferred height covers the extra rows, unlike a `FlowLayout`. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the one deliberate departure from `FlowLayout`, and it is the\n            reason the responsive grid flow layout can be nested and scrolled.\n\n            A `FlowLayout` reports the height of a single row no matter how narrow\n            its container is, so a parent which trusts that number hands out too\n            little height and the wrapped rows are clipped away. The responsive\n            grid flow layout instead reports the height its rows really occupy at\n            the width it currently has.\n\n            Note that the preferred *width* does not differ: both report the width\n            of the ideal single row. Only the height reacts to wrapping.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(UI.HorizontalAlignment.LEFT, 5, 5), FIVE_BOXES(), true, false)","var awts = _panelOf(new FlowLayout(FlowLayout.LEFT, 5, 5), FIVE_BOXES(), true, false)"]},

        {"kind":"when","text":"Both are laid out at a width which forces the children to wrap,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 400)","var awtBounds = _laidOutBoundsOf(awts, width, 400)"]},

        {"kind":"then","text":"they still place every child in exactly the same spot...","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"...and both really did wrap into the number of rows we expect.","code":["ourBounds.collect({ it.y }).toSet().size() == expectedRows"]},

        {"kind":"and","text":"Both agree on the preferred width, which is the ideal single row.","code":["ours.getLayout().preferredLayoutSize(ours).width == awts.getLayout().preferredLayoutSize(awts).width"]},

        {"kind":"and","text":"\n            But only our layout reports a height which actually covers those rows.\n            The `FlowLayout` still answers with the height of one single row.\n        ","code":["ours.getLayout().preferredLayoutSize(ours).height == _contentBottomOf(ours) + 5","awts.getLayout().preferredLayoutSize(awts).height == 20 + 5 + 5","ours.getLayout().preferredLayoutSize(ours).height > awts.getLayout().preferredLayoutSize(awts).height"]},

        {"kind":"where","text":"\n            Mind that a child is taken into the current row when `x + width` still\n            fits, which is checked *before* the horizontal gap is added. The five\n            boxes therefore still squeeze into a single row at a width of 175.\n        ","code":{"width":["30","60","80","100"],"expectedRows":["5","4","3","2"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"When the content wraps, the preferred height covers the extra rows, unlike a `FlowLayout`. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the one deliberate departure from `FlowLayout`, and it is the\n            reason the responsive grid flow layout can be nested and scrolled.\n\n            A `FlowLayout` reports the height of a single row no matter how narrow\n            its container is, so a parent which trusts that number hands out too\n            little height and the wrapped rows are clipped away. The responsive\n            grid flow layout instead reports the height its rows really occupy at\n            the width it currently has.\n\n            Note that the preferred *width* does not differ: both report the width\n            of the ideal single row. Only the height reacts to wrapping.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Two panels, one per layout manager.","code":["var ours = _panelOf(new ResponsiveGridFlowLayout(UI.HorizontalAlignment.LEFT, 5, 5), FIVE_BOXES(), true, false)","var awts = _panelOf(new FlowLayout(FlowLayout.LEFT, 5, 5), FIVE_BOXES(), true, false)"]},

        {"kind":"when","text":"Both are laid out at a width which forces the children to wrap,","code":["var ourBounds = _laidOutBoundsOf(ours, width, 400)","var awtBounds = _laidOutBoundsOf(awts, width, 400)"]},

        {"kind":"then","text":"they still place every child in exactly the same spot...","code":["ourBounds == awtBounds"]},

        {"kind":"and","text":"...and both really did wrap into the number of rows we expect.","code":["ourBounds.collect({ it.y }).toSet().size() == expectedRows"]},

        {"kind":"and","text":"Both agree on the preferred width, which is the ideal single row.","code":["ours.getLayout().preferredLayoutSize(ours).width == awts.getLayout().preferredLayoutSize(awts).width"]},

        {"kind":"and","text":"\n            But only our layout reports a height which actually covers those rows.\n            The `FlowLayout` still answers with the height of one single row.\n        ","code":["ours.getLayout().preferredLayoutSize(ours).height == _contentBottomOf(ours) + 5","awts.getLayout().preferredLayoutSize(awts).height == 20 + 5 + 5","ours.getLayout().preferredLayoutSize(ours).height > awts.getLayout().preferredLayoutSize(awts).height"]},

        {"kind":"where","text":"\n            Mind that a child is taken into the current row when `x + width` still\n            fits, which is checked *before* the horizontal gap is added. The five\n            boxes therefore still squeeze into a single row at a width of 175.\n        ","code":{"width":["30","60","80","100"],"expectedRows":["5","4","3","2"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
