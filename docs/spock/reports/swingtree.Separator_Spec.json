{
  "className":"swingtree.Separator_Spec",
  "title":"Separators",
  "narrative":"A separator is a simple component that draws a thin line\n    which is used to visually separate groups of components\n    from each other, in menus, toolbars or plain panels.\n\n    SwingTree exposes the `JSeparator` through the\n    `UI.separator()` factory methods and the `UIForSeparator`\n    builder, which allow you to declare the orientation\n    and the length of the separation line, and even bind\n    these aspects of its state to properties in your view model.",
  "subjects":["swingtree.UIForSeparator","swingtree.UI","swingtree.UIFactoryMethods"],
  "statistics":{
    "runs":"7",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"Use the `UI.separator()` factory method to create a horizontal separator.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The plain `UI.separator()` factory method wraps a `JSeparator`\n            with its default orientation, which is horizontal,\n            just like the line drawn between two menu items.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We declare and then build a plain separator component.","code":["var separator = UI.separator().get(JSeparator)"]},

        {"kind":"expect","text":"It has the default horizontal orientation of the underlying `JSeparator`.","code":["separator.orientation == SwingConstants.HORIZONTAL"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Pass an `UI.Align` constant to the `separator(UI.Align)` factory method to choose the orientation.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Instead of the integer constants that raw Swing expects\n            in `JSeparator.setOrientation(int)`, SwingTree uses the\n            type safe `UI.Align` enum to declare whether the separation\n            line runs horizontally or vertically.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A vertical and a horizontal separator.","code":["var vertical   = UI.separator(UI.Align.VERTICAL).get(JSeparator)","var horizontal = UI.separator(UI.Align.HORIZONTAL).get(JSeparator)"]},

        {"kind":"expect","text":"The orientation of each component matches its declaration.","code":["vertical.orientation   == SwingConstants.VERTICAL","horizontal.orientation == SwingConstants.HORIZONTAL"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `withOrientation(UI.Align)` method configures the orientation as part of a builder chain.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":[]
      },
      "blocks":[
        {"kind":"given","text":"We declare a separator and set its orientation in the middle of the builder chain.","code":["var separator =","        UI.separator()","        .withOrientation(UI.Align.VERTICAL)","        .get(JSeparator)"]},

        {"kind":"expect","text":"","code":["separator.orientation == SwingConstants.VERTICAL"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Bind an `UI.Align` property to a separator to control its orientation dynamically.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `separator(Val<UI.Align>)` factory method, and equally the\n            `withOrientation(Val<UI.Align>)` builder method, bind an alignment\n            property to the orientation of the separator.\n            Whenever the property changes in your view model, the separator\n            reorients itself automatically.\n            This is useful for layouts which rearrange themselves\n            dynamically, for example when the user resizes a window\n            from a wide to a narrow shape.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"An alignment property, as it would exist in a view model.","code":["var alignment = Var.of(UI.Align.HORIZONTAL)"]},

        {"kind":"and","text":"A separator bound to the property.","code":["var separator = UI.separator(alignment).get(JSeparator)"]},

        {"kind":"expect","text":"The separator starts out with the initial orientation of the property.","code":["separator.orientation == SwingConstants.HORIZONTAL"]},

        {"kind":"when","text":"The view model changes the alignment.","code":["UI.runNow({ alignment.set(UI.Align.VERTICAL) })"]},

        {"kind":"then","text":"The separator followed along.","code":["separator.orientation == SwingConstants.VERTICAL"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Use `withLength(int)` to declare how long the separation line should be.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A `JSeparator` does not have a dedicated \"length\" attribute,\n            instead its size is determined by its preferred size.\n            The `withLength(int)` method is an orientation aware\n            convenience method which sets the preferred width\n            for a horizontal separator, and the preferred height\n            for a vertical one, while leaving the respective\n            other dimension, the thickness of the line, untouched.\n\n            Note that the length is interpreted as an abstract pixel size,\n            which is scaled according to the current DPI scaling factor\n            of the SwingTree context. This is why the assertions below\n            compare against `UI.scale(int)`.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A horizontal and a vertical separator, both with a length of 42.","code":["var horizontal =","        UI.separator(UI.Align.HORIZONTAL)","        .withLength(42)","        .get(JSeparator)","var vertical =","        UI.separator(UI.Align.VERTICAL)","        .withLength(42)","        .get(JSeparator)"]},

        {"kind":"expect","text":"For the horizontal separator the length became the preferred width.","code":["horizontal.preferredSize.width == UI.scale(42)"]},

        {"kind":"and","text":"For the vertical separator on the other hand, it became the preferred height.","code":["vertical.preferredSize.height == UI.scale(42)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The length of a separator can be bound to an integer property.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Just like most other aspects of a component, the length of the\n            separation line can be modelled dynamically by a property\n            in your view model using the `withLength(Val<Integer>)` method.\n            Whenever the property changes, the preferred size of the\n            separator is updated according to its orientation.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"An integer property modelling the length of the separator.","code":["var length = Var.of(50)"]},

        {"kind":"and","text":"A horizontal separator bound to it.","code":["var separator =","        UI.separator(UI.Align.HORIZONTAL)","        .withLength(length)","        .get(JSeparator)"]},

        {"kind":"expect","text":"The initial preferred width comes from the initial property value.","code":["separator.preferredSize.width == UI.scale(50)"]},

        {"kind":"when","text":"The view model grows the separator.","code":["UI.runNow({ length.set(200) })"]},

        {"kind":"then","text":"The preferred width of the component was updated.","code":["separator.preferredSize.width == UI.scale(200)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Null is not a valid alignment for a separator.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The separator factory and builder methods reject null\n            arguments, as well as nullable alignment properties, eagerly\n            with an exception, so that programming errors surface\n            right where the UI is declared.\n        "]
      },
      "blocks":[
        {"kind":"when","text":"We try to declare a separator with a null alignment...","code":["UI.separator((UI.Align) null)"]},

        {"kind":"then","text":"...the declaration fails immediately.","code":["thrown(IllegalArgumentException)"]},

        {"kind":"when","text":"The same happens for a property which permits null in it:","code":["UI.separator().withOrientation(Var.ofNullable(UI.Align, null))"]},

        {"kind":"then","text":"","code":["thrown(IllegalArgumentException)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
