{
  "className":"swingtree.Decoupled_Property_Binding_Spec",
  "title":"Property Binding Across Threads",
  "narrative":"Swing only knows one thread, the event dispatch thread (EDT),\n    which performs both the rendering of the UI as well as the event handling.\n    This is a problem for applications that need to perform long running\n    tasks in the background, because the EDT is blocked until a task\n    is complete, freezing the UI. The decoupled threading mode solves this\n    by dispatching your business logic to a custom application thread, while\n    ensuring that all UI related operations still happen on the EDT.\n    This specification demonstrates how this threading model interacts\n    with the MVVM pattern and the binding of properties to UI components.\n\n    The single most important boundary in a decoupled SwingTree application\n    is the one between your property based view models, which live on the\n    application thread, and the Swing components, which live on the UI thread.\n\n    Whenever a bound property changes, the new state has to travel from the\n    application thread to the UI thread, and whenever the user interacts\n    with a component, the event has to travel from the UI thread to the\n    application thread. Both directions cross a thread boundary,\n    which raises delicate questions:\n    Do updates get lost? Can a stale value overtake a newer one?\n    Which thread executes what?\n\n    This specification answers these questions. To keep the concurrency\n    easy to reason about, most tests use two simple tricks:\n    the test thread itself plays the role of the application thread\n    (by draining the application event queue on demand), and where\n    necessary the UI thread is temporarily parked behind a latch so that\n    in-flight updates can be observed before they are applied.\n    No mocks are involved, everything runs through the real\n    event processor, real components and real properties.",
  "subjects":["swingtree.UIForAnySwing","swingtree.UIForTabbedPane","swingtree.threading.EventProcessor"],
  "statistics":{
    "runs":"32",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"A property change from the application thread reaches the component asynchronously, on the UI thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When a bound property is changed by the application thread,\n            the component is **not** updated synchronously as part of the\n            `set(..)` call. Instead, the new state is dispatched to the\n            UI thread, and the component catches up as soon as\n            the UI thread gets around to it.\n\n            To prove this deterministically, we briefly park the UI thread\n            behind a latch: the property change then demonstrably happens\n            *before* the component has updated, and the component follows\n            once the UI thread is released, without us touching\n            the component ourselves.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A text property, as it would live in one of your view models.","code":["var text = Var.of(\"Hello!\")"]},

        {"kind":"and","text":"A label built in the decoupled mode, with its text bound to the property.","code":["var label = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> UI.label(text)).get(JLabel)","})"]},

        {"kind":"expect","text":"Initially, the label shows the current property value.","code":["label.text == \"Hello!\""]},

        {"kind":"when","text":"We park the UI thread behind a gate, so that we can catch the next update in flight.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })"]},

        {"kind":"and","text":"We change the property from this thread and immediately look at the label, then release the UI thread.","code":["var textRightAfterSet = null","try {","    text.set(\"Goodbye!\")","    textRightAfterSet = label.text","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the label was still untouched...","code":["textRightAfterSet == \"Hello!\""]},

        {"kind":"and","text":"...but once the UI thread caught up, the new state arrived in the component.","code":["label.text == \"Goodbye!\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Rapid property changes are all replayed on the UI thread, in the order they were made.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Here a little explanation is in order:\n            Consider a property that is bound to a UI component, and that\n            property is changed by the application thread. This notifies the\n            property observers, which put the change procedure into the event\n            queue of the UI thread. However, it might take a while before the\n            UI thread gets around to processing the event, and until then the\n            property might have changed again, maybe multiple times!\n            Without further precautions, the sequence of changes arriving at\n            the component might then differ from the sequence in which they\n            were made to the property.\n\n            SwingTree prevents this: each change is shipped to the UI thread\n            together with the value the property carried *at the time of the\n            change*, and the UI thread replays them in their original order.\n            This test pins down both consequences: no intermediate value is\n            skipped, and no stale value can overtake a newer one, even though\n            the property already holds the final value long before the\n            UI thread wakes up.\n\n            To observe the individual updates as they land on the component,\n            this test uses a label which records every `setText(..)` call\n            together with the thread that performed it.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A recording label and a text property bound to it in decoupled mode.","code":["var underlying = new RecordingLabel()","var text = Var.of(\"Hello\")","var label = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> UI.of(underlying).withText(text)).get(JLabel)","})"]},

        {"kind":"and","text":"We forget the text assignments made while building the declaration.","code":["underlying.applied.clear()"]},

        {"kind":"when","text":"We change the property once and wait for the UI thread.","code":["text.set(\"Hi\")","UI.sync()"]},

        {"kind":"then","text":"As expected, the component received exactly this one update.","code":["underlying.applied == [\"'Hi' (UI thread: true)\"]"]},

        {"kind":"when","text":"We park the UI thread, fire four quick changes from this thread, and release the UI thread again.","code":["underlying.applied.clear()","var gate = new CountDownLatch(1)","UI.run({ gate.await() })","try {","    text.set(\"Hello\")","    text.set(\"Hi\")","    text.set(\"Hey\")","    text.set(\"Hi\")","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Every single value arrived on the component, in its original order, and all of them on the UI thread.","code":["underlying.applied == [","    \"'Hello' (UI thread: true)\",","    \"'Hi' (UI thread: true)\",","    \"'Hey' (UI thread: true)\",","    \"'Hi' (UI thread: true)\"","]"]},

        {"kind":"and","text":"The component settled on the final property value.","code":["label.text == \"Hi\"","text.is(\"Hi\")"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A button click travels to the application thread, updates the view model, and the new state travels back to the UI.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This is the full MVVM round trip of a decoupled SwingTree\n            application, in slow motion:\n\n            1. The user clicks a button, on the UI thread.\n            2. The click handler is enqueued for the application thread,\n               where it may safely work with the view model.\n            3. The view model property changes, which enqueues a display\n               update for the UI thread.\n            4. The UI thread applies the new state to the component.\n\n            The test thread plays the application thread, so every one\n            of these four steps happens exactly when the test says so.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A minimal view model: an integer counter property.","code":["var counter = Var.of(0)"]},

        {"kind":"and","text":"A trace which records where the business logic runs.","code":["var trace = new CopyOnWriteArrayList<String>()"]},

        {"kind":"and","text":"A decoupled view: a button which increments the counter, and a label displaying it.","code":["var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(","            UI.button(\"+\").onClick({","                counter.set(counter.get() + 1)","                trace << \"incremented on '${Thread.currentThread().name}'\".toString()","            })","        )","        .add(","            UI.label(counter.viewAsString())","        )","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"expect","text":"The label starts out with the initial counter value.","code":["label.text == \"0\""]},

        {"kind":"when","text":"The user clicks the button, on the UI thread.","code":["UI.runNow({ button.doClick() })"]},

        {"kind":"then","text":"Nothing has happened yet, the business logic is waiting in the application event queue.","code":["trace.isEmpty()","counter.is(0)"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event.","code":["EventProcessor.DECOUPLED.joinFor(1)"]},

        {"kind":"then","text":"The handler ran on this thread and updated the view model.","code":["trace == [\"incremented on '${Thread.currentThread().name}'\".toString()]","counter.is(1)"]},

        {"kind":"when","text":"We wait for the UI thread to catch up with the state change.","code":["UI.sync()"]},

        {"kind":"then","text":"The label displays the new counter value.","code":["label.text == \"1\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Under real concurrent load, from multiple threads, not a single update is lost. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The previous features scripted the thread handover step by step.\n            This one lets go of the reins: a continuously draining application\n            thread runs in the background while the UI thread and two\n            additional producer threads all funnel counter increments into\n            the application event queue, concurrently and as fast as they can.\n\n            Because all view model mutations are executed by the single\n            application thread, they are serialized, and so, no matter how\n            the threads interleave, the final counter must be exact and\n            the label must display it. The feature is repeated several times\n            to explore different thread interleavings.\n\n            Also note that a log spy watches the whole ride: if anything\n            inside SwingTree fails silently and merely logs an error,\n            this test fails.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A spy on the log, so that even silently logged errors fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A continuously draining application thread, like in a real decoupled application.","code":["var app = ApplicationThread.startDraining()"]},

        {"kind":"and","text":"A view model counter, and a decoupled view with an incrementing button and a label.","code":["var counter = Var.of(0)","var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.button(\"+\").onClick({ counter.set(counter.get() + 1) }))","        .add(UI.label(counter.viewAsString()))","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"when","text":"Two producer threads each enqueue 150 increments, while the UI thread fires 150 button clicks.","code":["var producers = (1..2).collect { producerNumber ->","    new Thread({","        150.times {","            EventProcessor.DECOUPLED.registerAppEvent({ counter.set(counter.get() + 1) })","        }","    }, \"producer-$producerNumber\")","}","producers.each { it.start() }","150.times {","    UI.runNow({ button.doClick() })","}","producers.each { it.join(10_000) }"]},

        {"kind":"and","text":"We wait for the application thread to work off the queue, and for the UI thread to catch up.","code":["app.awaitAllProcessed()","UI.sync()"]},

        {"kind":"then","text":"Every single one of the 450 increments arrived in the view model.","code":["counter.is(450)"]},

        {"kind":"and","text":"The label displays the exact final state.","code":["label.text == \"450\""]},

        {"kind":"and","text":"No event task failed, and nothing was silently logged as an error along the way.","code":["app.failures().isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["app.stop()","log.detach()"]},

        {"kind":"where","text":"We repeat this race a few times to explore different thread interleavings.","code":{"run":[]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Under real concurrent load, from multiple threads, not a single update is lost. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The previous features scripted the thread handover step by step.\n            This one lets go of the reins: a continuously draining application\n            thread runs in the background while the UI thread and two\n            additional producer threads all funnel counter increments into\n            the application event queue, concurrently and as fast as they can.\n\n            Because all view model mutations are executed by the single\n            application thread, they are serialized, and so, no matter how\n            the threads interleave, the final counter must be exact and\n            the label must display it. The feature is repeated several times\n            to explore different thread interleavings.\n\n            Also note that a log spy watches the whole ride: if anything\n            inside SwingTree fails silently and merely logs an error,\n            this test fails.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A spy on the log, so that even silently logged errors fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A continuously draining application thread, like in a real decoupled application.","code":["var app = ApplicationThread.startDraining()"]},

        {"kind":"and","text":"A view model counter, and a decoupled view with an incrementing button and a label.","code":["var counter = Var.of(0)","var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.button(\"+\").onClick({ counter.set(counter.get() + 1) }))","        .add(UI.label(counter.viewAsString()))","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"when","text":"Two producer threads each enqueue 150 increments, while the UI thread fires 150 button clicks.","code":["var producers = (1..2).collect { producerNumber ->","    new Thread({","        150.times {","            EventProcessor.DECOUPLED.registerAppEvent({ counter.set(counter.get() + 1) })","        }","    }, \"producer-$producerNumber\")","}","producers.each { it.start() }","150.times {","    UI.runNow({ button.doClick() })","}","producers.each { it.join(10_000) }"]},

        {"kind":"and","text":"We wait for the application thread to work off the queue, and for the UI thread to catch up.","code":["app.awaitAllProcessed()","UI.sync()"]},

        {"kind":"then","text":"Every single one of the 450 increments arrived in the view model.","code":["counter.is(450)"]},

        {"kind":"and","text":"The label displays the exact final state.","code":["label.text == \"450\""]},

        {"kind":"and","text":"No event task failed, and nothing was silently logged as an error along the way.","code":["app.failures().isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["app.stop()","log.detach()"]},

        {"kind":"where","text":"We repeat this race a few times to explore different thread interleavings.","code":{"run":[]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Under real concurrent load, from multiple threads, not a single update is lost. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The previous features scripted the thread handover step by step.\n            This one lets go of the reins: a continuously draining application\n            thread runs in the background while the UI thread and two\n            additional producer threads all funnel counter increments into\n            the application event queue, concurrently and as fast as they can.\n\n            Because all view model mutations are executed by the single\n            application thread, they are serialized, and so, no matter how\n            the threads interleave, the final counter must be exact and\n            the label must display it. The feature is repeated several times\n            to explore different thread interleavings.\n\n            Also note that a log spy watches the whole ride: if anything\n            inside SwingTree fails silently and merely logs an error,\n            this test fails.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A spy on the log, so that even silently logged errors fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A continuously draining application thread, like in a real decoupled application.","code":["var app = ApplicationThread.startDraining()"]},

        {"kind":"and","text":"A view model counter, and a decoupled view with an incrementing button and a label.","code":["var counter = Var.of(0)","var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.button(\"+\").onClick({ counter.set(counter.get() + 1) }))","        .add(UI.label(counter.viewAsString()))","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"when","text":"Two producer threads each enqueue 150 increments, while the UI thread fires 150 button clicks.","code":["var producers = (1..2).collect { producerNumber ->","    new Thread({","        150.times {","            EventProcessor.DECOUPLED.registerAppEvent({ counter.set(counter.get() + 1) })","        }","    }, \"producer-$producerNumber\")","}","producers.each { it.start() }","150.times {","    UI.runNow({ button.doClick() })","}","producers.each { it.join(10_000) }"]},

        {"kind":"and","text":"We wait for the application thread to work off the queue, and for the UI thread to catch up.","code":["app.awaitAllProcessed()","UI.sync()"]},

        {"kind":"then","text":"Every single one of the 450 increments arrived in the view model.","code":["counter.is(450)"]},

        {"kind":"and","text":"The label displays the exact final state.","code":["label.text == \"450\""]},

        {"kind":"and","text":"No event task failed, and nothing was silently logged as an error along the way.","code":["app.failures().isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["app.stop()","log.detach()"]},

        {"kind":"where","text":"We repeat this race a few times to explore different thread interleavings.","code":{"run":[]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Under real concurrent load, from multiple threads, not a single update is lost. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The previous features scripted the thread handover step by step.\n            This one lets go of the reins: a continuously draining application\n            thread runs in the background while the UI thread and two\n            additional producer threads all funnel counter increments into\n            the application event queue, concurrently and as fast as they can.\n\n            Because all view model mutations are executed by the single\n            application thread, they are serialized, and so, no matter how\n            the threads interleave, the final counter must be exact and\n            the label must display it. The feature is repeated several times\n            to explore different thread interleavings.\n\n            Also note that a log spy watches the whole ride: if anything\n            inside SwingTree fails silently and merely logs an error,\n            this test fails.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A spy on the log, so that even silently logged errors fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A continuously draining application thread, like in a real decoupled application.","code":["var app = ApplicationThread.startDraining()"]},

        {"kind":"and","text":"A view model counter, and a decoupled view with an incrementing button and a label.","code":["var counter = Var.of(0)","var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.button(\"+\").onClick({ counter.set(counter.get() + 1) }))","        .add(UI.label(counter.viewAsString()))","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"when","text":"Two producer threads each enqueue 150 increments, while the UI thread fires 150 button clicks.","code":["var producers = (1..2).collect { producerNumber ->","    new Thread({","        150.times {","            EventProcessor.DECOUPLED.registerAppEvent({ counter.set(counter.get() + 1) })","        }","    }, \"producer-$producerNumber\")","}","producers.each { it.start() }","150.times {","    UI.runNow({ button.doClick() })","}","producers.each { it.join(10_000) }"]},

        {"kind":"and","text":"We wait for the application thread to work off the queue, and for the UI thread to catch up.","code":["app.awaitAllProcessed()","UI.sync()"]},

        {"kind":"then","text":"Every single one of the 450 increments arrived in the view model.","code":["counter.is(450)"]},

        {"kind":"and","text":"The label displays the exact final state.","code":["label.text == \"450\""]},

        {"kind":"and","text":"No event task failed, and nothing was silently logged as an error along the way.","code":["app.failures().isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["app.stop()","log.detach()"]},

        {"kind":"where","text":"We repeat this race a few times to explore different thread interleavings.","code":{"run":[]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Under real concurrent load, from multiple threads, not a single update is lost. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The previous features scripted the thread handover step by step.\n            This one lets go of the reins: a continuously draining application\n            thread runs in the background while the UI thread and two\n            additional producer threads all funnel counter increments into\n            the application event queue, concurrently and as fast as they can.\n\n            Because all view model mutations are executed by the single\n            application thread, they are serialized, and so, no matter how\n            the threads interleave, the final counter must be exact and\n            the label must display it. The feature is repeated several times\n            to explore different thread interleavings.\n\n            Also note that a log spy watches the whole ride: if anything\n            inside SwingTree fails silently and merely logs an error,\n            this test fails.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A spy on the log, so that even silently logged errors fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A continuously draining application thread, like in a real decoupled application.","code":["var app = ApplicationThread.startDraining()"]},

        {"kind":"and","text":"A view model counter, and a decoupled view with an incrementing button and a label.","code":["var counter = Var.of(0)","var view = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.button(\"+\").onClick({ counter.set(counter.get() + 1) }))","        .add(UI.label(counter.viewAsString()))","    ).get(JPanel)","})","var button = (JButton) view.getComponent(0)","var label  = (JLabel)  view.getComponent(1)"]},

        {"kind":"when","text":"Two producer threads each enqueue 150 increments, while the UI thread fires 150 button clicks.","code":["var producers = (1..2).collect { producerNumber ->","    new Thread({","        150.times {","            EventProcessor.DECOUPLED.registerAppEvent({ counter.set(counter.get() + 1) })","        }","    }, \"producer-$producerNumber\")","}","producers.each { it.start() }","150.times {","    UI.runNow({ button.doClick() })","}","producers.each { it.join(10_000) }"]},

        {"kind":"and","text":"We wait for the application thread to work off the queue, and for the UI thread to catch up.","code":["app.awaitAllProcessed()","UI.sync()"]},

        {"kind":"then","text":"Every single one of the 450 increments arrived in the view model.","code":["counter.is(450)"]},

        {"kind":"and","text":"The label displays the exact final state.","code":["label.text == \"450\""]},

        {"kind":"and","text":"No event task failed, and nothing was silently logged as an error along the way.","code":["app.failures().isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["app.stop()","log.detach()"]},

        {"kind":"where","text":"We repeat this race a few times to explore different thread interleavings.","code":{"run":[]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [8]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [9]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [10]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"All kinds of bound widget state receive their property updates asynchronously, on the UI thread. [11]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The SwingTree API offers countless ways to bind component state\n            to properties: text, selection flags, slider values, selected\n            combo box items, enabled and visible flags, colors, and much more.\n            All of them cross the thread boundary through the same mechanism,\n            and so all of them share the same contract, which this data driven\n            feature verifies across a whole zoo of widgets and state types:\n\n            A property change made by the application thread is **not**\n            applied to the component synchronously. It is dispatched to the\n            UI thread, and until that thread gets around to it, the component\n            keeps its old state. Just like in the earlier features, we make\n            this deterministic by parking the UI thread while we look\n            at the in-flight update.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A widget bound to the property, built in decoupled mode. (See the `where` table for the current case!)","code":["var component = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> declare(property))","})"]},

        {"kind":"expect","text":"Initially the component state reflects the property value.","code":["read(component) == property.get()"]},

        {"kind":"when","text":"We park the UI thread, change the property from this thread, and peek at the component before releasing the UI thread again.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var stateRightAfterSet = null","try {","    property.set(newValue)","    stateRightAfterSet = read(component)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"Right after the property change, the component still had its old state...","code":["stateRightAfterSet != newValue"]},

        {"kind":"and","text":"...but as soon as the UI thread caught up, the new state arrived.","code":["read(component) == newValue"]},

        {"kind":"where","text":"The same asynchronous update contract holds for all of the following widget states:","code":{"widgetState":["'JLabel text'","'JTextField text'","'JCheckBox selection'","'JToggleButton selection'","'JSlider value'","'JProgressBar value'","'JSpinner value'","'JComboBox selection'","'JTabbedPane selected index'","'enabled flag'","'visible flag'","'background color'"],"property":["Var.of(\"Old\")","Var.of(\"Old\")","Var.of(false)","Var.of(false)","Var.of(10)","Var.of(10)","Var.of(5)","Var.of(\"B\")","Var.of(0)","Var.of(true)","Var.of(true)","Var.of(UI.Color.RED)"],"newValue":["\"New!\"","\"New!\"","true","true","90","90","42","\"C\"","1","false","false","UI.Color.BLUE"],"declare":["{ p -> UI.label(p).get(JLabel) }","{ p -> UI.textField(p).get(JTextField) }","{ p -> UI.checkBox(\"Check me\", p).get(JCheckBox) }","{ p -> UI.toggleButton(\"Toggle me\", p).get(JToggleButton) }","{ p -> UI.slider(UI.Align.HORIZONTAL).withMin(0).withMax(100).withValue(p).get(JSlider) }","{ p -> UI.progressBar(0, 100, p).get(JProgressBar) }","{ p -> UI.spinner(p).get(JSpinner) }","{ p -> UI.comboBox(p, [\"A\", \"B\", \"C\"]).get(JComboBox) }","{ p -> UI.tabbedPane().add(UI.tab(\"One\")).add(UI.tab(\"Two\")).withSelectedIndex(p).get(JTabbedPane) }","{ p -> UI.button(\"Button\").isEnabledIf(p).get(JButton) }","{ p -> UI.button(\"Button\").isVisibleIf(p).get(JButton) }","{ p -> UI.label(\"Colorful!\").withBackground(p).get(JLabel) }"],"read":["{ c -> c.text }","{ c -> c.text }","{ c -> c.selected }","{ c -> c.selected }","{ c -> c.value }","{ c -> c.value }","{ c -> c.value }","{ c -> c.selectedItem }","{ c -> c.selectedIndex }","{ c -> c.enabled }","{ c -> c.visible }","{ c -> c.background }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A combo box selection made by the user travels to the application thread before it reaches the bound property.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The selection state of a combo box follows the same threading\n            convention as every other bound widget state: the `JComboBox`\n            works with a UI thread owned copy of the selection, and the bound\n            property is only ever read and written by the application thread.\n\n            So when the user picks an item, the combo box updates its own\n            state immediately, but the new selection reaches the property\n            asynchronously, through the application event queue. This has\n            an important consequence for your view models: the `onChange`\n            listeners of the selection property are guaranteed to run on\n            the application thread, never on the UI thread, so your business\n            logic cannot accidentally block or corrupt the UI.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A selection property with a change listener which records the thread it runs on.","code":["var trace = new CopyOnWriteArrayList<String>()","var selection = Var.of(\"B\")","selection.onChange(From.VIEW, it ->","    trace << \"changed to '${it.currentValue().orElseThrowUnchecked()}' on '${Thread.currentThread().name}'\".toString()",")"]},

        {"kind":"and","text":"A combo box built around the property in decoupled mode.","code":["var combo = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()-> UI.comboBox(selection, [\"A\", \"B\", \"C\"])).get(JComboBox)","})"]},

        {"kind":"expect","text":"The combo box reports the initial selection.","code":["combo.selectedItem == \"B\""]},

        {"kind":"when","text":"The user selects \"C\", on the UI thread.","code":["UI.runNow({ combo.selectedItem = \"C\" })"]},

        {"kind":"then","text":"The combo box itself shows the new selection right away, it is UI owned state...","code":["combo.selectedItem == \"C\""]},

        {"kind":"and","text":"...but the property has not been touched yet! The write is waiting in the application event queue.","code":["selection.is(\"B\")","trace.isEmpty()"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue.","code":["EventProcessor.DECOUPLED.joinUntilDoneOrException()"]},

        {"kind":"then","text":"Now the property was updated, and its change listener ran on the application thread.","code":["selection.is(\"C\")","trace == [\"changed to 'C' on '${Thread.currentThread().name}'\".toString()]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The selectable options of a combo box, modelled as a tuple property, update asynchronously on the UI thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Not only the selection, but also the *options* of a combo box may\n            live in your view model, for example as a `Var<Tuple<E>>` property\n            bound through `withItems(selection, options)`.\n            Since a `Tuple` is deeply immutable, the two threads simply publish\n            their state to each other: the combo box works with a UI thread\n            owned snapshot of the tuple, which is refreshed asynchronously\n            whenever the property changes on the application thread.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A selection property and a tuple property of options.","code":["var selection = Var.of(\"B\")","var options = Var.of(Tuple.of(\"A\", \"B\", \"C\"))"]},

        {"kind":"and","text":"A combo box bound to both, built in decoupled mode.","code":["var combo = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.comboBox().withItems(selection, options)","    ).get(JComboBox)","})"]},

        {"kind":"expect","text":"The combo box offers the initial options.","code":["combo.itemCount == 3"]},

        {"kind":"when","text":"We park the UI thread, add an option from this thread, and peek at the combo box before releasing the UI thread.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var itemCountWhileParked = -1","try {","    options.update( it -> it.add(\"D\") )","    itemCountWhileParked = combo.itemCount","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"While the UI thread was parked, the combo box still offered the old options...","code":["itemCountWhileParked == 3"]},

        {"kind":"and","text":"...but once it caught up, the new option appeared.","code":["combo.itemCount == 4","combo.getItemAt(3) == \"D\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The selectable options of a combo box, modelled as a `Vars` property list, update asynchronously on the UI thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The options of a combo box may also be modelled as a `Vars`\n            property list, whose change events keep the UI thread owned\n            snapshot of the combo box in sync, following the same\n            asynchronous publication protocol as all other property\n            based widget state.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A selection property and a property list of options.","code":["var selection = Var.of(\"B\")","var options = Vars.of(\"A\", \"B\", \"C\")"]},

        {"kind":"and","text":"A combo box bound to both, built in decoupled mode.","code":["var combo = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.comboBox(selection, options)","    ).get(JComboBox)","})"]},

        {"kind":"expect","text":"The combo box offers the initial options.","code":["combo.itemCount == 3"]},

        {"kind":"when","text":"We park the UI thread, add an option from this thread, and peek at the combo box before releasing the UI thread.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var itemCountWhileParked = -1","try {","    options.add(\"D\")","    itemCountWhileParked = combo.itemCount","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"While the UI thread was parked, the combo box still offered the old options...","code":["itemCountWhileParked == 3"]},

        {"kind":"and","text":"...but once it caught up, the new option appeared.","code":["combo.itemCount == 4","combo.getItemAt(3) == \"D\""]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A tuple of tab models bound to a tabbed pane is kept in sync from the application thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Structural bindings are far more delicate than plain value\n            bindings: when a tuple property of tab models changes, the\n            tabbed pane is updated *incrementally*, based on a diff between\n            the old and the new tuple, rather than being rebuilt from scratch.\n            Across a thread boundary this only works if each change arrives\n            on the UI thread together with the diff information belonging to\n            exactly that change, and if the changes are replayed in their\n            original order.\n\n            To stress precisely that, this test parks the UI thread and fires\n            a whole burst of structural changes, adding, removing and renaming\n            tabs, from the application thread. Only then is the UI thread\n            allowed to catch up and replay the queue.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A tuple property of tab titles bound to a tabbed pane in decoupled mode.","code":["var models = Var.of(Tuple.of(\"Overview\", \"Details\", \"Settings\"))","TabSupplier<String> supplier = (String title) -> UI.tab(title)","var pane = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.tabbedPane().addAll(models, supplier)","    ).get(JTabbedPane)","})"]},

        {"kind":"expect","text":"The pane starts out with one tab per model.","code":["pane.tabCount == 3","(0..2).collect({ pane.getTitleAt(it) }) == [\"Overview\", \"Details\", \"Settings\"]"]},

        {"kind":"when","text":"We park the UI thread and fire a burst of structural changes from this thread.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var tabCountWhileParked = -1","try {","    models.update( it -> it.add(\"Help\") )","    models.update( it -> it.removeAt(0) )","    models.update( it -> it.setAt(0, \"Detail View\") )","    tabCountWhileParked = pane.tabCount","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"While the UI thread was parked, the pane still had its old structure.","code":["tabCountWhileParked == 3"]},

        {"kind":"and","text":"After the UI thread replayed the queue, the pane matches the final tuple state exactly.","code":["pane.tabCount == models.get().size()","(0..2).collect({ pane.getTitleAt(it) }) == [\"Detail View\", \"Settings\", \"Help\"]","models.get().toList() == [\"Detail View\", \"Settings\", \"Help\"]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Boolean selection properties of individual tabs stay consistent when the selection crosses the thread boundary.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Each tab of a tabbed pane may be bound to its own boolean\n            selection property. This is an especially tricky binding, because\n            the properties are coupled to each other through the pane:\n            selecting one tab must flip its property to true and the property\n            of the previously selected tab to false.\n\n            In the decoupled mode, this dance happens across threads, in both\n            directions: a user selection on the UI thread must propagate\n            outward to the properties, and a property change on the\n            application thread must propagate inward to the pane, without\n            the resulting updates echoing back and forth endlessly.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Three boolean selection properties, one per tab.","code":["var overviewSelected = Var.of(false)","var detailsSelected  = Var.of(false)","var settingsSelected = Var.of(false)"]},

        {"kind":"and","text":"A tabbed pane in decoupled mode, each tab bound to its property.","code":["var pane = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.tabbedPane()","        .add(UI.tab(\"Overview\").isSelectedIf(overviewSelected))","        .add(UI.tab(\"Details\").isSelectedIf(detailsSelected))","        .add(UI.tab(\"Settings\").isSelectedIf(settingsSelected))","    ).get(JTabbedPane)","})"]},

        {"kind":"expect","text":"Since no property is true, nothing is selected initially.","code":["pane.selectedIndex == -1"]},

        {"kind":"when","text":"The user selects the second tab on the UI thread, and both worlds settle.","code":["UI.runNow({ pane.selectedIndex = 1 })","letBothWorldsSettle()"]},

        {"kind":"then","text":"Exactly the second property is now true.","code":["!overviewSelected.get() && detailsSelected.get() && !settingsSelected.get()"]},

        {"kind":"when","text":"The application thread flips the third selection flag, and both worlds settle.","code":["settingsSelected.set(true)","letBothWorldsSettle()"]},

        {"kind":"then","text":"The pane followed the property...","code":["pane.selectedIndex == 2"]},

        {"kind":"and","text":"...and the flag of the previously selected tab was flipped back to false.","code":["!overviewSelected.get() && !detailsSelected.get() && settingsSelected.get()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A bound selection index follows structural tab changes made by the application thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            This scenario combines two bindings which must cooperate across\n            the thread boundary: the tabs themselves are bound to a tuple of\n            models, and the selection is bound to an integer index property.\n\n            When the application thread inserts a new tab *before* the\n            currently selected one, the selected tab shifts one position to\n            the right. The pane must keep the same tab selected, and the new\n            index must travel back into the index property, so that the view\n            model never falls out of sync with what the user is looking at.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A tuple of tab models and a selection index property pointing at the last tab.","code":["var models = Var.of(Tuple.of(\"A\", \"B\", \"C\"))","var selectedIndex = Var.of(2)","TabSupplier<String> supplier = (String title) -> UI.tab(title)"]},

        {"kind":"and","text":"A tabbed pane in decoupled mode with both bindings attached.","code":["var pane = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.tabbedPane()","        .withSelectedIndex(selectedIndex)","        .addAll(models, supplier)","    ).get(JTabbedPane)","})"]},

        {"kind":"expect","text":"The pane starts with tab \"C\" selected, as the index property demands.","code":["pane.tabCount == 3","pane.selectedIndex == 2","pane.getTitleAt(pane.selectedIndex) == \"C\""]},

        {"kind":"when","text":"The application thread inserts a new tab model at the very front, and both worlds settle.","code":["models.update( it -> it.addAt(0, \"Start\") )","letBothWorldsSettle()"]},

        {"kind":"then","text":"The pane now has four tabs and still shows the same selected tab, at its new position.","code":["pane.tabCount == 4","pane.selectedIndex == 3","pane.getTitleAt(pane.selectedIndex) == \"C\""]},

        {"kind":"and","text":"The new position also travelled back into the view model property.","code":["selectedIndex.get() == 3"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A tuple of view models rendered as sub-views of a panel stays consistent under cross-thread updates.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Just like tabs, plain sub-views may be bound to a tuple property,\n            with a view supplier creating one sub-view per item. The items are\n            identified by their `HasId.id()` and every item is handed to the\n            supplier as a property of its own, so there are two layers of\n            binding at work here: structural changes to the tuple add or\n            remove whole sub-views, while replacing an item with an updated\n            model carrying the *same id* recycles the existing sub-view and\n            channels the new state into it through its item property.\n            All of it crosses the thread boundary in the decoupled mode.\n\n            Note that just like in the other features, we park the UI thread\n            while firing a burst of updates from the application thread.\n            This proves two things at once: the updates are applied\n            asynchronously and in order, and mutating the bound tuple never\n            requires the application thread to wait for the UI thread.\n            (Historically it did! The item properties used to locate their\n            items by consulting the component tree, so a mere `update(..)`\n            call could block on, or even deadlock with, the UI thread.)\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A tuple property of little view models, rendered as labels inside a panel.","code":["var models = Var.of(Tuple.of(","    new Item(\"a\", \"Alpha\"),","    new Item(\"b\", \"Beta\"),","    new Item(\"c\", \"Gamma\")","))","BoundViewSupplier<Item> supplier = { Var<Item> model -> UI.label(model.viewAsString({ it.text() })) }","var panel = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel().addAll(models, supplier)","    ).get(JPanel)","})"]},

        {"kind":"expect","text":"The panel renders one label per item.","code":["panel.componentCount == 3","_textsOf(panel) == [\"Alpha\", \"Beta\", \"Gamma\"]"]},

        {"kind":"when","text":"We remember the sub-view of item \"a\" and then park the UI thread while firing a burst of updates from this thread.","code":["var viewOfItemA = panel.getComponent(0)","var gate = new CountDownLatch(1)","UI.run({ gate.await() })","try {","    models.update( it -> it.removeAt(1) )","    models.update( it -> it.addAll(new Item(\"d\", \"Delta\"), new Item(\"e\", \"Epsilon\")) )","    models.update( it -> it.setAt(0, new Item(\"a\", \"Alef\")) ) // <- same id, new text!","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"The panel replayed all changes in order and matches the final tuple state.","code":["panel.componentCount == models.get().size()","_textsOf(panel) == [\"Alef\", \"Gamma\", \"Delta\", \"Epsilon\"]","models.get().toList().collect({ it.text() }) == [\"Alef\", \"Gamma\", \"Delta\", \"Epsilon\"]"]},

        {"kind":"and","text":"The update of item \"a\" was channeled into its already existing sub-view, no new view was created for it.","code":["panel.getComponent(0).is(viewOfItemA)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `scrollPanels` component handles cross-thread tuple updates just like a plain panel.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `UI.scrollPanels()` component is the scrollable sibling of a\n            tuple bound panel: it wraps each generated sub-view in its own\n            internal entry panel. It shares the id based item binding with\n            the plain panel, so the same threading contract applies, which\n            this feature verifies with the same parked burst of updates\n            fired from the application thread.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A tuple property of view models, rendered as labels inside scroll panels.","code":["var models = Var.of(Tuple.of(","    new Item(\"a\", \"Alpha\"),","    new Item(\"b\", \"Beta\"),","    new Item(\"c\", \"Gamma\")","))","BoundViewSupplier<Item> supplier = { Var<Item> model -> UI.label(model.viewAsString({ it.text() })) }","var panels = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.scrollPanels().addAll(models, supplier)","    ).get(swingtree.components.JScrollPanels)","})"]},

        {"kind":"expect","text":"The component renders one entry per item.","code":["panels.numberOfEntries == 3"]},

        {"kind":"when","text":"We park the UI thread and fire a burst of structural changes and item updates from this thread.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","try {","    models.update( it -> it.removeAt(1) )","    models.update( it -> it.addAll(new Item(\"d\", \"Delta\"), new Item(\"e\", \"Epsilon\")) )","    models.update( it -> it.setAt(0, new Item(\"a\", \"Alef\")) ) // <- same id, new text!","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"The entries replayed all changes in order and match the final tuple state.","code":["panels.numberOfEntries == models.get().size()","_entryTextsOf(panels) == [\"Alef\", \"Gamma\", \"Delta\", \"Epsilon\"]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"An enum selection made through a radio button waits in the application event queue instead of touching the property directly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `isSelectedIf(state, property)` binding selects a toggle\n            button whenever the bound property is equal to the given\n            reference value, and conversely writes that reference value\n            into the property when the user selects the button.\n\n            The write direction must follow the same threading convention\n            as every other view-to-model communication: the UI thread hands\n            the new value over to the application event queue instead of\n            writing into the property directly, so that view model listeners\n            never run on the UI thread.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"An enum property with a change listener which records the thread it runs on.","code":["var trace = new CopyOnWriteArrayList<String>()","var step = Var.of(Step.ONE)","step.onChange(From.VIEW, it ->","    trace << \"changed to '${it.currentValue().orElseThrowUnchecked()}' on '${Thread.currentThread().name}'\".toString()",")"]},

        {"kind":"and","text":"Two radio buttons bound to the property, built in decoupled mode.","code":["var buttons = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.panel()","        .add(UI.radioButton(\"one\").isSelectedIf(Step.ONE, step))","        .add(UI.radioButton(\"two\").isSelectedIf(Step.TWO, step))","    ).get(JPanel)","}).components as List<JRadioButton>"]},

        {"kind":"expect","text":"The first button starts out selected, as the property demands.","code":["buttons[0].selected","!buttons[1].selected"]},

        {"kind":"when","text":"The user selects the second button, on the UI thread.","code":["UI.runNow({ buttons[1].setSelected(true) })"]},

        {"kind":"then","text":"The button itself shows the new selection right away, it is UI owned state...","code":["buttons[1].selected"]},

        {"kind":"and","text":"...but the property has not been touched yet! The write is waiting in the application event queue.","code":["step.is(Step.ONE)","trace.isEmpty()"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue, and both worlds settle.","code":["letBothWorldsSettle()"]},

        {"kind":"then","text":"The property was updated, and its change listener ran on the application thread.","code":["step.is(Step.TWO)","trace == [\"changed to 'TWO' on '${Thread.currentThread().name}'\".toString()]"]},

        {"kind":"and","text":"The rebroadcast of the new property value also deselected the first button.","code":["!buttons[0].selected","buttons[1].selected"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A divider moved by the user in a split pane waits in the application event queue instead of touching the property directly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `withDivisionOf(Var)` binding of a split pane keeps a double\n            property in sync with the divider location, expressed as a\n            percentage of the pane size. When the user drags the divider,\n            the new percentage must travel through the application event\n            queue instead of being written into the property directly,\n            because the property belongs to the application thread.\n            This matters twice over here, because divider drags fire a\n            continuous stream of location updates.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A percentage property and a split pane bound to it in decoupled mode.","code":["var percentage = Var.of(0.5d)","var pane = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.splitPane(UI.Align.HORIZONTAL).withDivisionOf(percentage)","    ).get(JSplitPane)","})"]},

        {"kind":"and","text":"The pane receives a concrete size, and we let the resulting resize event settle.","code":["UI.runNow({ pane.setSize(200, 200) })","letBothWorldsSettle()"]},

        {"kind":"when","text":"The user drags the divider to the three quarter mark, on the UI thread.","code":["UI.runNow({ pane.setDividerLocation(150) })"]},

        {"kind":"then","text":"The pane itself shows the new divider location right away, it is UI owned state...","code":["UI.runAndGet({ pane.dividerLocation }) == 150"]},

        {"kind":"and","text":"...but the property has not been touched yet! The write is waiting in the application event queue.","code":["percentage.is(0.5d)"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue.","code":["EventProcessor.DECOUPLED.joinUntilDoneOrException()"]},

        {"kind":"then","text":"Now the new percentage arrived in the view model.","code":["Math.abs(percentage.get() - 0.75d) < 0.05d"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A tab selected by the user waits in the application event queue instead of touching the index property directly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `withSelectedIndex(Var)` binding of a tabbed pane writes the\n            index of the tab selected by the user back into the bound\n            property. Like every other view-to-model communication, this\n            write must travel through the application event queue, so that\n            the listeners of the index property are guaranteed to run on\n            the application thread, never on the UI thread.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"An index property with a change listener which records the thread it runs on.","code":["var trace = new CopyOnWriteArrayList<String>()","var selectedIndex = Var.of(0)","selectedIndex.onChange(From.VIEW, it ->","    trace << \"changed to '${it.currentValue().orElseThrowUnchecked()}' on '${Thread.currentThread().name}'\".toString()",")"]},

        {"kind":"and","text":"A tabbed pane with three tabs, bound to the property in decoupled mode.","code":["var pane = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.tabbedPane()","        .withSelectedIndex(selectedIndex)","        .add(UI.tab(\"Overview\"))","        .add(UI.tab(\"Details\"))","        .add(UI.tab(\"Settings\"))","    ).get(JTabbedPane)","})"]},

        {"kind":"expect","text":"The first tab starts out selected, as the property demands.","code":["pane.selectedIndex == 0"]},

        {"kind":"when","text":"The user selects the third tab, on the UI thread.","code":["UI.runNow({ pane.selectedIndex = 2 })"]},

        {"kind":"then","text":"The pane itself shows the new selection right away, it is UI owned state...","code":["UI.runAndGet({ pane.selectedIndex }) == 2"]},

        {"kind":"and","text":"...but the property has not been touched yet! The write is waiting in the application event queue.","code":["selectedIndex.is(0)","trace.isEmpty()"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue.","code":["EventProcessor.DECOUPLED.joinUntilDoneOrException()"]},

        {"kind":"then","text":"Now the property was updated, and its change listener ran on the application thread.","code":["selectedIndex.is(2)","trace == [\"changed to '2' on '${Thread.currentThread().name}'\".toString()]"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The knob of a slider bound to fractional min, max and value properties moves asynchronously, on the UI thread.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When a slider is bound to fractional number properties, the\n            integer range of the underlying `JSlider` is derived from all\n            three properties together: min and max determine a scale factor,\n            and the value is mapped into the resulting integer range.\n\n            Because of that coupling, the UI thread must never read the\n            properties directly, otherwise it could observe a min from one\n            update and a max from another, and derive an inconsistent range.\n            Instead it works with UI thread owned copies of all three values,\n            refreshed through the property change events in their original\n            order. This feature updates the whole triple from the application\n            thread while the UI thread is parked, and checks that the slider\n            ends up in a state consistent with the final triple.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"Three fractional properties and a slider bound to all of them in decoupled mode.","code":["var min = Var.of(0d)","var max = Var.of(1d)","var current = Var.of(0.5d)","var slider = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.slider(UI.Align.HORIZONTAL, min, max, current)","    ).get(JSlider)","})"]},

        {"kind":"expect","text":"The knob starts out in the middle of the integer range.","code":["_fractionOf(slider) == 0.5d"]},

        {"kind":"when","text":"We park the UI thread and update the whole triple from this thread, peeking at the slider before releasing the UI thread.","code":["var gate = new CountDownLatch(1)","UI.run({ gate.await() })","var fractionWhileParked = -1d","try {","    min.set(-1d)","    max.set(3d)","    current.set(1d)","    fractionWhileParked = _fractionOf(slider)","} finally {","    gate.countDown()","}","UI.sync()"]},

        {"kind":"then","text":"While the UI thread was parked, the knob had not moved yet...","code":["fractionWhileParked == 0.5d"]},

        {"kind":"and","text":"...but once the UI thread replayed the updates, the knob sits at the position of the final triple.","code":["_fractionOf(slider) == 0.5d // (1 - (-1)) / (3 - (-1))","slider.minimum < slider.value","slider.value < slider.maximum"]},

        {"kind":"when","text":"The user then drags the knob all the way to the maximum, on the UI thread.","code":["UI.runNow({ slider.value = slider.maximum })"]},

        {"kind":"then","text":"The value property has not been touched yet! The write is waiting in the application event queue.","code":["current.is(1d)"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue.","code":["EventProcessor.DECOUPLED.joinUntilDoneOrException()"]},

        {"kind":"then","text":"Now the property holds the exact maximum of the bound range.","code":["current.is(3d)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
