{
  "className":"swingtree.SwingTree_Library_Context_Spec",
  "title":"Library Context",
  "narrative":"SwingTree is a feature rich library in which the\n    default behavior does not fit every use case.\n    This is why you can initialize the library with a custom\n    start configuration expressing your needs.\n\n    This library context is a singleton called `SwingTree` and\n    it can be initialized with a `SwingTreeInitConfig`\n    inside of a configurator lambda like so:\n\n    ```\n        SwingTree.initializeUsing( conf -> conf\n            .uiScaleFactor(2f)\n            .styleSheet(myStyleSheet)\n            .defaultAnimationInterval(42)\n        )\n    ```",
  "subjects":["swingtree.SwingTree","swingtree.SwingTreeInitConfig"],
  "statistics":{
    "runs":"17",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"The library context can be configured with custom settings.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            In this unit test you can see how to initialize the library context with\n            various custom settings. These include a custom UI scale factor,\n            a global style configuration in the form of a `StyleSheet`, \n            an event processor for running events on a custom thread and \n            much more.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We snapshot the original default font to reset it after the test","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")"]},

        {"kind":"and","text":"We initialize SwingTree with some custom settings!","code":["var myStyleSheet = new StyleSheet() { @Override protected void configure() {} }","var myEventProcessor = Mock(EventProcessor)","var myMarker = MarkerFactory.getMarker(\"MyMarker\")","var myFont = new java.awt.Font(\"Arial\", java.awt.Font.ITALIC, 73)","SwingTree.initializeUsing(conf -> conf","        .uiScaleFactor(4.2f)","        .styleSheet(myStyleSheet)","        .defaultAnimationInterval(42)","        .isUiScaleFactorEnabled(true)","        .logMarker(myMarker)","        .eventProcessor(myEventProcessor)","        .defaultFont(myFont, SwingTreeInitConfig.FontInstallation.SOFT)",")"]},

        {"kind":"expect","text":"The settings are applied to the library context.","code":["SwingTree.get().getUiScaleFactor() == 4.2f","SwingTree.get().getEventProcessor() == myEventProcessor","SwingTree.get().getStyleSheet() == myStyleSheet","SwingTree.get().getDefaultAnimationInterval() == 42","SwingTree.get().isUiScaleFactorEnabled() == true","SwingTree.get().logMarker() == myMarker"]},

        {"kind":"and","text":"","code":["UIManager.getDefaults().get(\"defaultFont\") === myFont"]},

        {"kind":"cleanup","text":"Teardown the library context and reset the \"defaultFont\" in the `UIManager` to the original value!","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", originalDefaultFont)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The soft font installation only installs the \"defaultFont\" but not every regular font.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            If you use scalable Look and Feels like FlatLaf, you can easily make\n            your application scale through the \"defaultFont\".\n            SwingTree as well as FlatLaf and many other libraries and frameworks then\n            use the \"defaultFont\" to compute a `float` scaling factor in the same magnitude\n            as the default font size. This scale factor is then used to scale the UI.\n            You can configure this default font in when initializing the library context!\n        "]
      },
      "blocks":[
        {"kind":"given","text":"","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")"]},

        {"kind":"and","text":"We snapshot the original default and panel fonts to reset them after the test.","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")","var originalPanelFont = UIManager.getDefaults().get(\"Panel.font\")"]},

        {"kind":"and","text":"We create a custom and unique default font as well as a custom panel font","code":["var myDefaultFont = new java.awt.Font(\"Arial\", java.awt.Font.ITALIC, 73)","var myPanelFont = new java.awt.Font(\"Comic Sans MS\", java.awt.Font.BOLD, 42)"]},

        {"kind":"and","text":"We set the panel font and initialize SwingTree with the custom default font as a soft installation.","code":["UIManager.getDefaults().put(\"Panel.font\", myPanelFont)","SwingTree.initializeUsing(conf -> conf","    .defaultFont(myDefaultFont, SwingTreeInitConfig.FontInstallation.SOFT)",")"]},

        {"kind":"expect","text":"SwingTree has its scaling factor computed from the default font size:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 6f : 4.75f)","UI.scale() == (isWindows ? 6f : 4.75f) // delegates to the above method"]},

        {"kind":"and","text":"The default font is changed to the custom one.","code":["UIManager.getDefaults().get(\"defaultFont\") === myDefaultFont"]},

        {"kind":"and","text":"The panel font is not changed to the custom one, because we only installed the default font softly.","code":["UIManager.getDefaults().get(\"Panel.font\") === myPanelFont"]},

        {"kind":"cleanup","text":"Reset the library context to be `null` again internally!","code":["SwingTree.clear()"]},

        {"kind":"and","text":"We also reset the default and panel fonts to the original ones:","code":["UIManager.getDefaults().put(\"defaultFont\", originalDefaultFont)","UIManager.getDefaults().put(\"Panel.font\", originalPanelFont)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The hard font installation installs the default font as well as every regular font.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            If the Look and Feel you are using does not support UI scaling there is\n            a brutal but effective way to at least scale all of the fonts in your application.\n            When you initialize the library context, you can choose to do a \"hard\" font installation. \n            This means that the default font is not only installed as the \"defaultFont\" but also as every \n            regular font like \"Panel.font\", \"Button.font\" and so on.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")"]},

        {"kind":"and","text":"We snapshot the original default and panel fonts to reset them after the test.","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")","var originalPanelFont = UIManager.getDefaults().get(\"Panel.font\")"]},

        {"kind":"and","text":"We create a custom and unique default font as well as a custom panel font","code":["var myDefaultFont = new FontUIResource(new java.awt.Font(\"Arial\", java.awt.Font.ITALIC, 73))","var myPanelFont = new FontUIResource(new java.awt.Font(\"Comic Sans MS\", java.awt.Font.BOLD, 42))"]},

        {"kind":"and","text":"We set the panel font and initialize SwingTree with the custom default font as a hard installation.","code":["UIManager.getDefaults().put(\"Panel.font\", myPanelFont)","SwingTree.initializeUsing(conf -> conf","        .defaultFont(myDefaultFont, SwingTreeInitConfig.FontInstallation.HARD)",")"]},

        {"kind":"expect","text":"SwingTree has its scaling factor computed from the default font size:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 6f : 4.75f)","UI.scale() == (isWindows ? 6f : 4.75f) // delegates to the above method"]},

        {"kind":"and","text":"The default font is changed to the custom one.","code":["UIManager.getDefaults().get(\"defaultFont\") === myDefaultFont"]},

        {"kind":"and","text":"The panel font is also changed to the custom one, because we installed every regular font hard.","code":["UIManager.getDefaults().get(\"Panel.font\") === myDefaultFont"]},

        {"kind":"cleanup","text":"Reset the library context to be `null` again internally!","code":["SwingTree.clear()"]},

        {"kind":"and","text":"We also reset the default and panel fonts to the original ones:","code":["UIManager.getDefaults().put(\"defaultFont\", originalDefaultFont)","UIManager.getDefaults().put(\"Panel.font\", originalPanelFont)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can specify a default font for the SwingTree UI scale without installing it in the `UIManager`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            If you want SwingTree to compute its UI scale factor from a custom default font\n            but you do not want to install this font in the `UIManager`, you can choose the \"none\"\n            font installation when initializing the library context.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")"]},

        {"kind":"and","text":"We snapshot the original default font to check if it stays the same!","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")"]},

        {"kind":"and","text":"We create a custom and unique default font","code":["var myDefaultFont = new java.awt.Font(\"Arial\", java.awt.Font.ITALIC, 73)"]},

        {"kind":"and","text":"We initialize SwingTree with the custom default font but do not install it.","code":["SwingTree.initializeUsing(conf -> conf","    .defaultFont(myDefaultFont, SwingTreeInitConfig.FontInstallation.NONE)",")"]},

        {"kind":"expect","text":"SwingTree has its scaling factor computed from the default font size:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 6f : 4.75f)","UI.scale() == (isWindows ? 6f : 4.75f) // delegates to the above method"]},

        {"kind":"and","text":"The default font is not installed in the UIManager.","code":["UIManager.getDefaults().get(\"defaultFont\") !== myDefaultFont","UIManager.getDefaults().get(\"defaultFont\") === originalDefaultFont"]},

        {"kind":"cleanup","text":"Reset the library context to be `null` again internally!","code":["SwingTree.clear()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A NONE font installation uses the supplied font even when the `UIManager` already has a different \"defaultFont\".",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The contract of `FontInstallation.NONE` is that SwingTree should compute its UI scale\n            factor from the supplied default font *and* ignore the `UIManager` state entirely.\n\n            This unit test reproduces the conditions under which other tests in the suite\n            (typically those that exercise alternative Look and Feels like Nimbus or FlatLaf)\n            can leak a non-null \"defaultFont\" into the `UIManager` and used to make the\n            sibling specification flaky: the previously stored font was preferred over the\n            user-supplied one, producing an unexpected scale factor.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A stale `UIManager` default font from a hypothetical earlier scenario:","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")","var staleFont = new java.awt.Font(\"Serif\", java.awt.Font.PLAIN, 12)","UIManager.getDefaults().put(\"defaultFont\", staleFont)"]},

        {"kind":"and","text":"A unique default font that we want SwingTree to scale from:","code":["var myDefaultFont = new java.awt.Font(\"Arial\", java.awt.Font.ITALIC, 73)"]},

        {"kind":"and","text":"We initialize SwingTree with the custom default font using a NONE installation:","code":["SwingTree.initializeUsing(conf -> conf","    .defaultFont(myDefaultFont, SwingTreeInitConfig.FontInstallation.NONE)",")"]},

        {"kind":"expect","text":"The scale factor is derived from our supplied font, not the leftover one in the `UIManager`:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 6f : 4.75f)","UI.scale() == (isWindows ? 6f : 4.75f)"]},

        {"kind":"and","text":"The `UIManager` \"defaultFont\" is left untouched, as documented by the NONE installation:","code":["UIManager.getDefaults().get(\"defaultFont\") === staleFont"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"SwingTree notices when the \"defaultFont\" changes in the `UIManager` and it updates its UI scale factor accordingly.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            When `SwingTree` initializes, it computes its UI scale factor from the size of the \"defaultFont\" in \n            the `UIManager` and also registers a listener to notice when the \"defaultFont\" changes. \n            So when you change the default font through the `UIManager` in your code, then\n            the entire SwingTree UI will scale up or down accordingly.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")"]},

        {"kind":"and","text":"We snapshot the original default font to reset it after the test","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")"]},

        {"kind":"and","text":"We create a custom and unique default font in the `UIManager`:","code":["var myDefaultFont = new java.awt.Font(\"Arial\", java.awt.Font.BOLD, 42)","UIManager.getDefaults().put(\"defaultFont\", myDefaultFont)"]},

        {"kind":"and","text":"We initialize `SwingTree`, it should use the current \"defaultFont\" for initialization...","code":["SwingTree.initialize()"]},

        {"kind":"expect","text":"The UI scale factor is computed from our custom default font:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 3.5f : 2.75f)","UI.scale() == (isWindows ? 3.5f : 2.75f) // delegates to the above method"]},

        {"kind":"when","text":"We change the default font in the `UIManager` to another custom font...","code":["var anotherDefaultFont = new java.awt.Font(\"Comic Sans MS\", java.awt.Font.ITALIC, 73)","UIManager.getDefaults().put(\"defaultFont\", anotherDefaultFont)"]},

        {"kind":"then","text":"SwingTree notices this change and updates its UI scale factor accordingly:","code":["SwingTree.get().getUiScaleFactor() == (isWindows ? 6f : 4.75f)","UI.scale() == (isWindows ? 6f : 4.75f) // delegates to the above method"]},

        {"kind":"cleanup","text":"Reset the library context to be `null` again internally, as well as the default font in the `UIManager`!","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", originalDefaultFont)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can listen to UI scale changes through a reactive property!",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            If you need to react to listen and react to changes of the UI scale\n            factor, then SwingTree offers a memory leak safe way to do this.\n            More specifically you can create a weakly referenced property view\n            which you can register listeners to.\n            When no longer referenced by your code, the property view\n            will be garbage collected together with all of the listeners it holds.\n\n            In this test we demonstrate that the property view receives change events\n            whenever the scale changes in the library context.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"","code":["boolean isWindows = System.getProperty(\"os.name\").toLowerCase().contains(\"win\")"]},

        {"kind":"and","text":"We snapshot the original default font to reset it after the test","code":["var originalDefaultFont = UIManager.getDefaults().get(\"defaultFont\")"]},

        {"kind":"and","text":"We create a custom and unique default font in the `UIManager`:","code":["var myDefaultFont = new java.awt.Font(\"Arial\", java.awt.Font.BOLD, 42)","UIManager.getDefaults().put(\"defaultFont\", myDefaultFont)"]},

        {"kind":"and","text":"We initialize `SwingTree`, it should use the current \"defaultFont\" for initialization...","code":["SwingTree.initialize()"]},

        {"kind":"and","text":"Finally, we create a reactive property view with a trace list:","code":["var trace = []","var scaleView = SwingTree.get().getUiScaleView()","scaleView.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))"]},

        {"kind":"when","text":"We change the default font in the `UIManager` to another custom font...","code":["var anotherDefaultFont = new java.awt.Font(\"Comic Sans MS\", java.awt.Font.ITALIC, 73)","UIManager.getDefaults().put(\"defaultFont\", anotherDefaultFont)"]},

        {"kind":"then","text":"The property view receives the new scale value:","code":["trace == [(isWindows ? 6f : 4.75f)]"]},

        {"kind":"when","text":"We set the scale factor manually through the library context...","code":["SwingTree.get().setUiScaleFactor(1.25f)"]},

        {"kind":"then","text":"The property view receives the manually specified scale:","code":["trace == [(isWindows ? 6f : 4.75f), 1.25f]"]},

        {"kind":"when","text":"We change back to the previous default font...","code":["UIManager.getDefaults().put(\"defaultFont\", myDefaultFont)"]},

        {"kind":"then","text":"The property view receives the previous scale value again:","code":["trace == [(isWindows ? 6f : 4.75f), 1.25f, (isWindows ? 3.5f : 2.75f)]"]},

        {"kind":"cleanup","text":"Reset the library context and set the \"defaultFont\" back to the original value!","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", originalDefaultFont)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can listen to `isDevToolEnabled` property changes through a reactive property!",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Similar to the UI scale factor, you can react to changes of the `isDevToolEnabled` property\n            through a reactive property view, without having to poll for it.\n            The \"dev tool\" is an inspector tool for inspecting the internal state of SwingTree\n            and your UI while the application is running.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We initialize `SwingTree`, by default, the dev tool is always disabled:","code":["SwingTree.initializeUsing(conf -> conf )"]},

        {"kind":"and","text":"We create a reactive property view with a trace list:","code":["var trace = []","var devToolEnabledView = SwingTree.get().isDevToolEnabledView()","devToolEnabledView.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))"]},

        {"kind":"expect","text":"We verify, `SwingTree` reports the \"isDevToolEnabled\" value as `false`:","code":["!SwingTree.get().isDevToolEnabled()"]},

        {"kind":"when","text":"We enable the dev tool through the library context...","code":["SwingTree.get().setDevToolEnabled(true)"]},

        {"kind":"then","text":"The property view receives the new value:","code":["trace == [true]"]},

        {"kind":"when","text":"We disable the dev tool again...","code":["SwingTree.get().setDevToolEnabled(false)"]},

        {"kind":"then","text":"The property view receives the new value again:","code":["trace == [true, false]"]},

        {"kind":"cleanup","text":"Reset the library context!","code":["SwingTree.clear()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The static `getPlatformScaleFactor` returns a sensible UI scale for the current platform without any side effects.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `SwingTree.getPlatformScaleFactor()` is a side-effect free static utility\n            that derives a sensible UI scale factor from the current platform's system\n            font. It does <i>not</i> create or access the `SwingTree` singleton, does\n            <i>not</i> register listeners on the `UIManager`, and does <i>not</i> mutate\n            any shared state.\n\n            On any reasonable platform, the returned value should always fall within\n            `[0.2f, 5f]`. Anything outside this range is almost certainly a bug\n            regardless of the operating system, look and feel, or display configuration.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We snapshot the `UIManager` \"defaultFont\" so we can check it is untouched afterwards.","code":["var defaultFontBefore = UIManager.getDefaults().get(\"defaultFont\")"]},

        {"kind":"when","text":"We ask SwingTree for a platform-derived scale factor.","code":["float scale = SwingTree.getPlatformScaleFactor()"]},

        {"kind":"then","text":"The result is a finite, positive number well inside the sensible range.","code":["Float.isFinite(scale)","scale >= 0.2f","scale <= 5f"]},

        {"kind":"and","text":"Calling the method again yields the exact same value (it is a pure read).","code":["SwingTree.getPlatformScaleFactor() == scale"]},

        {"kind":"and","text":"The `UIManager` \"defaultFont\" was not modified as a side effect.","code":["UIManager.getDefaults().get(\"defaultFont\") === defaultFontBefore"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `getScaledDefaultFont()` method always returns a `FontUIResource`, wrapping a plain `Font` if necessary.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Look and Feel authors usually want a font they can drop straight into\n            `UIDefaults` keys like `Label.font` without breaking Swing's\n            LAF-replacement contract. That contract is gated on `instanceof UIResource`:\n            a plain `Font` is treated as user-set and is *preserved* across LAF swaps,\n            while a `FontUIResource` is replaceable.\n\n            `SwingTree.get().getScaledDefaultFont()` therefore *always* returns a\n            `FontUIResource` — even when the underlying font in the `UIManager`\n            is a plain `Font`, SwingTree wraps it for you.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We seed the `UIManager` with a *plain* `Font` (not a `FontUIResource`) under \"defaultFont\":","code":["var plainFont = new java.awt.Font(\"Arial\", java.awt.Font.PLAIN, 17)","assert !(plainFont instanceof FontUIResource)","UIManager.getDefaults().put(\"defaultFont\", plainFont)"]},

        {"kind":"when","text":"We initialize SwingTree and ask for the default font:","code":["SwingTree.initialize()","var resolved = SwingTree.get().getScaledDefaultFont()"]},

        {"kind":"then","text":"It is a `FontUIResource` (so it can be installed into UIDefaults directly):","code":["resolved instanceof FontUIResource"]},

        {"kind":"and","text":"The wrapped font carries the same family, size and style as the original:","code":["resolved.getFamily() == plainFont.getFamily()","resolved.getSize()   == plainFont.getSize()","resolved.getStyle()  == plainFont.getStyle()"]},

        {"kind":"and","text":"The method never returns `null`:","code":["resolved != null"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `getScaledDefaultFont()` method tracks the currently active \"defaultFont\" in the `UIManager`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            `getScaledDefaultFont()` is the single source of truth for the active default font.\n            Whatever sits under `UIManager.get(\"defaultFont\")` at the time of the call\n            is what comes back (wrapped in a `FontUIResource` if it wasn't one already).\n\n            That means the method always reflects the *current* state — not a snapshot\n            taken when the library was first initialized — so changes pushed through\n            the `UIManager` after bootstrap are observed immediately.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A custom default font installed before SwingTree initializes:","code":["var configuredFont = new FontUIResource(new java.awt.Font(\"Dialog\", java.awt.Font.BOLD, 31))","UIManager.getDefaults().put(\"defaultFont\", configuredFont)"]},

        {"kind":"and","text":"SwingTree initializes:","code":["SwingTree.initialize()"]},

        {"kind":"expect","text":"getScaledDefaultFont() returns exactly the configured font (no wrapping needed):","code":["SwingTree.get().getScaledDefaultFont() === configuredFont"]},

        {"kind":"when","text":"We later swap the default font in the `UIManager`:","code":["var differentFont = new FontUIResource(new java.awt.Font(\"Serif\", java.awt.Font.ITALIC, 24))","UIManager.getDefaults().put(\"defaultFont\", differentFont)"]},

        {"kind":"then","text":"The next call to getScaledDefaultFont() returns the new font:","code":["SwingTree.get().getScaledDefaultFont() === differentFont"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can subscribe to live `defaultFont` changes through `getScaledDefaultFontView()`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            SwingTree publishes the authoritative default font through a reactive\n            `Viewable<FontUIResource>` so a Look and Feel can be truly dynamic:\n            re-install its per-component `*.font` keys and refresh open windows\n            as soon as the OS, the user or another part of the application\n            flips the system font.\n\n            The view fires for every input that drives the resolved default font —\n            the `UIManager` \"defaultFont\" key in particular — and never delivers\n            a `null` payload.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A reasonable default font in place before SwingTree initializes:","code":["var firstFont = new FontUIResource(new java.awt.Font(\"Dialog\", java.awt.Font.PLAIN, 14))","UIManager.getDefaults().put(\"defaultFont\", firstFont)","SwingTree.initialize()"]},

        {"kind":"and","text":"A view + trace list of every value the property emits:","code":["var trace = []","var fontView = SwingTree.get().getScaledDefaultFontView()","fontView.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))"]},

        {"kind":"expect","text":"The initial value of the view matches the current default font:","code":["fontView.get() === firstFont"]},

        {"kind":"when","text":"We push a new default font through the `UIManager`:","code":["var secondFont = new FontUIResource(new java.awt.Font(\"Serif\", java.awt.Font.BOLD, 28))","UIManager.getDefaults().put(\"defaultFont\", secondFont)"]},

        {"kind":"then","text":"The view subscribers see the new font:","code":["trace == [secondFont]"]},

        {"kind":"when","text":"We push yet another font:","code":["var thirdFont = new FontUIResource(new java.awt.Font(\"SansSerif\", java.awt.Font.ITALIC, 19))","UIManager.getDefaults().put(\"defaultFont\", thirdFont)"]},

        {"kind":"then","text":"Subscribers see that one too:","code":["trace == [secondFont, thirdFont]"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `getScaledDefaultFontView()` view does NOT fire when the same font value is re-installed.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            SwingTree's internal listener fires for *every* PropertyChangeEvent on\n            \"defaultFont\" — even when an application puts the same value back into the\n            `UIManager`. To prevent that low-level noise from leaking into reactive\n            subscribers, the published default-font property only signals when the\n            resolved value actually differs from the previous one (value equality,\n            not identity). That keeps Look and Feel re-installation cheap.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"An initialized SwingTree with a baseline default font:","code":["var baseline = new FontUIResource(new java.awt.Font(\"Dialog\", java.awt.Font.PLAIN, 14))","UIManager.getDefaults().put(\"defaultFont\", baseline)","SwingTree.initialize()"]},

        {"kind":"and","text":"A subscriber that records every emitted font:","code":["var trace = []","var fontView = SwingTree.get().getScaledDefaultFontView()","fontView.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))"]},

        {"kind":"when","text":"We push an *equal* (but not identical) FontUIResource back into the `UIManager`:","code":["var equalCopy = new FontUIResource(new java.awt.Font(\"Dialog\", java.awt.Font.PLAIN, 14))","UIManager.getDefaults().put(\"defaultFont\", equalCopy)"]},

        {"kind":"then","text":"The view subscriber does not fire — the resolved value did not change:","code":["trace == []"]},

        {"kind":"when","text":"We push a genuinely different font:","code":["var bigger = new FontUIResource(new java.awt.Font(\"Dialog\", java.awt.Font.PLAIN, 28))","UIManager.getDefaults().put(\"defaultFont\", bigger)"]},

        {"kind":"then","text":"The subscriber receives exactly one change notification:","code":["trace == [bigger]"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A configured `uiScaleFactor` is folded into the font returned by `getScaledDefaultFont()`.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            SwingTree can arrive at its UI scale factor in two ways: by *deriving* it\n            from the size of the default font, or by having it *dictated* explicitly\n            through the `swingtree.uiScale` system property (equivalently\n            `conf.uiScaleFactor(..)` at initialization).\n\n            When the scale is dictated this way, the raw font sitting in the\n            `UIManager` — or the active Look and Feel's `Label.font` — does **not** yet\n            carry that factor. If `getScaledDefaultFont()` handed that raw font back, a Look\n            and Feel author installing it into `UIDefaults` keys like `Label.font`\n            would get text at the unscaled size while the rest of SwingTree's layout\n            and painting is scaled — text and chrome would visibly disagree.\n\n            So `getScaledDefaultFont()` folds the configured factor into the font it returns,\n            honouring its documented \"already scaled\" contract. What scales is the font\n            *size*: doubling the configured factor doubles the returned size. That\n            relationship is platform independent, even though the absolute pixel size\n            depends on the platform's reference font size (e.g. 15 on a typical Linux\n            desktop, 12 on Windows, 13 on macOS), which is why this test asserts the\n            *ratio* rather than a hard-coded size.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A deliberately small raw font in the `UIManager` — its size must NOT survive verbatim:","code":["var rawFont = new java.awt.Font(\"Monospaced\", java.awt.Font.PLAIN, 9)","UIManager.getDefaults().put(\"defaultFont\", rawFont)"]},

        {"kind":"and","text":"We initialize SwingTree with the scale dictated explicitly as a factor of 2:","code":["SwingTree.initializeUsing(conf -> conf","    .isUiScaleFactorEnabled(true)","    .uiScaleFactor(2f)",")"]},

        {"kind":"and","text":"We capture the resolved default font:","code":["var scaledTwice = SwingTree.get().getScaledDefaultFont()"]},

        {"kind":"expect","text":"It is a `FontUIResource`, so a LAF can drop it straight into `UIDefaults`:","code":["scaledTwice instanceof FontUIResource"]},

        {"kind":"and","text":"The font family is preserved — only the size is touched:","code":["scaledTwice.getFamily() == rawFont.getFamily()"]},

        {"kind":"and","text":"The raw, unscaled size was NOT returned verbatim — the configured factor was applied:","code":["scaledTwice.getSize() != rawFont.getSize()","scaledTwice.getSize() > rawFont.getSize()"]},

        {"kind":"when","text":"We re-initialize with the very same font but a factor of 1, to obtain the unscaled baseline:","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", rawFont)","SwingTree.initializeUsing(conf -> conf","    .isUiScaleFactorEnabled(true)","    .uiScaleFactor(1f)",")","var scaledOnce = SwingTree.get().getScaledDefaultFont()"]},

        {"kind":"then","text":"The returned size scales linearly with the configured factor: twice the factor, twice the size.","code":["scaledTwice.getSize() == 2 * scaledOnce.getSize()"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The `getScaledDefaultFontView()` exposes the scaled default font when a `uiScaleFactor` is configured.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The reactive `getScaledDefaultFontView()` is the hook a dynamic Look and Feel\n            subscribes to so it can re-install its `*.font` keys whenever the\n            authoritative default font changes. It must publish the *same* value that\n            `getScaledDefaultFont()` returns — including the scaling applied for an explicitly\n            configured `uiScaleFactor`. A LAF that trusted the view but received an\n            unscaled font would render text out of step with the rest of the UI.\n\n            This test pins the scale to a factor of 2 and shows that:\n            <ul>\n                <li>the view's current value is the *scaled* font (not the raw\n                    `UIManager` font), and matches `getScaledDefaultFont()`; and</li>\n                <li>when the application swaps the source font in the `UIManager`, the\n                    view re-publishes a value that is still governed by the configured\n                    factor — the new family flows through, but the size stays locked to\n                    the dictated scale.</li>\n            </ul>\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A raw source font installed before SwingTree initializes:","code":["var firstRaw = new java.awt.Font(\"Dialog\", java.awt.Font.PLAIN, 10)","UIManager.getDefaults().put(\"defaultFont\", firstRaw)"]},

        {"kind":"and","text":"We initialize with the scale dictated explicitly as a factor of 2:","code":["SwingTree.initializeUsing(conf -> conf","    .isUiScaleFactorEnabled(true)","    .uiScaleFactor(2f)",")"]},

        {"kind":"and","text":"A view plus a trace list of every value the property emits:","code":["var trace = []","var fontView = SwingTree.get().getScaledDefaultFontView()","fontView.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))"]},

        {"kind":"expect","text":"The view never delivers a `null` payload and its current value is a `FontUIResource`:","code":["fontView.get() instanceof FontUIResource"]},

        {"kind":"and","text":"The view exposes exactly what `getScaledDefaultFont()` resolves — the scaled font, not the raw one:","code":["fontView.get() == SwingTree.get().getScaledDefaultFont()","fontView.get().getSize() != firstRaw.getSize()","fontView.get().getSize() > firstRaw.getSize()"]},

        {"kind":"when","text":"The application swaps the source font in the `UIManager` for a different family:","code":["var secondRaw = new java.awt.Font(\"Serif\", java.awt.Font.BOLD, 40)","UIManager.getDefaults().put(\"defaultFont\", secondRaw)"]},

        {"kind":"then","text":"The view fires once, the new family flows through, but the size stays governed by the factor:","code":["trace.size() == 1","trace.last().getFamily() == secondRaw.getFamily()","trace.last().getSize()   == SwingTree.get().getScaledDefaultFont().getSize()","trace.last().getSize()   != secondRaw.getSize()"]},

        {"kind":"cleanup","text":"","code":["SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A custom Look and Feel can fetch `getScaledDefaultFontView()` from its `initialize()` hook without NPE.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The recommended pattern for a SwingTree-aware Look and Feel is to\n            subscribe to `SwingTree.get().getScaledDefaultFontView()` from inside the\n            LAF's own `initialize()` hook — so that runtime DPI / system-font\n            changes flow into the UI without a restart.\n\n            That timing is tricky: during `LookAndFeel.initialize()`,\n            `UIManager.getLookAndFeelDefaults()` can still be `null` because the\n            install has not yet committed the new defaults table. SwingTree\n            therefore guards each `addPropertyChangeListener` call and re-attempts\n            any skipped attach on the next `\"lookAndFeel\"` PropertyChangeEvent\n            (which is guaranteed to fire at the end of `UIManager.setLookAndFeel(..)`).\n\n            This test reproduces the call order and verifies both halves of the\n            contract — no NPE during init, and the listener still propagates\n            `UIManager` font changes after the install completes.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A trace list of fonts the view emits after install:","code":["var trace = []","var capturedView = new AtomicReference<Viewable<FontUIResource>>()"]},

        {"kind":"and","text":"A minimal custom Look and Feel that subscribes from inside `initialize()`:","code":["var laf = new BasicLookAndFeel() {","    @Override String getName()                 { \"SelfHealTest\" }","    @Override String getID()                   { \"SelfHealTest\" }","    @Override String getDescription()          { \"self-heal test LAF\" }","    @Override boolean isNativeLookAndFeel()    { false }","    @Override boolean isSupportedLookAndFeel() { true  }","    @Override void initialize() {","        super.initialize()","        // This is exactly the call pattern that used to throw an","        // NPE because UIManager.getLookAndFeelDefaults() was null.","        var view = SwingTree.get().getScaledDefaultFontView()","        view.onChange(From.ALL, it -> trace.add(it.currentValue().orElseThrow()))","        capturedView.set(view)","    }","}"]},

        {"kind":"when","text":"We install the LAF — the bootstrap MUST NOT throw:","code":["UIManager.setLookAndFeel(laf)"]},

        {"kind":"then","text":"No exception was raised and the view was obtained successfully:","code":["noExceptionThrown()","capturedView.get() != null"]},

        {"kind":"when","text":"After the install completes, we push a fresh \"defaultFont\" through the `UIManager`:","code":["var afterInstall = new FontUIResource(new java.awt.Font(\"Serif\", java.awt.Font.BOLD, 33))","UIManager.getDefaults().put(\"defaultFont\", afterInstall)"]},

        {"kind":"then","text":"The listener that was attached during initialize self-healed: the view fires:","code":["trace.contains(afterInstall)"]},

        {"kind":"cleanup","text":"","code":["UIManager.setLookAndFeel(UIManager.getCrossPlatformLookAndFeelClassName())","SwingTree.clear()","UIManager.getDefaults().put(\"defaultFont\", null)"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"You can configure the keystroke for summoning the dev tool in the library context!",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The dev tool is a powerful inspector tool for inspecting the internal state of SwingTree.\n            You can summon it through a custom keystroke which you can configure when\n            initializing the library context. \n\n            By default, the keystroke is \"ctrl shift I\", exactly the same as in modern browsers, \n            but you can change it to whatever you like. \n            In this test we change it to \"ctrl shift alt D\" and verify that the new keystroke \n            is applied in the library context.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"We initialize `SwingTree` with a custom dev tool keystroke:","code":["SwingTree.initializeUsing(conf -> conf","    .devToolKeyStrokeShortcut(\"ctrl shift alt D\")",")"]},

        {"kind":"expect","text":"The dev tool keystroke is the one we specified:","code":["SwingTree.get().getDevToolKeyStrokeShortcut().toString() == \"ctrl shift alt D\""]},

        {"kind":"cleanup","text":"Reset the library context!","code":["SwingTree.clear()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
