{
  "className":"swingtree.Parent_Filter_Spec",
  "title":"Filtering the Parent Behind a Component",
  "narrative":"A component can be told to show its own parent through itself, put through a filter on the\n    way there. That is what `parentFilter(..)` does, and it is how a pane of frosted glass is\n    made: the pane itself is barely more than a tint, and everything which makes it read as\n    glass comes from the window behind it arriving blurred.\n\n    ```\n    UI.panel().withStyle( it -> it.parentFilter( f -> f.blur(6) ) )\n    ```\n\n    A blur is only one of the things the parent can be put through on the way. `f.scale(..)`\n    magnifies it, `f.offset(..)` slides it, `f.kernel(..)` runs an arbitrary convolution over\n    it, and `f.area(..)` says which part of the component the result is allowed to show in.\n\n    What all of them have in common, and what the scenarios below are about, is that the\n    picture a pane shows is a picture **of its parent**. Its siblings are not part of what it\n    is looking at. So a second pane which happens to filter the same parent may not change\n    what the first one shows - not by a single pixel, and no matter how many of them there\n    are. A pane which grows blurrier the more neighbours it has is showing something which is\n    not there.\n\n    Two notes on how these scenarios are built, both of which matter for reading them.\n\n    The parent is painted in fine-grained noise rather than in a colour or a gradient. Blurring\n    something smooth gives back very nearly the same smooth thing, so a smooth parent would\n    hide exactly the defect being looked for. Noise is the opposite: it is all detail, and\n    losing it twice over is plain to see.\n\n    The panes are compared against a *reference rendering of the very same layout*, in which\n    only the pane under test filters. Switching a filter off does not move anything, so the\n    pane occupies the same rectangle in both renderings and the two can be compared pixel for\n    pixel.\n\n    The last scenario is about a different way the same picture can go wrong. Swing repaints as\n    little as it can get away with: move the pointer onto a button and the only thing redrawn\n    is that button's rectangle, expressed as a clip on the graphics everything is painted\n    through. A filter still has to read the whole of the parent underneath its pane, because a\n    blur gathers colour from further away than the pixel it is computing. So what a pane shows\n    inside the repainted rectangle has to be the same whether that rectangle was the whole\n    window or a corner of one button.",
  "subjects":["swingtree.UI"],
  "statistics":{
    "runs":"34",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"Use `parentFilter(..)` on two panes over one parent, and neither changes what the other shows.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Two panes sit side by side over a noisy parent, and both are asked to blur it.\n            Each of them is then compared against a rendering in which it is the *only* pane\n            filtering - which is to say, against what it would look like if its neighbour were\n            not there at all.\n\n            Both panes have to pass. A pane is looking at the parent, and the parent does not\n            change when a sibling is told to blur it, so there is no reading of `parentFilter`\n            under which the answer depends on the neighbour.\n\n            Testing both of them rather than one is what makes this scenario complete. Swing\n            paints a container's children in reverse order of how they were added, so exactly\n            one of these two panes is painted after the other has had its turn - and a defect\n            in which a pane damages what it filtered would only ever show up in that one.\n            Which of the two that is, is not something the scenario needs to know.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, holding two equally sized panes side by\n            side. A pane has no background of its own, so everything it shows arrives through\n            the filter - and whether it filters at all is the one thing which varies here.\n            Everything else stays put, so switching a filter off does not move a pane.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var leftPane = leftFilters","            ? UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT)","                    .parentFilter( f -> f.blur(6) ) )","            : UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT) )","    var rightPane = rightFilters","            ? UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT)","                    .parentFilter( f -> f.blur(6) ) )","            : UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT) )","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", leftPane)","            .add(\"grow\", rightPane)","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"\n            A way of counting how many pixels of one half of a rendering differ from the same\n            half of another. A channel has to be off by more than 8 out of 255 to count, which\n            is far below what the defect this is looking for is worth: a pane which blurred an\n            already blurred parent loses detail the second time round, and here that costs it\n            up to 28 out of 255. It is also far above anything rounding can produce, since the\n            two renderings being compared come out of the same code on the same machine.\n        ","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes are asked to blur the parent behind them:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each of them is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"and","text":"The three renderings are all of the same 240 by 160 parent, so the panes line up:","code":["var leftHalf  = [0,   120]","var rightHalf = [120, 240]"]},

        {"kind":"then","text":"\n            The filter is doing something in the first place. A pane which quietly stopped\n            filtering would pass every other assertion here, so this one is what rules that\n            out: the blurred left pane is compared against a rendering of the same layout in\n            which nothing filters at all, and the two are nothing like each other.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), leftHalf[0], leftHalf[1]) > 15000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, leftHalf[0], leftHalf[1]) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, rightHalf[0], rightHalf[1]) == 0"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing however many siblings also filter. (one neighbour filters) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a stable picture of its parent, and a sibling which also looks at that\n            parent cannot change it. Once that is true for a single neighbour, the question is\n            whether it holds as the neighbours *add up*.\n\n            Four panes sit in a row over the same noisy parent. The first of them always blurs\n            it, and the other three are switched between filtering and not, one row of the\n            table at a time. Whatever the other three are doing, the first pane is looking at\n            the same parent through the same blur, so it has to come out identical every time.\n\n            A defect in which filtering damages what was filtered would not merely show up\n            here, it would *accumulate*: each pane which filtered before this one would have\n            left the parent that bit more blurred, and the pane would end up showing the parent\n            blurred as many times over as it has neighbours.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, this time under four equally sized panes.\n            The first pane is the one under test and always filters; the other three filter or\n            not according to the row of the table below.\n        ","code":["var fourPanesWhere = { boolean second, boolean third, boolean fourth ->","    var paneFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) )","        }","    var paneNotFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", paneFiltering())","            .add(\"grow\", second ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", third  ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", fourth ? paneFiltering() : paneNotFiltering())","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over just the first of the four panes.","code":["var pixelsDifferingInTheFirstPane = { BufferedImage a, BufferedImage b ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = 0; x < 60; x++ ) // the first of four panes across 240 pixels","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The pane under test, rendered with all three of its neighbours leaving the parent alone.","code":["var undisturbed = fourPanesWhere(false, false, false)"]},

        {"kind":"when","text":"The three neighbours are set to filter as the row of the table says:","code":["var candidate = fourPanesWhere(secondFilters, thirdFilters, fourthFilters)"]},

        {"kind":"then","text":"The pane under test is unchanged.","code":["pixelsDifferingInTheFirstPane(undisturbed, candidate) == 0"]},

        {"kind":"where","text":"The three neighbours take every combination of filtering and not.","code":{"description":["'one neighbour filters'","'a further neighbour does'","'all three neighbours do'","'only the furthest one does'"],"secondFilters":["true","true","true","false"],"thirdFilters":["false","true","true","false"],"fourthFilters":["false","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing however many siblings also filter. (a further neighbour does) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a stable picture of its parent, and a sibling which also looks at that\n            parent cannot change it. Once that is true for a single neighbour, the question is\n            whether it holds as the neighbours *add up*.\n\n            Four panes sit in a row over the same noisy parent. The first of them always blurs\n            it, and the other three are switched between filtering and not, one row of the\n            table at a time. Whatever the other three are doing, the first pane is looking at\n            the same parent through the same blur, so it has to come out identical every time.\n\n            A defect in which filtering damages what was filtered would not merely show up\n            here, it would *accumulate*: each pane which filtered before this one would have\n            left the parent that bit more blurred, and the pane would end up showing the parent\n            blurred as many times over as it has neighbours.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, this time under four equally sized panes.\n            The first pane is the one under test and always filters; the other three filter or\n            not according to the row of the table below.\n        ","code":["var fourPanesWhere = { boolean second, boolean third, boolean fourth ->","    var paneFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) )","        }","    var paneNotFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", paneFiltering())","            .add(\"grow\", second ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", third  ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", fourth ? paneFiltering() : paneNotFiltering())","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over just the first of the four panes.","code":["var pixelsDifferingInTheFirstPane = { BufferedImage a, BufferedImage b ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = 0; x < 60; x++ ) // the first of four panes across 240 pixels","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The pane under test, rendered with all three of its neighbours leaving the parent alone.","code":["var undisturbed = fourPanesWhere(false, false, false)"]},

        {"kind":"when","text":"The three neighbours are set to filter as the row of the table says:","code":["var candidate = fourPanesWhere(secondFilters, thirdFilters, fourthFilters)"]},

        {"kind":"then","text":"The pane under test is unchanged.","code":["pixelsDifferingInTheFirstPane(undisturbed, candidate) == 0"]},

        {"kind":"where","text":"The three neighbours take every combination of filtering and not.","code":{"description":["'one neighbour filters'","'a further neighbour does'","'all three neighbours do'","'only the furthest one does'"],"secondFilters":["true","true","true","false"],"thirdFilters":["false","true","true","false"],"fourthFilters":["false","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing however many siblings also filter. (all three neighbours do) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a stable picture of its parent, and a sibling which also looks at that\n            parent cannot change it. Once that is true for a single neighbour, the question is\n            whether it holds as the neighbours *add up*.\n\n            Four panes sit in a row over the same noisy parent. The first of them always blurs\n            it, and the other three are switched between filtering and not, one row of the\n            table at a time. Whatever the other three are doing, the first pane is looking at\n            the same parent through the same blur, so it has to come out identical every time.\n\n            A defect in which filtering damages what was filtered would not merely show up\n            here, it would *accumulate*: each pane which filtered before this one would have\n            left the parent that bit more blurred, and the pane would end up showing the parent\n            blurred as many times over as it has neighbours.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, this time under four equally sized panes.\n            The first pane is the one under test and always filters; the other three filter or\n            not according to the row of the table below.\n        ","code":["var fourPanesWhere = { boolean second, boolean third, boolean fourth ->","    var paneFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) )","        }","    var paneNotFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", paneFiltering())","            .add(\"grow\", second ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", third  ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", fourth ? paneFiltering() : paneNotFiltering())","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over just the first of the four panes.","code":["var pixelsDifferingInTheFirstPane = { BufferedImage a, BufferedImage b ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = 0; x < 60; x++ ) // the first of four panes across 240 pixels","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The pane under test, rendered with all three of its neighbours leaving the parent alone.","code":["var undisturbed = fourPanesWhere(false, false, false)"]},

        {"kind":"when","text":"The three neighbours are set to filter as the row of the table says:","code":["var candidate = fourPanesWhere(secondFilters, thirdFilters, fourthFilters)"]},

        {"kind":"then","text":"The pane under test is unchanged.","code":["pixelsDifferingInTheFirstPane(undisturbed, candidate) == 0"]},

        {"kind":"where","text":"The three neighbours take every combination of filtering and not.","code":{"description":["'one neighbour filters'","'a further neighbour does'","'all three neighbours do'","'only the furthest one does'"],"secondFilters":["true","true","true","false"],"thirdFilters":["false","true","true","false"],"fourthFilters":["false","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing however many siblings also filter. (only the furthest one does) [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a stable picture of its parent, and a sibling which also looks at that\n            parent cannot change it. Once that is true for a single neighbour, the question is\n            whether it holds as the neighbours *add up*.\n\n            Four panes sit in a row over the same noisy parent. The first of them always blurs\n            it, and the other three are switched between filtering and not, one row of the\n            table at a time. Whatever the other three are doing, the first pane is looking at\n            the same parent through the same blur, so it has to come out identical every time.\n\n            A defect in which filtering damages what was filtered would not merely show up\n            here, it would *accumulate*: each pane which filtered before this one would have\n            left the parent that bit more blurred, and the pane would end up showing the parent\n            blurred as many times over as it has neighbours.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, this time under four equally sized panes.\n            The first pane is the one under test and always filters; the other three filter or\n            not according to the row of the table below.\n        ","code":["var fourPanesWhere = { boolean second, boolean third, boolean fourth ->","    var paneFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) )","        }","    var paneNotFiltering = { ->","            UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", paneFiltering())","            .add(\"grow\", second ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", third  ? paneFiltering() : paneNotFiltering())","            .add(\"grow\", fourth ? paneFiltering() : paneNotFiltering())","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over just the first of the four panes.","code":["var pixelsDifferingInTheFirstPane = { BufferedImage a, BufferedImage b ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = 0; x < 60; x++ ) // the first of four panes across 240 pixels","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The pane under test, rendered with all three of its neighbours leaving the parent alone.","code":["var undisturbed = fourPanesWhere(false, false, false)"]},

        {"kind":"when","text":"The three neighbours are set to filter as the row of the table says:","code":["var candidate = fourPanesWhere(secondFilters, thirdFilters, fourthFilters)"]},

        {"kind":"then","text":"The pane under test is unchanged.","code":["pixelsDifferingInTheFirstPane(undisturbed, candidate) == 0"]},

        {"kind":"where","text":"The three neighbours take every combination of filtering and not.","code":{"description":["'one neighbour filters'","'a further neighbour does'","'all three neighbours do'","'only the furthest one does'"],"secondFilters":["true","true","true","false"],"thirdFilters":["false","true","true","false"],"fourthFilters":["false","false","true","true"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing when only part of the window is repainted. (a rectangle inside the pane) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing hands a component a graphics whose clip says which part of it is worth\n            redrawing, and redraws nothing else. Moving the pointer onto one button in a window\n            full of them sets that clip to the button, and every ancestor it is painted through\n            gets the same narrow clip.\n\n            The clip says what may be **drawn**. It does not say what may be **read**. A pane\n            blurring its parent needs the parent for a good distance around every pixel it\n            produces, including the parent from outside the rectangle being redrawn - so a\n            filter which only ever looks at the freshly repainted strip is looking at a parent\n            with a hole around it, and paints the hole.\n\n            The requirement is that a pane renders identically whatever rectangle Swing asks\n            for: a partial repaint is an optimisation, not a licence to change the picture.\n            The rows below repaint through three different rectangles: one lying wholly inside\n            the pane, one straddling its edge, and - as a control which must pass either way -\n            one covering the whole component, which is simply the full repaint again.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with a single pane over it blurring what is\n            behind it. The parent is inset by 40 pixels all round, so the pane sits well away\n            from the parent's own edges and nothing about this scenario depends on what a filter\n            does when it runs out of parent to read.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"\n            A way of painting that parent through a clip of our choosing, which is the one thing\n            `Utility.renderSingleComponent` cannot be asked for: it always paints the component\n            whole, and a partial repaint is exactly what is under test here.\n        ","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only the rectangle of this row is repainted:","code":["var partialRepaint = paintedThroughClip(clipX, clipY, clipWidth, clipHeight)"]},

        {"kind":"then","text":"Inside that rectangle, the two are the same picture.","code":["pixelsDifferingInside(fullRepaint, partialRepaint,","                      clipX, clipY, clipWidth, clipHeight) == 0"]},

        {"kind":"where","text":"The repainted rectangle sits inside the pane, across its edge, and around everything.","code":{"description":["'a rectangle inside the pane'","'a rectangle across its edge'","'the whole component (control)'"],"clipX":["100","30","0"],"clipY":["70","60","0"],"clipWidth":["40","60","240"],"clipHeight":["30","40","160"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing when only part of the window is repainted. (a rectangle across its edge) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing hands a component a graphics whose clip says which part of it is worth\n            redrawing, and redraws nothing else. Moving the pointer onto one button in a window\n            full of them sets that clip to the button, and every ancestor it is painted through\n            gets the same narrow clip.\n\n            The clip says what may be **drawn**. It does not say what may be **read**. A pane\n            blurring its parent needs the parent for a good distance around every pixel it\n            produces, including the parent from outside the rectangle being redrawn - so a\n            filter which only ever looks at the freshly repainted strip is looking at a parent\n            with a hole around it, and paints the hole.\n\n            The requirement is that a pane renders identically whatever rectangle Swing asks\n            for: a partial repaint is an optimisation, not a licence to change the picture.\n            The rows below repaint through three different rectangles: one lying wholly inside\n            the pane, one straddling its edge, and - as a control which must pass either way -\n            one covering the whole component, which is simply the full repaint again.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with a single pane over it blurring what is\n            behind it. The parent is inset by 40 pixels all round, so the pane sits well away\n            from the parent's own edges and nothing about this scenario depends on what a filter\n            does when it runs out of parent to read.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"\n            A way of painting that parent through a clip of our choosing, which is the one thing\n            `Utility.renderSingleComponent` cannot be asked for: it always paints the component\n            whole, and a partial repaint is exactly what is under test here.\n        ","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only the rectangle of this row is repainted:","code":["var partialRepaint = paintedThroughClip(clipX, clipY, clipWidth, clipHeight)"]},

        {"kind":"then","text":"Inside that rectangle, the two are the same picture.","code":["pixelsDifferingInside(fullRepaint, partialRepaint,","                      clipX, clipY, clipWidth, clipHeight) == 0"]},

        {"kind":"where","text":"The repainted rectangle sits inside the pane, across its edge, and around everything.","code":{"description":["'a rectangle inside the pane'","'a rectangle across its edge'","'the whole component (control)'"],"clipX":["100","30","0"],"clipY":["70","60","0"],"clipWidth":["40","60","240"],"clipHeight":["30","40","160"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane using `parentFilter(..)` shows the same thing when only part of the window is repainted. (the whole component (control)) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing hands a component a graphics whose clip says which part of it is worth\n            redrawing, and redraws nothing else. Moving the pointer onto one button in a window\n            full of them sets that clip to the button, and every ancestor it is painted through\n            gets the same narrow clip.\n\n            The clip says what may be **drawn**. It does not say what may be **read**. A pane\n            blurring its parent needs the parent for a good distance around every pixel it\n            produces, including the parent from outside the rectangle being redrawn - so a\n            filter which only ever looks at the freshly repainted strip is looking at a parent\n            with a hole around it, and paints the hole.\n\n            The requirement is that a pane renders identically whatever rectangle Swing asks\n            for: a partial repaint is an optimisation, not a licence to change the picture.\n            The rows below repaint through three different rectangles: one lying wholly inside\n            the pane, one straddling its edge, and - as a control which must pass either way -\n            one covering the whole component, which is simply the full repaint again.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with a single pane over it blurring what is\n            behind it. The parent is inset by 40 pixels all round, so the pane sits well away\n            from the parent's own edges and nothing about this scenario depends on what a filter\n            does when it runs out of parent to read.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"\n            A way of painting that parent through a clip of our choosing, which is the one thing\n            `Utility.renderSingleComponent` cannot be asked for: it always paints the component\n            whole, and a partial repaint is exactly what is under test here.\n        ","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only the rectangle of this row is repainted:","code":["var partialRepaint = paintedThroughClip(clipX, clipY, clipWidth, clipHeight)"]},

        {"kind":"then","text":"Inside that rectangle, the two are the same picture.","code":["pixelsDifferingInside(fullRepaint, partialRepaint,","                      clipX, clipY, clipWidth, clipHeight) == 0"]},

        {"kind":"where","text":"The repainted rectangle sits inside the pane, across its edge, and around everything.","code":{"description":["'a rectangle inside the pane'","'a rectangle across its edge'","'the whole component (control)'"],"clipX":["100","30","0"],"clipY":["70","60","0"],"clipWidth":["40","60","240"],"clipHeight":["30","40","160"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Repainting part of a window over what is already on it leaves a `parentFilter(..)` pane alone. (a spot on the left pane) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A window is already on screen, and something small happens on it - the pointer\n            arrives on a button, a caret blinks, a progress bar ticks. Swing does not redraw the\n            window; it redraws that rectangle, over what is already there.\n\n            Nothing about the window has changed here, so redrawing any rectangle of it has to\n            leave it exactly as it was. That makes every pixel of the window an assertion,\n            rather than only the ones inside the rectangle, and so it catches two things at\n            once: a pane which paints the wrong thing inside the rectangle, and a pane which\n            paints anything at all outside it.\n\n            When this fails, what the user sees is a rectangular stain around whatever they\n            touched, which stays until something forces the whole window to be redrawn.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with two panes over it blurring what is\n            behind them - two, because a single pane cannot show whether the repaint of one\n            damages another.\n        ","code":["var parent =","        UI.panel(\"fill, ins 20, gap 20\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"The window as it already sits on screen, which is simply a full repaint of it.","code":["var onScreen = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"when","text":"\n            Swing redraws one rectangle of it, the way it does when something small happens:\n            over what is already there, through a clip which says how far the redraw reaches.\n        ","code":["var afterRedraw = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    graphics.drawImage(onScreen, 0, 0, null)","    graphics.setClip(clipX, clipY, clipWidth, clipHeight)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"and","text":"Every pixel of the window is compared, not only the ones which were redrawn.","code":["int pixelsChanged = 0","for ( int y = 0; y < 160; y++ )","    for ( int x = 0; x < 240; x++ )","        for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","            if ( Math.abs(","                    ((onScreen.getRGB(x, y) >> channelShift) & 0xff) -","                    ((afterRedraw.getRGB(x, y) >> channelShift) & 0xff)","                 ) > 8 ) {","                pixelsChanged++","                break","            }"]},

        {"kind":"then","text":"Nothing changed, because nothing had changed.","code":["pixelsChanged == 0"]},

        {"kind":"where","text":"The redrawn rectangle lands on one pane, on both of them, and between them.","code":{"description":["'a spot on the left pane'","'a spot on the right pane'","'a strip across both'","'the gap between them'"],"clipX":["60","160","20","110"],"clipY":["70","70","75","20"],"clipWidth":["20","20","200","20"],"clipHeight":["20","20","10","120"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Repainting part of a window over what is already on it leaves a `parentFilter(..)` pane alone. (a spot on the right pane) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A window is already on screen, and something small happens on it - the pointer\n            arrives on a button, a caret blinks, a progress bar ticks. Swing does not redraw the\n            window; it redraws that rectangle, over what is already there.\n\n            Nothing about the window has changed here, so redrawing any rectangle of it has to\n            leave it exactly as it was. That makes every pixel of the window an assertion,\n            rather than only the ones inside the rectangle, and so it catches two things at\n            once: a pane which paints the wrong thing inside the rectangle, and a pane which\n            paints anything at all outside it.\n\n            When this fails, what the user sees is a rectangular stain around whatever they\n            touched, which stays until something forces the whole window to be redrawn.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with two panes over it blurring what is\n            behind them - two, because a single pane cannot show whether the repaint of one\n            damages another.\n        ","code":["var parent =","        UI.panel(\"fill, ins 20, gap 20\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"The window as it already sits on screen, which is simply a full repaint of it.","code":["var onScreen = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"when","text":"\n            Swing redraws one rectangle of it, the way it does when something small happens:\n            over what is already there, through a clip which says how far the redraw reaches.\n        ","code":["var afterRedraw = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    graphics.drawImage(onScreen, 0, 0, null)","    graphics.setClip(clipX, clipY, clipWidth, clipHeight)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"and","text":"Every pixel of the window is compared, not only the ones which were redrawn.","code":["int pixelsChanged = 0","for ( int y = 0; y < 160; y++ )","    for ( int x = 0; x < 240; x++ )","        for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","            if ( Math.abs(","                    ((onScreen.getRGB(x, y) >> channelShift) & 0xff) -","                    ((afterRedraw.getRGB(x, y) >> channelShift) & 0xff)","                 ) > 8 ) {","                pixelsChanged++","                break","            }"]},

        {"kind":"then","text":"Nothing changed, because nothing had changed.","code":["pixelsChanged == 0"]},

        {"kind":"where","text":"The redrawn rectangle lands on one pane, on both of them, and between them.","code":{"description":["'a spot on the left pane'","'a spot on the right pane'","'a strip across both'","'the gap between them'"],"clipX":["60","160","20","110"],"clipY":["70","70","75","20"],"clipWidth":["20","20","200","20"],"clipHeight":["20","20","10","120"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Repainting part of a window over what is already on it leaves a `parentFilter(..)` pane alone. (a strip across both) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A window is already on screen, and something small happens on it - the pointer\n            arrives on a button, a caret blinks, a progress bar ticks. Swing does not redraw the\n            window; it redraws that rectangle, over what is already there.\n\n            Nothing about the window has changed here, so redrawing any rectangle of it has to\n            leave it exactly as it was. That makes every pixel of the window an assertion,\n            rather than only the ones inside the rectangle, and so it catches two things at\n            once: a pane which paints the wrong thing inside the rectangle, and a pane which\n            paints anything at all outside it.\n\n            When this fails, what the user sees is a rectangular stain around whatever they\n            touched, which stays until something forces the whole window to be redrawn.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with two panes over it blurring what is\n            behind them - two, because a single pane cannot show whether the repaint of one\n            damages another.\n        ","code":["var parent =","        UI.panel(\"fill, ins 20, gap 20\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"The window as it already sits on screen, which is simply a full repaint of it.","code":["var onScreen = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"when","text":"\n            Swing redraws one rectangle of it, the way it does when something small happens:\n            over what is already there, through a clip which says how far the redraw reaches.\n        ","code":["var afterRedraw = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    graphics.drawImage(onScreen, 0, 0, null)","    graphics.setClip(clipX, clipY, clipWidth, clipHeight)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"and","text":"Every pixel of the window is compared, not only the ones which were redrawn.","code":["int pixelsChanged = 0","for ( int y = 0; y < 160; y++ )","    for ( int x = 0; x < 240; x++ )","        for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","            if ( Math.abs(","                    ((onScreen.getRGB(x, y) >> channelShift) & 0xff) -","                    ((afterRedraw.getRGB(x, y) >> channelShift) & 0xff)","                 ) > 8 ) {","                pixelsChanged++","                break","            }"]},

        {"kind":"then","text":"Nothing changed, because nothing had changed.","code":["pixelsChanged == 0"]},

        {"kind":"where","text":"The redrawn rectangle lands on one pane, on both of them, and between them.","code":{"description":["'a spot on the left pane'","'a spot on the right pane'","'a strip across both'","'the gap between them'"],"clipX":["60","160","20","110"],"clipY":["70","70","75","20"],"clipWidth":["20","20","200","20"],"clipHeight":["20","20","10","120"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Repainting part of a window over what is already on it leaves a `parentFilter(..)` pane alone. (the gap between them) [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A window is already on screen, and something small happens on it - the pointer\n            arrives on a button, a caret blinks, a progress bar ticks. Swing does not redraw the\n            window; it redraws that rectangle, over what is already there.\n\n            Nothing about the window has changed here, so redrawing any rectangle of it has to\n            leave it exactly as it was. That makes every pixel of the window an assertion,\n            rather than only the ones inside the rectangle, and so it catches two things at\n            once: a pane which paints the wrong thing inside the rectangle, and a pane which\n            paints anything at all outside it.\n\n            When this fails, what the user sees is a rectangular stain around whatever they\n            touched, which stays until something forces the whole window to be redrawn.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, with two panes over it blurring what is\n            behind them - two, because a single pane cannot show whether the repaint of one\n            damages another.\n        ","code":["var parent =","        UI.panel(\"fill, ins 20, gap 20\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(6) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"The window as it already sits on screen, which is simply a full repaint of it.","code":["var onScreen = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"when","text":"\n            Swing redraws one rectangle of it, the way it does when something small happens:\n            over what is already there, through a clip which says how far the redraw reaches.\n        ","code":["var afterRedraw = UI.runAndGet(() -> {","    var image = Utility.createDeterministicImage(240, 160)","    var graphics = Utility.createDeterministicGraphics(image)","    graphics.drawImage(onScreen, 0, 0, null)","    graphics.setClip(clipX, clipY, clipWidth, clipHeight)","    Utility.paintWithoutWindow(parent, graphics)","    graphics.dispose()","    return image","})"]},

        {"kind":"and","text":"Every pixel of the window is compared, not only the ones which were redrawn.","code":["int pixelsChanged = 0","for ( int y = 0; y < 160; y++ )","    for ( int x = 0; x < 240; x++ )","        for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","            if ( Math.abs(","                    ((onScreen.getRGB(x, y) >> channelShift) & 0xff) -","                    ((afterRedraw.getRGB(x, y) >> channelShift) & 0xff)","                 ) > 8 ) {","                pixelsChanged++","                break","            }"]},

        {"kind":"then","text":"Nothing changed, because nothing had changed.","code":["pixelsChanged == 0"]},

        {"kind":"where","text":"The redrawn rectangle lands on one pane, on both of them, and between them.","code":{"description":["'a spot on the left pane'","'a spot on the right pane'","'a strip across both'","'the gap between them'"],"clipX":["60","160","20","110"],"clipY":["70","70","75","20"],"clipWidth":["20","20","200","20"],"clipHeight":["20","20","10","120"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a narrow blur) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a blur wide enough to be shrunk) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a blur wider than the shrinking) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a convolution kernel of our own) [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a kernel and a wide blur) [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a wide blur and an offset) [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a wide blur and a scale) [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane keeps what it shows however `parentFilter(..)` is configured, whoever else filters. (a wide blur inside the interior) [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A pane shows a picture of its parent, and no sibling can change it. A blur is only\n            the most familiar way to configure that picture; the rest of the filtering API has\n            to be trusted in the same way.\n\n            `parentFilter(..)` can also slide the parent with `offset(..)`, magnify it with\n            `scale(..)`, run it through a convolution matrix of one's own with `kernel(..)`, and\n            confine the result to one area of the component with `area(..)` - and any of those\n            may be combined with a blur. Each is a different route through the filtering code,\n            so the guarantee has to be established for each of them separately: what a pane\n            shows is a picture of its parent, and a sibling looking at the same parent cannot\n            change it.\n\n            The blur radii in the table are chosen to land on either side of the point at which\n            a wide blur stops being convolved pixel by pixel. Beyond a certain radius the parent\n            is shrunk before it is blurred and stretched back out afterwards, because a blur\n            that wide throws away the detail a smaller raster could not have held anyway. That\n            is an entirely different route through the same method, and a radius from each side\n            of the threshold is what keeps both of them covered.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            Two panes side by side over a noisy parent, except that\n            what the panes are asked to do is now whatever the row of the table says.\n        ","code":["var twoPanesWhere = { boolean leftFilters, boolean rightFilters ->","    var pane = { boolean filters ->","            filters","                ? UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT)","                        .parentFilter( f -> filter(f) ) )","                : UI.panel().withStyle( it -> it","                        .backgroundColor(UI.Color.TRANSPARENT) )","        }","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", pane(leftFilters))","            .add(\"grow\", pane(rightFilters))","            .get(JPanel)","    parent.setSize(240, 160)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A count of pixels differing between two renderings, over a half of the rendering.","code":["var pixelsDifferingBetween = { BufferedImage a, BufferedImage b, int fromX, int toX ->","    int count = 0","    for ( int y = 0; y < a.height; y++ )","        for ( int x = fromX; x < toX; x++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(x, y) >> channelShift) & 0xff) -","                        ((b.getRGB(x, y) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"Both panes filter the parent as this row says:","code":["var bothFiltering = twoPanesWhere(true, true)"]},

        {"kind":"and","text":"And each is rendered again as the only pane filtering, to be compared against:","code":["var onlyTheLeftFiltering  = twoPanesWhere(true, false)","var onlyTheRightFiltering = twoPanesWhere(false, true)"]},

        {"kind":"then","text":"\n            This configuration does something in the first place. Without this, a filter which\n            silently did nothing at all would satisfy every other assertion here.\n        ","code":["pixelsDifferingBetween(onlyTheLeftFiltering, twoPanesWhere(false, false), 0, 120) > 1000"]},

        {"kind":"and","text":"The left pane shows exactly what it shows when the right one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheLeftFiltering, 0, 120) == 0"]},

        {"kind":"and","text":"And the right pane shows exactly what it shows when the left one does not filter.","code":["pixelsDifferingBetween(bothFiltering, onlyTheRightFiltering, 120, 240) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a narrow blur) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a blur wide enough to be shrunk) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a blur wider than the shrinking) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a convolution kernel of our own) [3]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a kernel and a wide blur) [4]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a wide blur and an offset) [5]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a wide blur and a scale) [6]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A pane shows the same thing under a partial repaint however `parentFilter(..)` is configured. (a wide blur inside the interior) [7]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Swing repaints as little as it can: when only part of a window is touched, only that\n            rectangle is redrawn, as a clip on the graphics. A pane using `parentFilter(..)`\n            has to show the same picture there as it would in a full repaint - and that has to\n            hold for every way the filter is configured, not just for a blur.\n\n            It is a sharper question for a wide blur than for a narrow one, because of *how* a\n            wide blur is computed. The parent is shrunk before it is convolved, and the region\n            which is shrunk is the part of the parent the pane needs - which is smaller when\n            Swing asks for a smaller rectangle to be redrawn. So the pixels a filter samples\n            the parent at are decided by something which changes from one repaint to the next,\n            and a filter which let that reach its output would give a pane which shifts, very\n            slightly, every time the pointer passes over it.\n\n            The requirement is plain: whatever rectangle Swing asks for, and however the\n            filter is configured, the picture in that rectangle has to match what a full\n            repaint puts there. Every row below is a different route through the filtering\n            code, and a defect hiding in any one of those routes is a defect this catches.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A parent painted in fine-grained noise, inset by 40 pixels all round, with a single\n            pane over it filtering as this row says.\n        ","code":["var parent =","        UI.panel(\"fill, ins 40\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"grow\", UI.panel().withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> filter(f) ) ))","        .get(JPanel)","parent.setSize(240, 160)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(240, 160)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"and","text":"The whole component painted in one go, which is what a partial repaint has to agree with.","code":["var fullRepaint = paintedThroughClip(0, 0, 240, 160)"]},

        {"kind":"when","text":"Only a rectangle well inside the pane is repainted:","code":["var insideThePane = paintedThroughClip(100, 70, 40, 30)"]},

        {"kind":"and","text":"And then one straddling the edge of the pane:","code":["var acrossTheEdge = paintedThroughClip(30, 60, 60, 40)"]},

        {"kind":"then","text":"Inside either rectangle, what the pane shows is what the full repaint showed.","code":["pixelsDifferingInside(fullRepaint, insideThePane, 100, 70, 40, 30) == 0","pixelsDifferingInside(fullRepaint, acrossTheEdge, 30, 60, 60, 40) == 0"]},

        {"kind":"where","text":"The filter is configured every way the style API allows.","code":{"description":["'a narrow blur'","'a blur wide enough to be shrunk'","'a blur wider than the shrinking'","'a convolution kernel of our own'","'a kernel and a wide blur'","'a wide blur and an offset'","'a wide blur and a scale'","'a wide blur inside the interior'"],"filter":["{ f -> f.blur(4) }","{ f -> f.blur(24) }","{ f -> f.blur(64) }","{ f -> f.kernel(Size.of(3, 3), 0d,1d,0d, 1d,2d,1d, 0d,1d,0d) }","{ f -> f.kernel(Size.of(3, 3), 1d,1d,1d, 1d,1d,1d, 1d,1d,1d).blur(24) }","{ f -> f.blur(24).offset(12, -8) }","{ f -> f.blur(24).scale(1.4, 1.4) }","{ f -> f.blur(24).area(UI.ComponentArea.INTERIOR) }"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wide blur still reads as a blur however far the shrinking goes. (crossing into the shrinking) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A blur has to actually blur: the wider the radius a pane is asked for, the more\n            fine detail it has to remove. That is the requirement this scenario is here to\n            guard, and the reason it has to be guarded at all is a shortcut. Beyond a small\n            radius the parent is shrunk before it is convolved and stretched back out\n            afterwards, and the danger is that this performance optimisation quietly decides\n            the blur is no longer needed and stops. The rows land on the points where that\n            shrinking grows: crossing into it (8), a step deep (16), at the cap (64) and\n            beyond it (100). A blur which dropped out at any of them would leave the pane\n            looking sharp while the API still promises a blur - a promise the library quietly\n            broke, which is exactly the defect this scenario is here to catch.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A single pane spanning an entire noisy parent, filtering with this row's radius. The\n            pane covers all of the parent, so the region measured below stays well inside even\n            the widest blur's convolved band.\n        ","code":["var parentWith = { boolean filters, int radius ->","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", UI.panel().withStyle( it -> {","                    if ( filters )","                        return it","                            .backgroundColor(UI.Color.TRANSPARENT)","                            .parentFilter( f -> f.blur(radius) )","                    return it.backgroundColor(UI.Color.TRANSPARENT)","                } ))","            .get(JPanel)","    parent.setSize(480, 320)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A way of measuring fine detail: the mean difference between horizontally adjacent pixels.","code":["var fineDetailOf = { BufferedImage img, int x, int y, int w, int h ->","    double total = 0","    long   n     = 0","    for ( int row = y; row < y + h; row++ )","        for ( int col = x; col < x + w - 1; col++ ) {","            total += Math.abs(","                        (img.getRGB(col, row) & 0xff) -","                        (img.getRGB(col + 1, row) & 0xff) )","            n++","        }","    return n == 0 ? 0 : total / n","}"]},

        {"kind":"when","text":"The same parent, once behind a sharp pane and once behind this blur:","code":["var raw     = parentWith(false, 0)","var blurred = parentWith(true, blurRadius)"]},

        {"kind":"then","text":"\n            The blurred pane has kept only a small fraction of the parent's fine detail. This\n            is the point of a low pass, and it is what the shrinking has to keep putting out\n            no matter how far the radius has gone.\n        ","code":["fineDetailOf(blurred, 150, 100, 180, 120) * 2 < fineDetailOf(raw, 150, 100, 180, 120)"]},

        {"kind":"where","text":"A radius from each of the shrinking tiers.","code":{"description":["'crossing into the shrinking'","'a step deep'","'beyond where the cap is reached'"],"blurRadius":["8","16","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wide blur still reads as a blur however far the shrinking goes. (a step deep) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A blur has to actually blur: the wider the radius a pane is asked for, the more\n            fine detail it has to remove. That is the requirement this scenario is here to\n            guard, and the reason it has to be guarded at all is a shortcut. Beyond a small\n            radius the parent is shrunk before it is convolved and stretched back out\n            afterwards, and the danger is that this performance optimisation quietly decides\n            the blur is no longer needed and stops. The rows land on the points where that\n            shrinking grows: crossing into it (8), a step deep (16), at the cap (64) and\n            beyond it (100). A blur which dropped out at any of them would leave the pane\n            looking sharp while the API still promises a blur - a promise the library quietly\n            broke, which is exactly the defect this scenario is here to catch.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A single pane spanning an entire noisy parent, filtering with this row's radius. The\n            pane covers all of the parent, so the region measured below stays well inside even\n            the widest blur's convolved band.\n        ","code":["var parentWith = { boolean filters, int radius ->","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", UI.panel().withStyle( it -> {","                    if ( filters )","                        return it","                            .backgroundColor(UI.Color.TRANSPARENT)","                            .parentFilter( f -> f.blur(radius) )","                    return it.backgroundColor(UI.Color.TRANSPARENT)","                } ))","            .get(JPanel)","    parent.setSize(480, 320)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A way of measuring fine detail: the mean difference between horizontally adjacent pixels.","code":["var fineDetailOf = { BufferedImage img, int x, int y, int w, int h ->","    double total = 0","    long   n     = 0","    for ( int row = y; row < y + h; row++ )","        for ( int col = x; col < x + w - 1; col++ ) {","            total += Math.abs(","                        (img.getRGB(col, row) & 0xff) -","                        (img.getRGB(col + 1, row) & 0xff) )","            n++","        }","    return n == 0 ? 0 : total / n","}"]},

        {"kind":"when","text":"The same parent, once behind a sharp pane and once behind this blur:","code":["var raw     = parentWith(false, 0)","var blurred = parentWith(true, blurRadius)"]},

        {"kind":"then","text":"\n            The blurred pane has kept only a small fraction of the parent's fine detail. This\n            is the point of a low pass, and it is what the shrinking has to keep putting out\n            no matter how far the radius has gone.\n        ","code":["fineDetailOf(blurred, 150, 100, 180, 120) * 2 < fineDetailOf(raw, 150, 100, 180, 120)"]},

        {"kind":"where","text":"A radius from each of the shrinking tiers.","code":{"description":["'crossing into the shrinking'","'a step deep'","'beyond where the cap is reached'"],"blurRadius":["8","16","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wide blur still reads as a blur however far the shrinking goes. (beyond where the cap is reached) [2]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A blur has to actually blur: the wider the radius a pane is asked for, the more\n            fine detail it has to remove. That is the requirement this scenario is here to\n            guard, and the reason it has to be guarded at all is a shortcut. Beyond a small\n            radius the parent is shrunk before it is convolved and stretched back out\n            afterwards, and the danger is that this performance optimisation quietly decides\n            the blur is no longer needed and stops. The rows land on the points where that\n            shrinking grows: crossing into it (8), a step deep (16), at the cap (64) and\n            beyond it (100). A blur which dropped out at any of them would leave the pane\n            looking sharp while the API still promises a blur - a promise the library quietly\n            broke, which is exactly the defect this scenario is here to catch.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A single pane spanning an entire noisy parent, filtering with this row's radius. The\n            pane covers all of the parent, so the region measured below stays well inside even\n            the widest blur's convolved band.\n        ","code":["var parentWith = { boolean filters, int radius ->","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", UI.panel().withStyle( it -> {","                    if ( filters )","                        return it","                            .backgroundColor(UI.Color.TRANSPARENT)","                            .parentFilter( f -> f.blur(radius) )","                    return it.backgroundColor(UI.Color.TRANSPARENT)","                } ))","            .get(JPanel)","    parent.setSize(480, 320)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A way of measuring fine detail: the mean difference between horizontally adjacent pixels.","code":["var fineDetailOf = { BufferedImage img, int x, int y, int w, int h ->","    double total = 0","    long   n     = 0","    for ( int row = y; row < y + h; row++ )","        for ( int col = x; col < x + w - 1; col++ ) {","            total += Math.abs(","                        (img.getRGB(col, row) & 0xff) -","                        (img.getRGB(col + 1, row) & 0xff) )","            n++","        }","    return n == 0 ? 0 : total / n","}"]},

        {"kind":"when","text":"The same parent, once behind a sharp pane and once behind this blur:","code":["var raw     = parentWith(false, 0)","var blurred = parentWith(true, blurRadius)"]},

        {"kind":"then","text":"\n            The blurred pane has kept only a small fraction of the parent's fine detail. This\n            is the point of a low pass, and it is what the shrinking has to keep putting out\n            no matter how far the radius has gone.\n        ","code":["fineDetailOf(blurred, 150, 100, 180, 120) * 2 < fineDetailOf(raw, 150, 100, 180, 120)"]},

        {"kind":"where","text":"A radius from each of the shrinking tiers.","code":{"description":["'crossing into the shrinking'","'a step deep'","'beyond where the cap is reached'"],"blurRadius":["8","16","100"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wider blur is not sharper than a narrower one while both are meaningful. (crossing into the shrinking) [0]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The shrinking computes a wide blur by convolving a smaller kernel on a shrunk raster\n            and stretching the result back out. That is an approximation, and the approximation\n            is only acceptable if it does not *sharpen* the parent: a pane told to blur more has\n            to look more blurred, or the control reads the wrong way. Between radii where the\n            blur is still visually meaningful this has to hold strictly. A defect which made the\n            shrinking re-introduce detail would pass a stability check while being wrong.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A pane over a full noisy parent, rendered at each of the two radii.","code":["var parentWith = { int radius ->","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT)","                    .parentFilter( f -> f.blur(radius) ) ))","            .get(JPanel)","    parent.setSize(480, 320)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A measure of fine detail: the mean difference between horizontally adjacent pixels.","code":["var fineDetailOf = { BufferedImage img, int x, int y, int w, int h ->","    double total = 0","    long   n     = 0","    for ( int row = y; row < y + h; row++ )","        for ( int col = x; col < x + w - 1; col++ ) {","            total += Math.abs(","                        (img.getRGB(col, row) & 0xff) -","                        (img.getRGB(col + 1, row) & 0xff) )","            n++","        }","    return n == 0 ? 0 : total / n","}"]},

        {"kind":"when","text":"Both radii are rendered from the very same parent:","code":["var narrower = parentWith(narrow)","var wider    = parentWith(wide)"]},

        {"kind":"then","text":"The wider radius has not retained more fine detail than the narrower one.","code":["fineDetailOf(wider, 150, 100, 180, 120) <= fineDetailOf(narrower, 150, 100, 180, 120) * 1.5 + 0.2"]},

        {"kind":"where","text":"A pair straddling each of the meaningful shrink transitions.","code":{"description":["'crossing into the shrinking'","'crossing a second shrink'"],"narrow":["8","16"],"wide":["16","32"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wider blur is not sharper than a narrower one while both are meaningful. (crossing a second shrink) [1]",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The shrinking computes a wide blur by convolving a smaller kernel on a shrunk raster\n            and stretching the result back out. That is an approximation, and the approximation\n            is only acceptable if it does not *sharpen* the parent: a pane told to blur more has\n            to look more blurred, or the control reads the wrong way. Between radii where the\n            blur is still visually meaningful this has to hold strictly. A defect which made the\n            shrinking re-introduce detail would pass a stability check while being wrong.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A pane over a full noisy parent, rendered at each of the two radii.","code":["var parentWith = { int radius ->","    var parent =","            UI.panel(\"fill, ins 0, gap 0\")","            .withStyle( it -> it","                .backgroundColor(UI.Color.BLACK)","                .noise( n -> n","                    .function(UI.NoiseType.STOCHASTIC)","                    .scale(0.3)","                    .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","            .add(\"grow\", UI.panel().withStyle( it -> it","                    .backgroundColor(UI.Color.TRANSPARENT)","                    .parentFilter( f -> f.blur(radius) ) ))","            .get(JPanel)","    parent.setSize(480, 320)","    parent.doLayout()","    return Utility.renderSingleComponent(parent)","}"]},

        {"kind":"and","text":"A measure of fine detail: the mean difference between horizontally adjacent pixels.","code":["var fineDetailOf = { BufferedImage img, int x, int y, int w, int h ->","    double total = 0","    long   n     = 0","    for ( int row = y; row < y + h; row++ )","        for ( int col = x; col < x + w - 1; col++ ) {","            total += Math.abs(","                        (img.getRGB(col, row) & 0xff) -","                        (img.getRGB(col + 1, row) & 0xff) )","            n++","        }","    return n == 0 ? 0 : total / n","}"]},

        {"kind":"when","text":"Both radii are rendered from the very same parent:","code":["var narrower = parentWith(narrow)","var wider    = parentWith(wide)"]},

        {"kind":"then","text":"The wider radius has not retained more fine detail than the narrower one.","code":["fineDetailOf(wider, 150, 100, 180, 120) <= fineDetailOf(narrower, 150, 100, 180, 120) * 1.5 + 0.2"]},

        {"kind":"where","text":"A pair straddling each of the meaningful shrink transitions.","code":{"description":["'crossing into the shrinking'","'crossing a second shrink'"],"narrow":["8","16"],"wide":["16","32"]}}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"A wide blur works on a pane flush against the far edge of its parent.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            A blur gathers its colour from far beyond the pixels it is drawn into. For a pane\n            that lies flush against the far edge of its parent that padding reaches past the\n            parent, where there are no pixels to read - the shrinking grows the window onto a\n            grid, and the grown cells past the far edge simply have nothing to contribute. This\n            has to be handled without an exception and without the pane changing what it shows\n            when only part of it is repainted.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"\n            A noisy parent with a single wide-blurred pane pinned into its bottom-right corner,\n            so the blur's read-ahead padding is cut short on two sides at once.\n        ","code":["var parent =","        UI.panel(\"fill, ins 0, gap 0\")","        .withStyle( it -> it","            .backgroundColor(UI.Color.BLACK)","            .noise( n -> n","                .function(UI.NoiseType.STOCHASTIC)","                .scale(0.3)","                .colors(UI.Color.BLACK, UI.Color.WHITE) ) )","        .add(\"pos 360 200\",","            UI.panel()","            .withPrefSize(120, 120)","            .withStyle( it -> it","                .backgroundColor(UI.Color.TRANSPARENT)","                .parentFilter( f -> f.blur(24) ) ) )","        .get(JPanel)","parent.setSize(480, 320)","parent.doLayout()"]},

        {"kind":"and","text":"A way of painting the parent through a clip of our choosing.","code":["var paintedThroughClip = { int x, int y, int width, int height ->","    return UI.runAndGet(() -> {","        var image = Utility.createDeterministicImage(480, 320)","        var graphics = Utility.createDeterministicGraphics(image)","        graphics.setClip(x, y, width, height)","        Utility.paintWithoutWindow(parent, graphics)","        graphics.dispose()","        return image","    })","}"]},

        {"kind":"and","text":"The same pixel count, over a rectangle of our choosing.","code":["var pixelsDifferingInside = { BufferedImage a, BufferedImage b,","                              int x, int y, int width, int height ->","    int count = 0","    for ( int row = y; row < y + height; row++ )","        for ( int column = x; column < x + width; column++ )","            for ( int channelShift : [0, 8, 16, 24] ) // blue, green, red and alpha","                if ( Math.abs(","                        ((a.getRGB(column, row) >> channelShift) & 0xff) -","                        ((b.getRGB(column, row) >> channelShift) & 0xff)","                     ) > 8 ) {","                    count++","                    break","                }","    return count","}"]},

        {"kind":"when","text":"The whole component is painted in one go:","code":["var fullRepaint = paintedThroughClip(0, 0, 480, 320)"]},

        {"kind":"and","text":"And then only a rectangle partway down and in across the corner pane:","code":["var patched = paintedThroughClip(380, 220, 60, 50)"]},

        {"kind":"then","text":"Painting at all did not throw, and the pane shows the same thing under a partial repaint.","code":["pixelsDifferingInside(fullRepaint, patched, 380, 220, 60, 50) == 0"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
