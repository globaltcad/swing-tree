{
  "className":"swingtree.Scroll_Panels_Threading_Spec",
  "title":"Scroll Panels Entry Bindings Across Threads",
  "narrative":"The `JScrollPanels` component supports two ways of binding its entries.\n\n    The recommended pathway binds a `Var<Tuple<M extends HasId<ID>>>` property:\n    the entries are immutable value objects owned by the application thread,\n    each entry is handed to the view supplier as a property of its own, and\n    all entry state - including a selection flag, if you need one - is simply\n    data in the value objects. There is no UI owned entry state to synchronize,\n    which is what makes this pathway fully thread safe in the decoupled mode.\n\n    The older pathway hands `EntryViewModel` implementations to the component,\n    whose contract requires the UI thread to write position and selection state\n    directly into the view models while building their views. That handshake\n    cannot be made thread safe, which is why the interface is deprecated:\n    it keeps working in the coupled threading modes, but binding it in a\n    declaration which uses a decoupled event processor logs a loud warning.\n\n    This specification documents both pathways and their threading contracts.",
  "subjects":["swingtree.UIForScrollPanels","swingtree.components.JScrollPanels","swingtree.api.mvvm.EntryViewModel","swingtree.threading.EventProcessor"],
  "statistics":{
    "runs":"3",
    "successRate":"100.0%",
    "failures":"0",
    "errors":"0",
    "skipped":"0",
    "duration":"?"
  },
  "headers":[],"tags":{},"see":[],
  "features":[ 
    {
      "id":"Selection of tuple bound entries is plain application state, updated safely across the thread boundary.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            With the tuple based binding there is no UI owned selection state\n            at all: the selection flag is data in the immutable entry objects,\n            owned by the application thread like everything else. Selecting one\n            entry and deselecting all others is a single atomic tuple update.\n\n            A click in the view therefore does not touch any state directly.\n            It merely queues the tuple update on the application event queue,\n            and once the application thread has applied it, the change events\n            carry the new entry states back to the UI thread, where the\n            existing sub-views are recycled (based on the entry ids) and\n            restyled. This feature walks through that full round trip.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A log spy, so that both silent errors and unexpected warnings fail this test.","code":["var log = LogSpy.attach()"]},

        {"kind":"and","text":"A tuple property of entries, of which the second one starts out selected.","code":["var entries = Var.of(Tuple.of(","    new Entry(\"a\", \"Alpha\", false),","    new Entry(\"b\", \"Beta\",  true ),","    new Entry(\"c\", \"Gamma\", false)","))"]},

        {"kind":"and","text":"A view supplier rendering each entry as a label, whose click selects only that entry.","code":["BoundViewSupplier<Entry> supplier = { Var<Entry> entry ->","    String id = entry.get().id()","    UI.label(entry.viewAsString({ e -> (e.selected() ? \"> \" : \"\") + e.text() }))","    .onMouseClick({ it ->","        entries.update({ tuple -> tuple.map({ e -> e.withSelected(e.id() == id) }) })","    })","}"]},

        {"kind":"and","text":"Scroll panels built around the entries in decoupled mode.","code":["var panels = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.scrollPanels().addAll(entries, supplier)","    ).get(JScrollPanels)","})"]},

        {"kind":"expect","text":"The entries render with the initial selection marker on the second entry.","code":["_entryTextsOf(panels) == [\"Alpha\", \"> Beta\", \"Gamma\"]"]},

        {"kind":"when","text":"We remember the sub-view of the first entry, and then the user clicks its label on the UI thread.","code":["var viewOfEntryA = panels.getContentPanel().getComponent(0)","UI.runNow({","    var label = (JLabel) ((java.awt.Container) panels.getContentPanel().getComponent(0)).getComponent(0)","    label.dispatchEvent(new MouseEvent(label, MouseEvent.MOUSE_CLICKED, System.currentTimeMillis(), 0, 1, 1, 1, false))","})"]},

        {"kind":"then","text":"The entries themselves have not been touched yet! The tuple update is waiting in the application event queue.","code":["entries.get().toList().collect({ it.selected() }) == [false, true, false]"]},

        {"kind":"when","text":"This test thread, playing the application thread, processes the event queue.","code":["EventProcessor.DECOUPLED.joinUntilDoneOrException()"]},

        {"kind":"then","text":"The selection moved to the first entry, atomically, in a single tuple update.","code":["entries.get().toList().collect({ it.selected() }) == [true, false, false]"]},

        {"kind":"when","text":"The UI thread catches up with the resulting change event.","code":["UI.sync()"]},

        {"kind":"then","text":"The views show the new selection marker...","code":["_entryTextsOf(panels) == [\"> Alpha\", \"Beta\", \"Gamma\"]"]},

        {"kind":"and","text":"...and the sub-view of the first entry was recycled, not rebuilt, thanks to its stable entry id.","code":["panels.getContentPanel().getComponent(0).is(viewOfEntryA)"]},

        {"kind":"and","text":"Nothing was logged along the way, this pathway is neither deprecated nor thread unsafe.","code":["log.errors().isEmpty()","log.warnings().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["log.detach()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"The deprecated `EntryViewModel` pathway keeps working in the coupled threading modes, without any warning.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            Deprecation is a migration aid, not a breaking change: view models\n            implementing the old `EntryViewModel` contract continue to work\n            exactly as before when the UI runs in a coupled threading mode,\n            where the UI thread and the application thread are one and the\n            same, so the contract's thread safety problem cannot bite.\n            No warning is logged in this configuration.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A log spy and a property list of legacy view models.","code":["var log = LogSpy.attach()","var entries = Vars.of(new LegacyEntry(\"A\"), new LegacyEntry(\"B\"), new LegacyEntry(\"C\"))"]},

        {"kind":"and","text":"Scroll panels built around them in the (strict) coupled mode, which is the global default here.","code":["var panels = UI.runAndGet({","    UI.scrollPanels().addAll(entries, m -> UI.label(m.text)).get(JScrollPanels)","})"]},

        {"kind":"expect","text":"The entries render.","code":["panels.numberOfEntries == 3","_entryTextsOf(panels) == [\"A\", \"B\", \"C\"]"]},

        {"kind":"when","text":"The legacy selection machinery is used to select the second entry.","code":["UI.runNow({ panels.setSelectedFor(JLabel, { it.text == \"B\" }) })"]},

        {"kind":"then","text":"The selection arrived in the legacy view model, the old contract is fully intact.","code":["entries.at(1).get().isSelected().is(true)"]},

        {"kind":"when","text":"A new legacy view model is added to the property list.","code":["UI.runNow({ entries.add(new LegacyEntry(\"D\")) })"]},

        {"kind":"then","text":"The new entry appears in the view.","code":["panels.numberOfEntries == 4"]},

        {"kind":"and","text":"No deprecation warning and no error was logged, the coupled modes stay quiet.","code":["log.warnings().findAll({ it.contains(\"EntryViewModel\") }).isEmpty()","log.errors().isEmpty()"]},

        {"kind":"cleanup","text":"","code":["log.detach()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    },
  
    {
      "id":"Binding `EntryViewModel`s in a decoupled UI declaration logs one loud warning, but still functions.",
      "result":"PASS",
      "duration":"?",
      "iterations":{
      "tags":{},"see":[],"extraInfo":["\n            The `EntryViewModel` contract requires the UI thread to write\n            position and selection state directly into the view models while\n            building their entry views, and the view supplier reads that state\n            back mid-construction. This handshake cannot be handed over to the\n            application event queue without breaking it, which is why the\n            combination of `EntryViewModel`s and a decoupled event processor\n            is fundamentally thread unsafe.\n\n            SwingTree does not break existing applications over this: the\n            binding still works. But it logs a single loud warning at\n            declaration time, pointing to the thread safe tuple based\n            alternative documented on the `EntryViewModel` interface.\n        "]
      },
      "blocks":[
        {"kind":"given","text":"A log spy and a property list of legacy view models.","code":["var log = LogSpy.attach()","var entries = Vars.of(new LegacyEntry(\"A\"), new LegacyEntry(\"B\"), new LegacyEntry(\"C\"))"]},

        {"kind":"when","text":"Scroll panels are built around them in decoupled mode.","code":["var panels = UI.runAndGet({","    UI.use(EventProcessor.DECOUPLED, ()->","        UI.scrollPanels().addAll(entries, m -> UI.label(m.text))","    ).get(JScrollPanels)","})"]},

        {"kind":"then","text":"Exactly one loud warning was logged, at declaration time, explaining the problem and the migration path.","code":["log.warnings().count({ it.contains(\"EntryViewModel\") && it.contains(\"NOT thread safe\") }) == 1"]},

        {"kind":"and","text":"The binding itself still works, the entries render.","code":["panels.numberOfEntries == 3","_entryTextsOf(panels) == [\"A\", \"B\", \"C\"]"]},

        {"kind":"when","text":"The application thread appends another legacy view model, and the UI thread catches up.","code":["entries.add(new LegacyEntry(\"D\"))","UI.sync()"]},

        {"kind":"then","text":"The structural update was applied, the legacy pathway remains functional.","code":["panels.numberOfEntries == 4"]},

        {"kind":"and","text":"No error was silently logged, and the warning was not repeated for the update.","code":["log.errors().isEmpty()","log.warnings().count({ it.contains(\"EntryViewModel\") && it.contains(\"NOT thread safe\") }) == 1"]},

        {"kind":"cleanup","text":"","code":["log.detach()"]}
      ],
      "problems":{"dataValues":[], "errors":[]}
    }
  
  ],
  "generator":"https://github.com/renatoathaydes/spock-reports"
}
